# Vit-DAW IPC 通信契约

本文档已迁移至专门的文档库。请查阅：[docs/VIT_IPC_CONTRACT.md](docs/VIT_IPC_CONTRACT.md)。

Phase 4.2 新增 `move_clip` / `resize_clip`（双时间基准 + Cut-only 重叠策略）定义见该文档对应章节。

**工程状态机 / 最近工程 / `save_project`·`save_as_project`·`open_project`** 等契约以 `docs/VIT_IPC_CONTRACT.md` 中「工程状态机与最近工程」一节为准。**`get_project_state`**（全息拓扑）见同文档对应小节。
