# 2026-09-23 决策记录：手测链三次取证汇总（演示路径打通 + 两长线登记 + 一结论修正）

- 会话：Mac 决策会话；用户驱动三轮手测取证（17:2x / 17:38 / 18:2x），全部工件 `~/Documents/vit-handtest-forensics-20260923/`
- 上游：REALSTEMS→EQ-1→FIX-D1-PLUGIDENT→FIX-FE-SCANPATH 四卡链条收口——**模型驱动 EQ 全链『假设→准入→装载→A/B』在前端入口首次走通**（2026-09-24 导师课演示路径就绪）

## 分界澄清（用户质询"Q10 是否强引导"，记录结论）

- 模型自主面=语义决策（做不做 EQ/频段/幅度/理由；历次实证有变化：合成 55Hz、真实 250Hz −1.5、判定轮 3kHz +1.5），模型不见插件名（PCA 门设计）；
- "static_eq 族用哪个插件"=白名单 v5 单槽位机器绑定（EQ-1 证据管线选定 Q10）——结构稳定非模型偏好；
- 素材侧注记：spv1_p01 为受控问题素材（注入可检测问题），模型在该素材上反复提出 EQ 属预期；p02/p06 为不同配方场景，可作演示泛化素材。

## 三层故障链（同一晚串行暴露，均已闭环）

1. 前端设置页 mac 路径分支缺失（FIX-FE-SCANPATH-MAC-1 修复，前端仓 a9890ba）；
2. knownPluginList64 PC 幽灵条目污染（994 条遮蔽 mac 扫描）——决策侧机器态清污（备份在案）；
3. 扫描取消触发崩溃隔离拉黑主壳（静默跳过后续扫描）——决策侧清黑名单+pedal（备份在案）。
→ 2/3 根治转卡 **FIX-KERNEL-PLUGINLIST-HYGIENE-1**（P2，todo，双端共建）。

## 模型叙事抢跑（登记，暂不开卡）

装载失败轮模型先称"已加载静态 EQ"、内核随即失败回滚、GUI 正确显示空——叙述先于执行确认。呈现层/提示层应收敛（成功叙述以执行收据为准）。待 webui/提示层相关卡工作时一并考虑。

## 结论修正（先前记录更正）

- **修正**："mac 扫描不落盘、每进程空表"（REALSTEMS 验收期判断）——错误。`knownPluginList64` 落盘持久化存在（跨工程共享、重启保留）；当时判断系旅程脚本隔离 kernel_workspace 的特例。
- 开源就绪 backlog 原"扫描落盘"关切收敛为"表卫生+取消语义"（即 HYGIENE 卡范围）；其余两件（白名单构建器未入仓/认证旅程未产品化）不变。
