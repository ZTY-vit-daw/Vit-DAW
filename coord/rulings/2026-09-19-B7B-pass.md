# Ruling：PORT-B7B pass（2026-09-19）

- 卡：`coord/cards/done/2026-09-19-PORT-B7B-webui-embedded-mac.md`
- 实现：前端仓 `port/b7b-cef-webview-host` @`b55c2b7`（bc82057 主体 + 6 轮手测反馈 fix）；主仓零改动
- 裁定：**pass；两笔用户直令扩域（fix5/fix6）追认；真栈手测闸门以用户 2026-09-19"全部完成"宣告闭合**

## 决策侧核验（mac 本地直审前端仓）

1. **平台分支**：`_ensure_agent_webui_host()` Windows 臂原行逐字保留（else 内仅缩进）；mac 臂 `load().new()` 实例化适配类——GDScript class_name 不入 ClassDB 故 ClassDB.instantiate 不可用的判断正确，下游消费全经 has_method/has_signal 守卫的核实采信。
2. **适配类**（454 行）：契约功能子集 11 方法+5 信号实现；preload_script 垫片（chrome.webview 兼容层）+ ipc 透传闭环有正面实证（探针回包 bridge/ready/ipc 三真）；降级清单五项显式声明（overlay no-op / eval_js 异步无返回 / create 缺省形态 / user_data_subdir 回显 / get_selected_plugin_* 非本类成员按 B7A 契约勘正报备）——报备采认。
3. **6 轮 fix 质量采认**：fix1 窗口半注册态三连缺陷（popup 同帧/半注册刷屏/关窗失效）根因链栈级实锤后修复；fix2/3 shadowing→unused_parameter 的用户编辑器口径教训；fix4 deprecated 清零（非 TCP 告警双零）；每轮 headless 冒烟 39/39 复验在档。
4. **扩域追认**：fix5 `vsp_realtime_adapter.gd`（mac 默认关 ws + `VIT_GUI_VSP_REALTIME_WS_ENABLE` 强开，vsphub 未移植 mac 的构造性刷屏根治）与 fix6 `start_page.gd`（vsphub 两告警 mac 安静单行、Windows 保留真异常告警）——两笔均用户直令、单独 commit、diff 最小（+6 / +11−2）、语义正确，**追认入档**。vsphub mac 移植后应翻回默认值（记入收官记录遗留项）。
5. **决策侧复验**：check-only 适配类零错误；整工程 headless boot 零脚本错误、零 TCP no-delay（fix5 生效实证）。
6. **噪声边界采认**：ANGLE objc 每轮一行=上游已知（B2P §4.3）；退出期 ObjectDB 2 行=**先在 dock 核心区泄漏（viewport_registry），与本卡无关，开后续卡候选**。

## 手测闸门

用户全程在环（fix1-6 均由其复测反馈驱动），2026-09-19 宣告"B7 已全部完成"——闸门闭合。内嵌窗口已替换 Safari 兜底（用户 fix1 反馈即证窗口已弹出，后续轮次均为打磨）。
