# 裁定：PORT-MAC-HARNESS-FLAKY-1 pass（2026-09-17，决策侧）

- 对象：取证卡（无实现分支；工作树零源码改动）
- 决策侧独立核验：① 三个代码锚点实地核对成立（harness.go:8996 内核兜底 / harness.go:8510 scan_plugins 派发先于 enforceAgentProcessorLoadGate / index.go:246-258 Get() 精确失败后模糊兜底 Limit 1）；② 本机无 `~/.vit/plugin_semantics.json` 前提下复现变体 A：两测试确定性 FAIL×2（与"非时序、机器状态确定性依赖"结论一致）；③ 工件五件在位（Export/flaky1-forensics-20260917/），repro-run1 实锤内核收到 `[scan_plugins, rack_add_node, scan_plugins]` 三条、变体 B PASS
- 结论采纳：**"FLAKY" 是误称**——根因为共享代码缺陷（规范化阶段内核副作用 + 测试对机器全局索引的隐性依赖），Windows 通过系开发机状态巧合；任何干净机器/CI 必失败
- 处置：**修复方案 A（生产根治）已定案**并开卡 `PORT-HARNESS-FIX-1`（todo/）：删除 rackPluginSemanticEntry 内核兜底，索引未命中走默认 Z3 分支；接受无索引机器 Z2→Z3 的 zone tradeoff（zone 解析正路=校准/显式装载建索引，非内核副作用）；`Get()` 模糊兜底跨插件错配隐患（index.go:253）登记为独立待裁项，不在修复卡内
- 卡面验收标准（代码锚点/最小复现/原始输出保存）全部满足；取证纪律（零源码改动、环境变量隔离、未触碰机器全局状态）合规
