# 裁定：PORT-C4 pass（2026-09-17，决策侧）

- 对象：`port/c4-readonly-smoke-mac`（实现 `560ddef`；领取基线 af5aa65）
- diff 审查：仅 `scripts/g_runtime_readonly_smoke_mac.sh`（新增 319 行）+ `scripts/SMOKE_TESTS.md` 条目，ps1 原件零触碰，全在卡面文件域；白名单 4 探针与 ps1 一一对应，路径白名单 fail-closed；自扫描守卫碎片拼装防自匹配（短 data flag 与 `mktemp -d` 前缀冲突的豁免有注释，对齐 ps1 长格式口径）；运行态全隔离（mktemp workdir + `VIT_ORCHESTRATION_STORE_PATH` 钉入 + 独立 agent_cwd）；端口被占拒启、只清理自启进程
- 决策侧独立复验（隔离 git worktree @560ddef，2026-09-17，与执行侧工件互不覆盖）：
  1. 真实 agent 全链路 `--start-agent --timeout 30` → **exit 0**（构建→隔离启动→4 白名单 GET 全 2xx→摘要→TERM 收尾；事后 pgrep 无 vitagent 残留、7878 无监听）——agent 在 mac 首次被决策侧独立证实可完整起停探活（R5 第二道护栏落地）
  2. fixture 等价性（`g_runtime_readonly_fixture_server.py` :7879）→ **exit 0**，12 项摘要字段与已知轨迹逐一相符（task-g-readonly / slice_count=2 / event_count=1 / observation_in_progress）
  3. 守卫负例：注入 `-X POST` 与 `--data` 各自 fail-closed **exit 1**
  4. AGENTS §10：worktree `git status` 零污染
- events 契约修正接受：`chat/events.go` 无 conversation_id 答 400，ps1 裸 `?limit=200` 只满足忽略全部查询参数的 fixture server——**ps1 版对真实 agent 存在潜伏失败**；mac 版补 `conversation_id=<probe>&limit=200` 是正确修正（limit=200 < 真实 buffer 上限 500）
- 端测边界：覆盖 agent HTTP 白名单探活面（ps1 1:1 口径）；ZMQ 5555/5556 为内核 connect-out、不在 ps1 白名单故未纳入；webui 渲染面与用户旅程面无涉——**接受**（本卡无相关改动）
- 处置：cherry-pick `560ddef` 入 main（`8e198a5`）；ps1 侧 events 探针潜伏缺陷（域外）待开小卡修复，建议 PC 侧 flash/L0 执行；port 分支清理与 A2 验收后一并处理
