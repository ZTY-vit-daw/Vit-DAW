# Ruling：PORT-B7A pass（2026-09-19，PC 决策侧）

- 卡：`coord/cards/done/2026-09-19-PORT-B7A-webview-host-source-adoption.md`
- 实现：`port/b7a-webview-host` `300b510b`（单 commit，8 文件 +66270 全在 `extension/` 域）→ 本裁定随附 cherry-pick 入 main
- 裁定：**pass**

## 决策侧核验（全部亲验，非采信回执）

1. **验收①原样收编**：5 个收编文件（`vit_webview_host.{cpp,h}` + `third_party/webview2/` 3 文件）blob hash 与前端仓 HEAD **逐位一致**（c06643fc/c592a088/45f02978/5b3f17f9/c403f4fd，决策侧独立比对）；前端仓工作树该 5 文件无未提交改动。`register_types.cpp` +2 行（include+register，与前端仓形态逐字一致）、`SConstruct` +16 行（webview2 守卫接线合并进 B1 版本，`api_version=4.6` 行未动）——均为卡面授权的最小接线改动。
2. **验收②构建（决策侧复跑）**：仓库外干净 worktree 检出 `300b510b` 从头构建（godot-cpp 经 junction 共享主仓 clone，master@507ed9d = B1 同 pin），`scons platform=windows target=template_debug` **exit 0**；dll 486400 B（执行侧回执 485888 B，512 B 差为构建路径/PDB 时间戳，良性）。dll 证据亲验：`VitWebViewHost` 字节串 12 处、`vit_extension_library_init` 导出入口 1 处、WebView2 真后端特征（`WebView2Loader` 3 处 / `CreateCoreWebView2EnvironmentWithOptions` 2 处 / UTF-16 宽串 VitDAW）、stub 串 "not linked in this build" **零命中**——真后端确证。
3. **验收③契约文档**：`extension/src/vit_webview_host.md` 21 方法/5 信号清单与源码 `_bind_methods` 逐项核对一致（bind_method 21 个、ADD_SIGNAL 5 个，签名/参数名全符）；4 消费方 ClassDB 引用点（vit_dock_root.gd:2545/3117、browser_panel.gd:437、artifact_panel.gd:899）逐一实证在位；**"非本类成员"分界独立复核成立**——`get_selected_plugin_*` 系 vit_dock_root 自身方法委托 `_find_agent_plugin_context_host()` 面板节点、`new_window_requested` 属 `_app_header`（vit_dock_root.gd:1231 实证），卡面原清单此三项确实不在 VitWebViewHost 契约内，文档防 B7B 误实现的标注正确。
4. **验收④审计一致性**：文档 §0 记录核对结论——源码与前端仓 HEAD 无漂移，漂移的是 PORT_AUDIT §1.3(a) 的引用判定（4 消费方均 ClassDB 字符串引用，文件名搜索盲区）；决策侧已独立确认 4 处引用点，结论成立。
5. **停止条件扫描（决策侧亲扫）**：源码内 "token" 命中全为 WinRT `EventRegistrationToken`（良性）；零硬编码盘符路径；单一宿主类（无 overlay/第二变体，overlay 系同类内模式）；无凭据/密钥。`.obj`/`bin`/`.sconsign`/`godot-cpp` 均未入仓（bin/ 命中 .gitignore:9）。

## 过程注记（非阻塞）

- 验收复跑首轮曾误在主仓 main 树构建（无 webview 宿主，329728 B）并覆盖执行侧留在 bin/ 的产物；已改由干净 worktree 权威复验，并将**决策侧核验过的 486400 B dll** 回填主树 `extension/bin/`（gitignored 构建产物，非权威工件——权威证据为本 ruling 与可复现构建）。
- 执行侧在主工作树切分支构建后切回 main，遗留 untracked `.obj`×3 / `.sconsign.dblite` / `godot-cpp/`（构建中间物，回执已如实声明"未入仓"）。主树 bin/ 产物与中间物不构成验收障碍。
- dll 仅静态证据；Godot 运行时加载验证不在本卡范围（B7A 端测边界声明成立），真栈面归 B7B/后续集成。

## 解锁

B7B（mac VitCefWebViewHost 适配层）契约前置就位——契约文档随本 cherry-pick 入 main，mac 侧可领取。
