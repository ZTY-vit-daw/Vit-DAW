# Ruling：PORT-PS1-SYNC-3 pass（2026-09-20，PC 决策侧；④⑤ 双件跑绿，端测断言族收官）

- 卡：`coord/cards/done/2026-09-20-PORT-PS1-SYNC-3-double-hop-assertions.md`
- 实现：`port/ps1-sync-3` @`d3a5c222`+`c490e387`+`f69d9ea1`（3 commit，scripts/ 4 文件断言块+SMOKE_TESTS，域纯）→ 本裁定随附 cherry-pick 入 main
- 裁定：**pass**——验收①②③④全过：④⑤ 断言按用户裁定的双跳语义（提案面→工具面→应用回读→d1 终态）双端重设计，PC 双件权威 run exit 0。

## 决策侧核验（全部亲验）

1. **④ exit 0（`mix_single_tick_e2e_20260920_195512`）**：console 链证据逐跳亲核——observe 停提案面（needs_confirmation）→ hop-1 确认后停工具面（`improvement_proposal_native_tool_confirmation_required`，`[mix.tick.pending] stored` 锚按设计移至此跳，载荷"Track 1010 电平 -0.50 dB"）→ hop-2 确认后 `explicit confirmation routed`→**已应用并回读验证 0 dB→-0.5 dB**→d1 终态（workflow free_state_d1_s1）。三面分层与用户裁定语义逐字吻合。
2. **⑤ exit 0（`product_path_20260920_203153`）**：summary.json `status: passed` 亲核；vocal focus settled 响应亲核（`stop_reason=needs_confirmation`/`needs_confirmation=true`/`settled_from="continuation+events"`/提案面确认回复全文在案）；**derive 反转 A/B 直接证据**——run3 derive=0（新断言绿；旧断言此处必红）。
3. **断言语义 diff 亲核**：④ 双跳断言（提案面 kind/工具面 workflow=mix_tick+有界载荷校验 delta_db±2/delta_pan±0.15/target_pan±1/d2 终态）；⑤ **derive 反转实证**（分支版 :3260-3261：确认前 derive/apply/daw_invoke/track_volume 任一非零即 Fail——与旧"必含 derive"完全反转，判据注释引 ④ 探针）；route 断言维持 ccb.observation_catalog 别名组（PS1-SYNC-1 契约不动）；主 confirm 块保留并文档化"不可达口径"（新工作流下主回合停提案面，legacy 路径不触发——最小 diff 处置合理）。
4. **红证驱动追加两处**（合理且必要）：④ vocal 守卫分支容忍（run2 conv 194908：推断后停提案面为合法分支，no-stored-tick 反断言保持严格）；⑤ clarify ask 三分支容忍（run1 conv 200812：clarify/提案停车/能力边界 done 三分支，ask 期 no-tick 反断言保持严格）。两处均为新工作流下的真实行为分支，非断言放宽套绿——反断言（不该发生的仍红）均保持。
5. **settle 触发收窄**：真切片=limit_reached 或 waiting_continue+空 stop_reason；同步终态 d1 park（waiting_continue+d1_post_action_evaluation_required）为完整响应直接返回——修复旧触发把确认②响应误入 720s 轮询的缺陷。④ mac settle 默认 300→720 补齐（SYNC-2 遗留）。
6. **§8 合格**：④ 1 红（observe 散文分支=模型随机分支，定性准确）→1 红（推断停车→修正）→绿；⑤ 1 红（capability_blocked→修正）→1 红（**agent 侧 goal=failed**，error=null 7 轮链——非断言非脚本，见观察项）→绿；环境类（bash 路径/VitAgent 文件锁竞态×2）未计有效轮；无同版本同断点两败。

## 观察项（记录在案，不阻塞）

1. **⑤ round2 goal=failed**：agent 侧回合链 7 轮后 failed（error=null），run3 未复发；如复发属 agent 侧取证对象（回执已注明根因不在工件可及范围）。
2. **clarify-first 非确定性**（回执契约注记）：两轮证据证实歧义人声 ask 的 clarify/推断/能力边界三分支均为合法行为，现无断言强制 needs_clarification 必现；若产品要求 clarify-first 强制，需另开 agent 侧卡——现记录为已知行为方差。

## 记分板（端测时代 · PC 侧）

PC 套件断言族全绿：②preflight（VIT1）③stems（queued-run）⑥live_material（per-track ready/v1.5）④mix_single_tick（双跳）⑤product_path（双跳+CCB 路由）。settle 基建（720s/合成面/触发收窄）双端就位。
