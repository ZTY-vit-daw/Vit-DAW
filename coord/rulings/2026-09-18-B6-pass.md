# Ruling：PORT-B6 pass + 结论采纳（2026-09-18）

- 卡：`coord/cards/done/2026-09-18-PORT-B6-python-bridge-retirement-assessment.md`
- 实现：`port/b6-python-bridge-assessment` @`bd9654f`（报告单文件 109 行）→ cherry-pick 入 main `904dd06`
- 裁定：**pass，且决策侧采纳报告结论：mac 侧退役 python bridge。**

## 核验与采纳

1. 报告四节齐备且锚点完备：①12 项职能逐函数清单；②Go 对照 12/12 已覆盖零空白（`agent/internal/bridge` 639 行=完整移植+超集；"Go 零引用"独立 grep 命令+输出入文；VSP 收口报告:108 权威口径佐证）；③影响面（start_page.gd:355-380 剔除、python_embed 唯一消费者=桥、PC 零影响）；④建议+风险表。停止条件未触发。
2. **对 B3 卡的设计输入即时生效**：B3 按"剔除该链"设计，无 python 运行时 mac 化子项；vitagent（darwin 构建）为唯一桥接承担者。**B3 必含前置检查项**：start_page.gd 桥拉起条件现场确认（报告 R1——python 桥与 vitagent 同绑 UDP 4445 不能并存，若现链为无条件并行拉起须先改条件分支再剔除）。
3. 阈值差异（inline 60KB→32KB、落盘目录名）与 legacy UDP 抑制差异均属有意设计/契约同构，采认报告"无需动作"判断。
4. **文档卫生两项记档，随 B3 收口由决策侧修订**：`VIT_IPC_CONTRACT.md:6`（发布入口仍写 python）与《发布模式一键启动配置清单》（python 链口径）。

## 边界声明采认

Godot 工程不在 Mac、start_page.gd 事实引 PORT_AUDIT §1.3(d) 的证据边界如实声明；R1 留 B3 现场确认的处理正确。
