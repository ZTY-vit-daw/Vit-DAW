# Ruling：PORT-C2-PCR pass（2026-09-18）

- 卡：`coord/cards/done/2026-09-18-PORT-C2-PCR-pc-rerun-and-orphan-check.md`
- 实现：`port/c2-pc-rerun` @`802c616`（脚本单文件 206+/41-）→ cherry-pick 入 main `8b09480`
- 裁定：**pass**

## 四项验收核验

1. **① 孤儿改动归属**：`agent/cmd/vitagent/main.go` 的 required 注册有界重试 = 2026-09-17 晚 `sess_655bbfcf` v0.93 打包会话的"安装包启动卡死"修复。证据链三重（tool artifacts 时间戳↔文件 mtime 秒级吻合、重编译/装配时间线、打包版功能日志的重试序列）+ 从未提交的 git 考古，采认。卡面纪律（全程不触碰不回退）执行正确。**处置裁定：收编**——下一张 PC 卡将此 diff 正式 commit（附归属溯源注记）；v0.93 iss/redist 重写在 Export/ 不入库。
2. **② PC 全链**：run `pca_calibration_chain_win_20260918-190731` 六门 PASS exit 0（991 主体扫描 / 12 族 24 体 / 24/24 认证晋升 / 只读冒测 / 停栈同 mac 型）；run1 github.com:443 中断按环境类记录，`--fetch-src` 离线旁路（`FETCHCONTENT_SOURCE_DIR_*`，与 `--cmake-proxy` 同性质）闭合且 run1 `~/.vit` 零触碰——处置合规。
3. **③ 对照**：24/24 名单全同、族分布逐项一致、identifier 锚点跨端命中（VST3 uid 平台稳定又一实证）。
4. **④ ~/.vit delta**：+48/−0/改 5，逐主体判定全为"同主体收据取代更新"（v2 store 31→36 的 5 条新 Mono 收据 = 旧基线五族只认证过 Stereo 的历史欠账，本次补齐属设计内），未触发回滚——权威基线保护条款执行到位。
5. **适配量**：206+/41- 为 uname 守卫平台分支级（工具面/路径形态/winpid 断言/MSVC 多配置/--fetch-src/win 侧去重容忍），darwin 路径逐字未动（diff 抽查采认）；"两端可重复"从设计变实证。

## U2"23"出处裁定修正（本卡 ③ 发现）

**修正 2026-09-18-C2-pass ruling 中"23 系 PC 机器上的计数事实"的表述：不成立。** PC 当前扫描与 2026-07-28 旧基线索引均为 24 体（每族 Mono/Stereo 齐）；23 的唯一自洽出处是收窄算术（31−5 PA−3 FabFilter=23），系决策文档层面的计数推导，双端机器枚举从来都是 24。**C2 ruling 的结论（采纳 24 为校准面）不变，且经本卡双端实证后依据更硬**。U2 决定文档已加更正注记。

## 遗留移交

- PC 参数面指纹生产（12 族级，闭 C3 pending_pc_reference）+ 孤儿 main.go 收编 commit → 同批并入下一张 PC 卡。
