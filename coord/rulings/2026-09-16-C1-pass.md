# 裁定：PORT-C1 pass（2026-09-16，决策侧）

- 对象：`port/c1-plugin-scan-paths`（`c40c8763` 实现 + `29748a87` 回执）
- diff 审查：Windows 分支为原函数逐字搬迁（env 优先级与兜底路径零变化）；darwin 返回 `~/Library/Audio/Plug-Ins/VST3` + `/Library/Audio/Plug-Ins/VST3`（符合 PORT_AUDIT §1.4）；三平台 build-tag 兄弟文件 + 显式降级注释，纪律合规；测试无硬编码易变标识
- Windows 侧复验（临时 worktree 实测）：`go build ./...` exit 0；新测试 PASS；`GOOS=darwin`/`GOOS=linux` 交叉编译 exit 0；Mac 失败 2 例在 Windows PASS（确证与本卡无关，转 PORT-MAC-HARNESS-FLAKY-1）
- 端测边界：止于单测+交叉编译，符合 handover §4（C4 就位前的既定门槛），**接受**
- 处置：ff 合入 main；mac 既有失败开排查卡；B-1/B-2 裁定见同日会话记录
