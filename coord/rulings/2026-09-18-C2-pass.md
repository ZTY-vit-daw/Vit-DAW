# Ruling：PORT-C2 pass（2026-09-18）

- 卡：`coord/cards/done/2026-09-17-PORT-C2-mac-calibration-chain.md`
- 实现：`port/c2-mac-calibration-chain` @`309e640`（单 commit，基 ad5d2e2）→ cherry-pick 入 main
- 裁定：**pass**

## 决策侧独立核验（工件级 + 状态级；未整链重跑）

纯新增脚本（`scripts/` 两文件、989 行），零既有代码路径改动，agent/ 与 VitApp/Source 零触碰（diff stat 核对）；据此采用工件级+机器状态级复验而非整链重跑，边界在此显式声明。

1. **diff 域**：`scripts/pca_calibration_chain_mac.sh` +917、`scripts/SMOKE_TESTS.md` +72，与卡面文件域一致；`bash -n` 语法通过；mktemp 独立 workdir、trap cleanup、`~/.vit` 前后态快照入工件、源码树零写入——AGENTS §10 纪律符合。
2. **权威 run**（`pca_calibration_chain_mac_20260918-115112`，workdir `…T/pca_calibration_chain_mac.vulCBEUv`）：`summary.json` 六门全 true、`overall_status=PASS`；探针 719 主体 0 失败；PCA 重认证 24/24 certified+promoted；只读冒测 24/24 promoted、0 missing、0 not_promoted；停栈记录 kernel `SIGTERM:143:1`（A1 已知模式）+ agent `SIGTERM:0:1`；`run_meta.txt` 锚定 HEAD=ad5d2e2（dirty 2 entries 即本卡两文件，事后 worktree 干净停在 309e640，先跑后提交属实）。
3. **机器状态**（`~/.vit/`）：语义索引 719 条；v1 attestations 3 条（2 promoted + 1 stale 为 run2 崩溃遗留、被 run3 新收据按 store 取代语义正确压 stale，非污染）；v2 attestations 22 promoted；`pca_certifications/` 25 个 job 收据——与回执逐项吻合。
4. **失败分类**（AGENTS §8）：run1 环境中断（FetchContent 直连被墙→加 `--cmake-proxy`，平台中立保留默认直连）、run2 脚本缺陷（v2 零值 `promoted:null` 解析击杀，已修）、run3 全绿——三个 workdir 在盘佐证，分类成立。
5. **identifier 稳定性**：`b943518d`（PSE Mono）/`65ffd264`（C1 comp Stereo）同时存在于白名单终稿与机器本地语义索引——跨内核会话稳定声明成立。

## 23vs24 口径裁定（回执上报项，本次裁定）

**采纳 24 主体为 mac 校准面，不裁剪。** 依据：U2（2026-09-16 决定）的语义是"全部 Waves 已装且授权覆盖"（31→23 的收窄本质是排除 PA 5 + FabFilter 3），23 是 PC 机器上的计数事实而非可移植不变量；mac 对 12 个 plain 族忠实枚举 = 24 个 Mono/Stereo 体，全集草稿覆盖 PC 侧任意 23 子集，强裁到 23 缺乏逐体对照依据。草稿内嵌 `reconciliation_note` 已固化此差异；非晋升变体（C1 comp-gate/comp-sc、C6-SideChain、Sibilance-Live、TransX Multi）排除正确。experimentplugins v5 域钉白名单的 mac 采纳不在本卡范围，如需另开卡。

## 边界声明采认

回执的端测覆盖边界声明（真实双件栈 + 24 主体真实实例化 + typed 控制面已覆盖；pluginprobe 原生观察宿主 Windows-only、Godot/webui 不在卡面）采认，足以支撑本卡交付。pluginprobe mac 化归 C3 口径不变。
