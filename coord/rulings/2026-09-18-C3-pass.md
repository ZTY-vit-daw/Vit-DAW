# Ruling：PORT-C3 pass（2026-09-18）

- 卡：`coord/cards/done/2026-09-18-PORT-C3-pluginprobe-mac-reprobe.md`
- 实现：`port/c3-pluginprobe-mac-reprobe` @`8051f01`；main 落点 `0a39d66`（推送范围事故副本，见下）
- 裁定：**pass**（验收②按"mac 半边 + 缺口证据"口径采认，PC 对照转后续件）

## 决策侧核验

1. **diff 域**：5 文件全在 `PluginProbe/`（native-host main.cpp/CMakeLists/README + reprobe driver+README，781 行），`agent/` 与 `VitApp/Source` 零触碰——Go 链路（cmd/pluginprobe→adapter→vst3host）mac 本可用无需适配，与回执一致。
2. **工件**（`~/Documents/vit-c3-artifacts/`）：权威 run `20260918-202010` summary PASS——subjects_ok=24/0 fail、installation_consistent 22/22 与 `~/.vit` v2 store 逐字节一致（CommonCrypto ≡ Go FingerprintPath 文法等价的实证）、R4 三面标记全 true（真机 bundle symlink count=0；注入 symlink 红绿闭合 fail-closed 拒绝；Go 侧 `-overlay` 行为验证）；双跑 202010/202104 指纹零漂移；201912 失败 run 存档（driver 断言形状缺陷已修）；语义索引前后哈希断言不变。
3. **验收②前提失效采认**：卡面预设"PC 侧现存指纹"——实测 PC 参数面指纹不存在（主仓唯一在案=Lindell MBC 一条，非 Waves；transfer 空；全盘排查），`pc_absence_evidence.json` 固化。`fingerprint_comparison.json` 24 主体全标 `pending_pc_reference` 属如实交付而非缺口失职。**跨平台对照转后续件**：PC 侧用本卡双平台代码（win32 面原样保留）产指纹补齐；因 Mono/Stereo 共享同一参数面指纹（12/12 族实证），PC 侧产 12 族级指纹即可完成对照。
4. **既有缺陷最小修复采认**：mac 首编暴露的 AppleClang `pluginFile={}` 歧义与 0 字节 `Icon\r` 误判，修复在 `#if defined(_WIN32)` 外但语义等价/可移植，win32 回归面构造级论证（bcrypt SHA256 固定 32 字节等）采信，PC 侧可复验。

## 推送范围事故处置（[等待决策侧] 项裁定）

**裁定：`0a39d66` 保留，等值于提前 cherry-pick，随本 ruling 形式收口。** 依据：`PluginProbe/` 内容 8051f01↔0a39d66 diff 为空（决策侧独立复核）；验收分支完好；无历史改写；内容经本次验收评审背书。revert+重放同内容属仪式性动作，不做。

**违规入档与根因**：执行侧收口推 `HEAD:main` 前做了 `pull --rebase`，把实现提交 rebase 为 0a39d66 并连带进入 main——违反 PROTOCOL §3"执行侧仅卡片状态变更直推 main"。处置正确点：自报及时、事实核验完备、不再自行补救。**预防措施**：PROTOCOL §3 已增补推送纪律条款（见 2026-09-18 coord 提交）。

## 遗留移交

- PC 参数面指纹生产（12 族级）→ 并入下一张 PC 卡（与孤儿 main.go 收编同批）。
