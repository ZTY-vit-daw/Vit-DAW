# 裁定：PORT-A5 pass（2026-09-17，决策侧）——层 A 收官

- 对象：`port/a5-kernel-mac-smoke`（实现 `bfe58ec`；分支尖端 `745143a` 仅卡面状态，实现无遗漏）；领取基线 4208de8，独立 worktree 开工（PROTOCOL §3 新规首批实践）
- diff 审查：仅 `scripts/dev_agent_smoke_mac.sh`（710 行新增）+ `SMOKE_TESTS.md` 条目；**VitApp/Source 与 agent/ 零触碰**；隔离面合规（mktemp 工件目录、内核假根目录状态隔离、端口独占预检 fail-env、trap 只清理自启进程、AGENTS §9 单属主规则注释在案）
- 决策侧独立复验（隔离 worktree @bfe58ec + 子模块 @b439749，**全模式含自建内核与 agent**，工件目录与执行侧 run7/run8 互不覆盖）：**exit 0，overall PASS，8/8 门槛 true**（内核起停+三端口属主 / agent /health / C4 白名单 GET / 直连 ZMQ ping→pong / agent invoke project.state 往返 / 插件扫描 completed / R2 WaveShell / R9 子进程）；kernel_stop=`SIGTERM:143:1`；扫描 total_files=2、**plugin_count=719、failed_files=0**——与执行侧 run7 逐项一致；复跑后无残留进程
- 工件抽查（执行侧 run7）：`waves_list.json` 719 主体中 U2 的 12 晋升族（C4/C6/L1/L2/LinMB/DeEsser/RDeEsser/Sibilance/PSE/Smack Attack/TransX）Mono/Stereo 变体逐一在列；ping_reply={"status":"ok","message":"pong"}；lsof 端口/shm 证据齐
- **R2 首验结论采纳**：WaveShell1-VST3 在 mac 枚举成功（719 主体超集，Windows 23 晋级主体为其子集）——风险清单 R2 由"未证"降级为"枚举面已证，实例化/加载验证归 C3"；**R9 首证采纳**：三重证据（out_of_process + `--PluginScan` 子进程 ps 实拍 + 内核日志锚点），ad-hoc 签名无 Gatekeeper 阻碍
- **A1 平台发现闭环采纳**：kernel_stop=SIGTERM:143:1（1 秒退出、无优雅 shutdown 面）；shm 残留 O_EXCL=EEXIST 实证 + shm_unlink 清理——A5 已按裁定以脚本侧 SIGTERM+宽限→SIGKILL 兜底，**无需内核侧改动**；内核 shutdown 命令面如未来需要另开卡
- 失败分类（验收④）合规：run7 前 5 次失败均为环境/脚本缺陷（逐条分类），栈本体自 run4 起全绿——符合 AGENTS §8
- 端测边界：内核+agent 两件真栈已覆盖；Godot 前端不在卡面（B 波）；Waves 主体实实例化/重探针归 C3；webui/旅程面无涉——**接受**
- 处置：实现取文件域入 main；**层 A（A1-A5）五卡全部验收闭环**；下一波 = B（Godot 前端 mac 化）+ C2/C3（校准）；HARNESS-FIX-1 另以 rework 裁定处置（同日）
