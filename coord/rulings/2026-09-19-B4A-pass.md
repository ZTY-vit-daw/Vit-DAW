# Ruling：PORT-B4A pass（2026-09-19）

- 卡：`coord/cards/done/2026-09-18-PORT-B4A-frontend-transport-package.md`
- 裁定：**pass**（零源码改动打包卡，无 cherry-pick）

## 决策侧核验

1. **tarball**：决策侧对本机 transfer 件亲测 sha256 = `f5c4fdd4…` 与 README §0 一致；98,298,849 B（93.7 MiB）<100MB 硬限达标；成员级核验（排除项 grep 缺席 + 必备项在场）与打包前后 status 一致性声明在案。
2. **bundle**：全史 331 MiB 超限 → 按卡面既定预案降级增量 `c7bcd29..port/b3-start-page`（3 commits），README §2 说明——预案内动作，采认；工程重建以 tarball 为权威的口径正确。
3. **README 三节齐**（191 行）：状态清单（31 行 status/log/diffstat 16 文件 +1212/−476）+ 双 sha256 + 排除项及原因（EXR 附主仓 `scripts/vit_baker.py` 再生命令）；回执如实携带会话杂项（`.vit_derived/`、`8.1test*/`、根层 `.vit` 单文件工程）并区分于被排除的同名 `_project/` 目录——诚实申报采认。注：回执所引"派发消息所附追加排除清单"系派发面补充口径，其结果（EXR 排除+再生命令）已文档化，采认。
4. **worktree 清理**：B3 移交项完成（remove 前 status 干净、exit 0、worktree list 仅余主树、分支保留）。
5. **transfer 投递**：commit `83a3a25` 已推（决策侧 pull 到位并消费）。

## 遗留移交

- 运输包三件（tarball/bundle/README）随本 ruling 移入 transfer `received/`（B4B 已消费完毕）。
