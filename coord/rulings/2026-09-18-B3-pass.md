# Ruling：PORT-B3 pass（2026-09-18）

- 卡：`coord/cards/done/2026-09-18-PORT-B3-startpage-platform-branch.md`
- 实现：仓外 Godot 工程 `D:\Godot\project\vit-daw-frontend` 分支 `port/b3-start-page`（`e4b0ca1` 开发态快照 → `546c2ac` 主体 → `9fd4820` 文案补漏；净 diff `app/startup/start_page.gd` +96/−29）；证据=transfer 9 文件包
- 裁定：**pass**

## 决策侧核验（证据=transfer inbox-pc 2026-09-18-PORT-B3-*）

1. **patch 逐段审**（0002/0003）：`_is_mac_platform()`/`_default_dev_root()`/三个可执行名 helper 结构干净；**Windows 臂 else 分支逐字保留**（候选路径与原名在 diff 中原样可见）；mac 臂命名/布局引用正确（B1 无后缀事实、A3/A5 cmake 布局含 Debug、`~/Documents/Vit-DAW` 缺省根）；python 桥段 guard return 附 B6 裁定注释（4445 互斥说明）——B6 裁定正确落地。
2. **R1 前置检查结论采认**：`_try_boot_local_bridge_if_needed()` 为**顺序 fallback**（vitagent 候选存在即 spawn 并 return，无候选才落 python 桥）——B6 报告担心的"无条件并行双绑 4445"不存在于现链，无需先改条件分支，剔除以 guard 实现。审计锚点行号漂移但结构相符，未触发停止条件。
3. **PC 回归独立复验**：决策侧亲测 pre/post 两探针日志的 B3PROBE 行 diff=空（默认/同根/异根三态候选全同）。pre 日志多出的 telemetry_manager.gd 解析 ERROR 系 pre 轮跑活树（含其他文件未提交开发态）而 post 轮跑 B3 worktree 之差，文件不在 B3 域、与 B3 改动无关，判定良性。check-only 活树+worktree 双 exit 0。
4. **§12 纪律**：领取前活树 +89 行开发态先快照入档再叠加，未整理未回退——正确。

## 关键事实入档（B4 规划依据）

- **Godot 工程 git 版控、无 remote**——前端登 mac 需 patch/整仓拷贝（B4 卡将按此定同步方式）。
- mac 臂候选路径未经真机实跑（PC 无 mac），启动行为归 B4 真栈验收；`[等待真栈验收]` 标注符合手测入口裁定。

## 遗留移交

- PC 侧 `D:\Godot\vit-b3-worktree` 验收后清理（下轮 PC 值班流执行）。
- transfer 证据 9 文件随本 ruling 移入 `received/`。
