# Ruling：PORT-PS1-SYNC-2 pass（2026-09-20，PC 决策侧；交付面口径——三段+追加交付、⑥ 跑绿、④⑤ 止损上交）

- 卡：`coord/cards/done/2026-09-20-PORT-PS1-SYNC-2-assertion-overhaul.md`
- 实现：`port/ps1-sync-2` @`1f424b5c`+`85e2936f`（10 文件次，全在 scripts/ 域）→ 本裁定随附 cherry-pick 入 main
- 裁定：**pass（交付面）**——验收①②③+SMOKE_TESTS 全交付；⑥ exit 0（kernel-prepared 分支双端首次真实执行）；④⑤ 按 §8 止损上交，红因经排除法收敛到 agent 自由态工作流语义（域外），三对接选项待用户裁定（见 §④⑤ 处置）。

## 决策侧核验（全部亲验）

1. **验收①⑤ 断言族重设计**：ps1 `Assert-KernelPreparedTrackWaveformRows` 重写 per-track ready（ExpectedTrackIDs/readyByTrack/每轨 ≥1 ready 行+provenance 字段），与 _mac.sh 先行修复同构；**⑥ 绿轮工件反证旧断言必红**——summary 含混合行状态（132 ready/4 partial/3 missing/16 deferred/1 materialized）且终判 passed，正是实际 schema（每 request 历史轨迹行）；`Assert-AcousticReadinessReasons` 传参 ExpectedTrackIDs。
2. **验收②needle 扩容 + 前提修正**：confirmNeedle（0x786E,0x8BA4=确认）入 ④ ps1:799+_mac.sh 两处，判据结构不变。**前提修正实证**：product_path 双端 grep executeNeedle 零命中——卡面"四处"失准（该件判据=结构性只读断言），执行侧静态+历史双重取证后修正为 2 文件，避免在错处加 needle（确认措辞在该件反而是 FAIL 语义）——修正正确，追认。
3. **验收③settle 720 双端**：ps1:55 与 _mac.sh 默认 300→720，理由注释含 flash A/B 结论出处。
4. **追加段**（run1 红证驱动，合理）：④ 原无 settle 锚——移植 SETTLE-1 锚（默认 720）；双端 settle 合成补 waiting_confirmation 面（needs_confirmation=true+PendingCandidate 重建）；PS5.1 StrictMode 两陷阱修复+④ ps1 UTF-8 BOM。⑤ run4 实证合成有效：settled `stop_reason=needs_confirmation`/`needs_confirmation=true` 两断言过。
5. **验收④ PC 跑绿**：⑥ `live_material_observation_20260920_182336` exit 0（工件亲核 passed+混合行状态）；④ 3 轮止损——round2/3 **同断点**"Timed out waiting for log pattern: [mix.tick.pending] stored"（console 亲核两轮同形）；⑤ 4 轮止损——run1/2/3 为脚本面缺陷（修复合规：缺 needs_confirmation 合成/StrictMode 崩/@($null) 陷阱），run4 到语义终点：needs_confirmation 合成过、终断言"pending route 含 mix.derive"红（route 仅 ccb.observation_catalog）、agent 日志 `improvement_proposal_confirmation_required` 在案。§8 合格：脚本面修复后重跑非原样重跑、同断点两败止损执行、flash 口径未换模型。
6. **域外根因定性采信**：flash 将 mix 请求路由进自由态改善性提案轨迹——手动探针完整链路在案（确认①→native tool confirmation required→确认②→**已应用并回读验证 0→-1dB**+终态 d1_post_action_evaluation_required）。即产品路径功能上**走通**，但为双跳确认+d1 终态，与经典单跳断言族（mix.tick.pending 事件/单跳 applied_reobserved/提案时 derive 投影）不匹配。

## ④⑤ 处置（三对接选项，用户裁定中）

a) agent 侧：改善性提案工作流补可观测面（mix.tick.pending 事件/单跳确认/提案时 derive 投影）；b) 烟测断言族按新工作流重设计（④⑤ 语义改双跳+d1 终态）；c) 采认为自由态演进预期行为，④⑤ 转 agent 侧改造配套卡。裁定结果回填卡片验收字段。

## 过程注记

- 执行侧领卡时工作树含决策侧 SETTLE-1 doing 卡删除遗留（未代提交，处置正确）；远端 main 并行推进（mac b15b35b2）经软重置避让，未动他人改动。
- 工作树完工时留在分支 port/ps1-sync-2——验收收尾由决策侧切回 main。
