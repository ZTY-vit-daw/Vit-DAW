# 裁定：PORT-A4 pass（2026-09-17，决策侧）

- 对象：`port/a4-tracktion-mac-build`（**零源码改动纯验证卡**，无实现 commit；分支仅含领取提交 2baabd2）
- 验证基础复核：构建基线 = main@`2baabd2` + submodule 纯 pin `b439749`（3009ca2 重 pin 后值，无补丁叠加）——**A3 回执遗留的"纯 pin 可复现"验证点就此闭合**（A3 验收构建当时基于补丁分支工作树、gitlink 尚为 bae0331；本卡在纯 pin 下复现同等结果）
- 决策侧工件抽查（只读，/tmp/vit-a4-build/）：产物 Mach-O 64-bit arm64；`nm -u` tracktion undefined 符号 **0**（链接解析断言独立证实）；build log 无真实 error（仅 2 行 warning 上下文中的 `error` 形参名）；决策侧 A3 验收构建在同一子模块内容上独立复现过全链编译（201 单元级）
- 卡面三项验收全达成：① tracktion 目标编译 exit 0（13/13 编译单元，JUCE 模块模式挂 VitApp 目标下）；② VitApp 链接解析通过；③ 命令与退出码入回执
- Windows 零回归自明（零改动）；WaveShell mac 枚举/加载不在本卡（A5 真机）
- 处置：无实现可合入；卡片归档即可；A5（内核 mac 冒测最小集）前置条件就绪，待决策侧建卡
