# Ruling：PORT-B1 pass（2026-09-18）

- 卡：`coord/cards/done/2026-09-18-PORT-B1-vit-extension-mac.md`
- 实现：`port/b1-vit-extension-mac` @`bac98a9`（3 文件 +126/−6 全在 `extension/` 域）→ cherry-pick 入 main
- 裁定：**pass**

## 决策侧核验

1. **diff 域与质量**：SConstruct（godot-cpp v10 适配：Glob 源/api_version 导出/Default 抢占修复/产物路径 `#extension/bin`→`#bin`）、`vit_extension.gdextension` macos.arm64 双条目（lib 前缀+.dylib 命名律正确）、`vit_waveform_reader.cpp` POSIX 实现——shm_open "/" 前缀规约对齐内核 SharedMemorySegmentPosix、fstat 尺寸守卫防 SIGBUS、ENOENT 静默空/他错 warning 空、PROT_READ 精确映射；Win32 分支 `#ifdef _WIN32` 内逐字未动；`__has_include` 双路径头兼容 PC 旧 godot-cpp。实现质量采认。
2. **产物路径改动无 PC 破坏面（回执上报项裁定）**：`build_release.ps1:87-97` 的 DLL 搜索根全是 VitApp 内核目录（`VitApp/build_release`、`VitApp/build`、`VitApp`），零引用 extension 构建输出路径；`assemble_windows_release.ps1` 零 extension 引用——旧 `#extension/bin` 叠路径（extension/extension/bin）是双端共同潜伏瑕疵，修为 `#bin`（=extension/bin）**采认，无 PC 流受影响**。`build_release.ps1` 本身是 v0.1 遗留口径，不在此处置。
3. **pin 前提偏差采认**：上游无 `godot-4.6.stable` tag 属卡面预设失效；master@`507ed9d` 以仓内 `extension_api-4-6.json`（sha256 `00a3ad6d…`）自证 4.6.stable.official + SConstruct 显式 `api_version=4.6` 对齐本机引擎——按仓内事实执行的处置正确。
4. **工件核验**（`/tmp/vit-b1-artifacts/b1-build-20260918-210251/`）：双 target exit 0（dylib 184688/184656 B，sha256 `d0c0be10…`/`f31ac752…`，arm64 单架构 ad-hoc 签名）；运行模式 headless exit 0 零 error 零 warning、CLASSDB 注册+实例化 ✓；fail-closed 语义（缺段 ENOENT→空、float_count≤0→空）✓；Python 跨进程真实 POSIX 段正向读回 8/8 逐位相等 ✓。
5. **引擎首 import SIGSEGV 定性采认（环境事实入档）**：Godot 4.6.stable mac 对任何含 GDExtension 工程的首次 headless `--import` 在扩展加载成功后的编辑器退出文档生成段崩溃（E1 2/2 复现、二次自愈、无扩展对照 0）；扩展自身加载注册已完成且成功。**移交 B4**：前端工程首次 import 将命中一次，属已知引擎环境事实（二次 import 自愈），B4 卡面注记，不计为缺陷。

## 边界采认

release 模板内 release dylib 加载未测（本机无 release 模板 player）与真实内核段端到端读回（B 层集成面）两项边界声明采认，归 B4/旅程冒测覆盖。
