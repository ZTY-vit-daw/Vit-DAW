# 裁定：PORT-A3 pass（2026-09-17，决策侧）

- 对象：`port/a3-mac-cmake`（实现 `f3a4cd3`；领取基线 30307e1）
- diff 审查：9 文件 +101/−26 全在卡面文件域。守卫模式逐块核实：`windows.h`/`CloseHandle`/shm 发布块/Tester 整类 `#if defined(_WIN32)` 包住且守卫内零编辑；`HANDLE`→`void*` 为恒等 typedef；`[[maybe_unused]]` 纯属性；`startOfflineRender` 默认实参去除后调用点显式 `{}` 与原默认一致（CWG 1397 注释在案）；`atomic<shared_ptr>` Apple libc++ 无 P0718 特化 → 非 Windows 侧 juce::SpinLock 等效 shim（Windows 侧原实现不变）
- 决策侧独立复验（隔离 git worktree @f3a4cd3 + 子模块 @b439749，构建/运行态全在独立临时目录，AGENTS §10）：
  1. `cmake -G "Unix Makefiles -DCMAKE_BUILD_TYPE=Debug` configure exit 0；`make -j8 VitApp` 构建到 100%，产物 `VitApp_artefacts/Debug/VitApp`（Mach-O 64-bit arm64，106MB Debug）——**内核 darwin 编译+链接由决策侧独立复现**
  2. libsodium GLOB 原样可用实证：`libsodium.a`（1.1MB）产出，`_sodium_init`/`_sodium_version_string` 符号导出（T）——R1 消解，无需改显式源列表
  3. Tests 工程 configure+build exit 0，**4/4 可执行档运行 exit 0**（StateTests / PersistPolicy / AudioPlane / RenderWatchdog，后两者为真 tracktion Engine headless）
- **镜像缺陷处置（本批一并执行）**：补丁分支 `restore-juce-extras-build` @`b439749` 审阅通过（44 文件纯新增、`.gitignore` 负向规则、MIRROR_PROVENANCE.md 全链溯源，juce 8.0.12 tarball SHA256 在案）；已 ff 合入镜像 main 并主仓重 pin（`3009ca2`，bae0331→b439749）——A3 产物自此纯 pin 可复现，PC 侧全新 configure fatal 同步修复
- 验收④（Windows 零回退）：执行侧自检 + 决策侧 diff 审查通过；**PC 侧复编译确认待 PC 会话执行**（改动均平台守卫，回退风险低；CMake `LANGUAGES C CXX` 顺带修复 PC CMake 4.x 潜伏失败）
- 端测边界：编译面 + Tests 运行面；未含 VitApp 进程真实栈启动/通信冒测（ZMQ 网关、音频设备、shm 发布链路）——归 A5；既有 clang 风格警告非阻断未触碰——**接受**
- 处置：实现取 `f3a4cd3` 的 VitApp 文件域入 main（`d33f933`；分支提交混入的卡片状态文件不入，main 已由协调提交覆盖）；后续 A1/A4 解锁可领
