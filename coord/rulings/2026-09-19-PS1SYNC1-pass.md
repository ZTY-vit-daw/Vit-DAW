# Ruling：PORT-PS1-SYNC-1 pass（2026-09-19，PC 决策侧；按停止条件"单件上交、其余继续"口径）

- 卡：`coord/cards/done/2026-09-19-PORT-PS1-SYNC-1-contract-upgrade.md`
- 实现：`port/ps1-sync-1` @`f9203fe8`（9 文件 +104/−60 全在 scripts/）→ 本裁定随附 cherry-pick 入 main
- 裁定：**pass（交付面）**——A/B/C 三件 PC 权威 run exit 0；F⑤ 断言双端落地且升级方向被两轮 PC 工件实测证实；F⑤ PC 全脚本跑绿一项按卡面停止条件上交，拆后续卡 **PORT-PS1-SETTLE-1**（见 todo/）。SMOKE-MAC-2 不被 SETTLE 阻塞（mac 侧 ⑤ 已有 `--chat-settle-seconds`）。

## 决策侧核验（全部亲验）

1. **验收①断言升级 diff**：A② VIT1 magic 断言 + `--legacy-template` 明文模板派生（ps1/_mac.sh 双端传参）+ 重开往返保留 ✓；B③ queued/run 语义全套字段（deferred=false/baking=running/queue=running/jobs_queued=N×2）✓；C⑥ v1.4→v1.5 双端+注释 ✓；F⑤ 四处别名组+消息纳入 `ccb.observation_catalog`/`ccb_observation_catalog`、旧名保留双态计数 ✓。四件与 SMOKE-MAC-1 ruling 升级语义逐项吻合。
2. **验收②PC 权威 run（工件亲核）**：A `20260919_211341` passed（`saved_vit_container_magic:"VIT1"`、44100/48000 往返实测在工件）；B round2 `20260919_211457` passed（`analysis_jobs_queued:122`=61×2、queue_status=running 工件实证）；C round2 `20260919_211727` passed（`mom_version:"v1.5"`、obs id 符）；F 两轮 exit 1 同断点止损——工件实证 route 均含 `ccb.observation_catalog`（agent_state.json + chat_vocal_focus.json 双在）、vocal focus `stop_reason:"limit_reached"`+`completed_steps:1`、agent 日志 continuation.arm durable 续跑记录在案。§8 纪律合格：A 1 轮 / B、C 各红→绿且红均定性后修复（非原样重跑）/ F 1 次环境中断未计有效轮 + 两败止损执行。
3. **验收③④**：SMOKE_TESTS.md 四件条目同步 ✓；工件七目录在位、字段与回执逐项一致 ✓。
4. **红轮定性复核**：B round1 红=探针内实现选择（rebuild 路径被旧取消任务挡死→改直接节流 auto 任务，探针侧选择非产品缺陷）；C round1 红=**第五项腐化**（下条追认）。两定性均成立。

## 四项上交裁定

1. **F⑤ PC 跑绿驱动缺口**：采认诊断（PC ps1 `Invoke-AgentChat` 单发 POST 无 settle 等待；vocal focus 回合在新终态路由下确定性超 max_turns 切片进 durable continuation——两轮同断点工件+日志实证）。**拆 PORT-PS1-SETTLE-1**（已写入 todo/，仍 PC 侧）：授权扩域 PC ps1 补 settle 等待（mac 同款语义、`d1_audition_gap_smoke.ps1` 轮询先例）后 F⑤ 复跑。
2. **第五项腐化修复追认**：锚点亲验 `agent/internal/mixboard/project_package.go:433` 条件发射 `masking_analysis_not_ready_on_current_project_cut`（旧词代码中已不存在，旧断言必失败）；ps1:1021 + _mac.sh:841 双端最小同步，C round2 绿实证。同类同域，**追认入本卡**。
3. **文件域偏差追认**：A②/B③ 断言物理载体为 `scripts/` 下 2 个 probe py（ps1/_mac.sh 委托驱动，`--legacy-template` 传参实证委托关系）；SMOKE_TESTS.md 系验收③要求。9 文件全在 scripts/ 域、最小 diff，**追认**。
4. **工作树遗留 default_project.xml**：核定为烟测内核运行时改写（新 uuid/导入记账，清更早 fixture 残留），非实现工作。**裁定：保持现状不提交**（BOOTSTRAP-PC 已知保留项惯例，同 B4B 先例）。**附脆弱性备案**：A② legacy 模板现耦合该运行时文件须含 VIT_AUDIO_SETTINGS，未来内核改写剥离该段将致 A② 红——后续卡注意，必要时改固定 fixture。

## 过程注记

- 回执"分支 push 暂挂"注记已过时：`port/ps1-sync-1` 实际已在远端（决策侧 ls-remote 核实 f9203fe8）。
