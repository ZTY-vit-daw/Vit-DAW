# Ruling：PORT-B2P pass（2026-09-18）

- 卡：`coord/cards/done/2026-09-18-PORT-B2P-cef-mac-probe.md`
- 实现：`port/b2p-cef-mac-probe` @`0484111`（docs/CEF_MAC_PROBE_2026-09.md，仓内唯一产出）→ cherry-pick 入 main
- 裁定：**pass；R3 正式消除**（PORT_AUDIT 九项风险全部有了着落）

## 决策侧核验

1. **出图视觉级复验**：决策侧用视觉模型直读权威轮（A3 metal-accelerated）viewport.png——完整渲染页确认（标题/渐变/canvas 动画方块含帧计数 240/脉冲圆环/文字），非空背景；PNG 1280×800 属实。
2. **链路证据抽验**：evidence.json 的 console 回传链（PROBE_JS_ALIVE/JS_BRIDGE present/RAF started/FRAME 计数）=JS 执行+桥在位实证；ps_during.log 5 个真实 CEF helper（gpu-process/network/storage 等，PPID=godot）实拍在案；软件轮另有 browser_texture.png 直读纹理。三轮退出零残留。
3. **渲染路径结论采认**：metal 加速/默认驱动加速/软件三路径全通；**审计前提修正采信**——Godot 4.6 mac 默认 Forward+ 即原生 Metal（非 MoltenVK），CEF 与 Godot 同在 Metal 上，加速 OSR 开箱成立、无需旗标。R3 从"唯一未验真机风险"变"已实测可用"。
4. **手册采认**：§2.2 B2 正式集成步骤清单（zip 抽取/extension_list.cfg 生成坑/无旗标/安全基线/验收锚点 grep 串/打包执行位坑）+ §4 坑位清单（含 223305 无效轮的 --import 教训）——**作为 B2 正式集成的执行依据**。
5. **无效轮分类采认**：223305（gdextension 未注册=探针流程缺 --import）/224421/224426（bash3.2 空数组）均流程性，非 CEF 功能失败；下载面（直连超时→代理 1.3MB/s）如实记录。

## 边界采认

探针=最小工程，非前端工程本体；zip/framework 未入仓未入 transfer（>100MB 限制）；B2 正式集成时按手册重取上游资产。
