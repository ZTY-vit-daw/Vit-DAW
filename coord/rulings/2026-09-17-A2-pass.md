# 裁定：PORT-A2 pass（2026-09-17，决策侧）

- 对象：`port/a2-shm-darwin`（实现 `6a54aa2`；领取基线 18ac692）
- diff 审查：6 文件 +449/−6。域内 4 文件（harness/vsphub 各 1 实现 + 1 测试）；域外 2 文件仅 fallback build tag 收窄 `!windows` → `!windows && !darwin` + 注释更新，与回执声明一致，linux 保持显式报错降级；`shm_windows.go`/`audio_feature_shm_windows.go` 零触碰。实现语义逐项核对：O_RDONLY shm_open（读端不可改写内核段）、fstat 前置尺寸检查先于 mmap（避免越界 SIGBUS，对齐 MapViewOfFile fail-closed）、偏移 0 显式 LittleEndian 解码、`posixShmName` 前导斜杠规约；harness/vsphub 两处校验差异（trim 有无、错误文案）各随其 windows 兄弟，与语义对照表一致
- 决策侧独立复验（隔离 git worktree @6a54aa2，2026-09-17）：
  1. 构建：宿主 `go build ./...`、`GOOS=darwin GOARCH=arm64`、`GOOS=linux`、`GOOS=windows` 全部 exit 0（不回退）
  2. `go vet ./internal/harness/ ./internal/vsphub/` exit 0
  3. 目标单测（真实 POSIX shm 段，非 mock）：harness 7/7 PASS、vsphub 4/4 PASS；`go test ./internal/vsphub -count=1` 整包 ok
  4. `go test ./internal/harness -count=1` 整包：仅 PORT-MAC-HARNESS-FLAKY-1 已登记 2 例失败（FullProjectAccessDoesNotDisplace… / AgentProcessorLoadGateRechecksPCA…），**无新增失败**
  5. gofmt 卡内 6 文件全过；worktree `git status` 零污染（AGENTS §10）
- 平台约束（执行侧实证，决策侧采纳为 A1 硬约束）：① macOS shm_open 段名含前导斜杠上限 31 字符（PSHMNAMLEN）——内核现有命名必然超限，A1 发布段名须 ≤30 正名字符（短前缀或哈希）；② shm 存储按 16KiB 粒度取整、fstat 报取整后尺寸，亚粒度逻辑尺寸不可见，由调用方 float_count/stride 一致性检查守门（两平台一致）
- 端测边界：交叉编译面 + 真实 POSIX shm 单测；无真实栈烟测——内核 darwin 侧 shm 发布端属 A1（未开工），端到端读链路由 A1/A5 兜底——**接受**
- 处置：cherry-pick `6a54aa2` 入 main（`c1b54f3`）；A1 建卡时必须吸收上述两条硬约束写入卡面；已验收 port 分支清理待用户确认后批量执行
