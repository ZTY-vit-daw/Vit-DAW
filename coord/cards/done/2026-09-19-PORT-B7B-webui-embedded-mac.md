# PORT-B7B：Ask Vit WebUI mac 内嵌（VitCefWebViewHost 适配层，替换 Safari 兜底）

- 优先级 / 预估 / 依赖：P1 / 1 天 / 依赖 B7A（API 契约文档）
- 模型分级：L3 / GLM-5.3（跨宿主适配语义 + CEF 纹理集成；关键点可找参谋讨论）
- 目标：Ask Vit WebUI 在 mac 以**内嵌窗口**呈现（CEF），彻底替换 Safari 兜底路径：
  1. **平台分支**：`vit_dock/scenes/vit_dock_root.gd` `_ensure_agent_webui_host()`——`OS.get_name()=="macOS"` → 实例化 `VitCefWebViewHost`（新 GDScript 适配类），Windows → `VitWebViewHost` 原样（B3 平台分支同款纪律，Windows 臂逐字保留）
  2. **适配类**（前端仓新增 `vit_dock/scenes/vit_cef_web_view_host.gd`，`class_name VitCefWebViewHost`）：以 godot_cef `CefTexture`（B2P 手册 `docs/CEF_MAC_PROBE_2026-09.md`）实现 B7A 契约面——**功能性子集**：`create_window(parent,title,w,h)`（Godot Window+CefTexture 内构，parent 句柄仅用于居中/忽略并注明）、`create`、`navigate`、`close`、`set_bounds`、`set_visible`、`focus`、`eval_js`、`post_web_message`、`set_user_data_subdir`、`debug_state` + 信号 `ready/navigation_started/page_state_changed/load_error/web_message_received`；**降级子集**（overlay/插件编辑器面：`create_overlay_window/get_selected_plugin_*/vit_open_strip_silence_dialog`）：v1 显式 no-op+warning，回执与端测边界**显式声明降级清单**
  3. webui 交互闭环：web_message 通道（mutation/browser/external-browser）经适配层透传不断链；`AGENT_WEBUI_URL` 页面加载、会话往返（journey S3 面已证 agent 侧就绪）
  4. 验证：`godot --headless --check-only` 解析 exit 0 + headless 适配类实例化冒烟（可 headless 的面）+ **`[等待真栈验收]` 用户手测**（内嵌窗口出现、页面渲染、对话往返、关窗干净退出无 CEF 残留——清单交决策侧转用户）
- 文件域：前端仓（mac `~/Documents/vit-daw-frontend`，git 化）`vit_dock/scenes/vit_dock_root.gd` + 新增 `vit_dock/scenes/vit_cef_web_view_host.gd`；不改主仓（证据=前端仓分支 commit，决策侧在 mac 直接可审）
- 验收标准：①平台分支 diff（Windows 臂逐字保留）；②适配类实现清单 vs B7A 契约逐项对照（功能/降级标注）；③check-only + headless 冒烟 exit 0；④真栈手测清单成品+`[等待真栈验收]`；⑤前端仓分支与 commit hash 回执
- 停止条件：契约面有 webui 主链必需但 CefTexture 无法等价实现的能力 → 证据上交裁定口径；CEF 适配在真实前端工程实例化崩溃 → 按 B2P 手册 §4 排查仍败转 blocked；需改 agent/内核配合 → 域外上报
- 领取：2026-09-19 10:38 / origin/main=d117dcb1aad69982726eb55379cf4f66e4c93530（B7A 已验收合入：ruling 2026-09-19-B7A-pass + 契约文档 extension/src/vit_webview_host.md 在 main，前置满足）/ 分支 port/b7b-cef-webview-host（前端仓 ~/Documents/vit-daw-frontend，本地仓无远端）
- 回执：前端仓（本地仓无远端，工作树即 port/b7b-cef-webview-host 检出，决策侧在 mac 直接审）分支 `port/b7b-cef-webview-host` commit `bc82057`（3 文件 +463/-9：vit_dock_root.gd +30/-9、新增 vit_cef_web_view_host.gd 441 行 + .uid）。**验收①**：`_ensure_agent_webui_host()` 平台分支——macOS→`load("res://vit_dock/scenes/vit_cef_web_view_host.gd").new()`（GDScript class_name 类不在 ClassDB，ClassDB.instantiate 不可用；已核实 B7A 契约面消费全部经 has_method/has_signal 守卫，RefCounted 实例即契约兼容）；Windows 臂原行文本逐字保留包入 else（仅缩进 +1），下游信号连接/create 调用段共享零改动。**验收②**：适配类 vs B7A 契约逐项——功能子集 11 方法（create_window/create/navigate/close/set_bounds/set_visible/focus/eval_js/post_web_message/set_user_data_subdir/debug_state）+5 信号全实现；CefTexture 权威 API 经 headless ClassDB 内省核实（v1.15.4：preload_script 属性在位）；webui 闭环零页面改动——preload_script 文档创建期注入 chrome.webview 垫片（postMessage→sendIpcMessage / addEventListener('message')→__vitDispatchHostMessage 分发 event.data，垫片幂等+load_finished 兜底再注入），ipc_message 原样透传 web_message_received（三通道不拆包；页面侧 App.tsx browserBridge 面已核实对齐）。**验收③**：--import exit 0（VitCefWebViewHost 全局类注册）；check-only 适配类 exit 0；vit_dock_root check-only 报错与 HEAD 原版**签名一致**（VitDebugFlags 系 autoload 不入 check-only 的工具限制，最小对照实验+HEAD 基线双证，零新增错误）；整工程 headless boot --quit-after 3 exit 0 零脚本错误；适配类 headless 冒烟 39/39 PASS（契约面存在性/非成员缺席/未就绪守卫发 load_error/降级 no-op 返 false/subsanitize/垫片双端点/CefTexture 在位）；测试运行后 ps 零 CEF helper 残留。**验收④**：手测清单见下，`[等待真栈验收]`。**验收⑤**：分支+commit 见上。**降级清单（v1 显式声明）**：a) create_overlay_window no-op+warning 返回 false（Ask Vit Browser overlay 面——_agent_browser_host 仍指 VitWebViewHost，mac 类缺→该面板报不可用，属设计内降级）；b) eval_js 异步无返回值返 null（契约 §5 分界条款；返回值消费方在 browser host capture，mac webui 主链不消费）；c) create 等价 create_window 缺省形态 800x600（真实主窗内嵌布局不在 v1 面）；d) set_user_data_subdir 仅存储回显 debug_state（CefTexture 无按实例 profile API，CEF 数据按工程全局落 app_userdata，B2P 手册 §4.4）；e) get_selected_plugin_*/vit_open_strip_silence_dialog **未实现**——按 B7A 契约 §3（ruling 已核准"防 B7B 误实现标注正确"）非 VitWebViewHost 成员（目标为 webui 宿主面板节点，vit_dock_root:3505 自身委托链，平台无关不受本卡影响）；卡面降级清单此三项系卡面早于 B7A 勘正的陈旧表述，按已核准契约执行并在此报备。**端测边界声明**：headless 覆盖=脚本解析/类注册/契约面/守卫/降级 no-op；**未覆盖（真栈手测闸门）**=真实 OS 窗口创建与 CefTexture 实浏览器、页面渲染、对话往返、关窗干净退出、IME（上游未测面）。前端仓另有先在未跟踪 VitApp/ 目录（领取前已存在，未触碰，报备）。
- 回执附：**用户手测清单**（手测入口按 AGENTS §5：用户从 Godot 拉起 DAW 前端→启动页进入工程；前端仓工作树已检出本卡分支）——①打开 Ask Vit（聊天入口）：应弹出独立"Ask Vit WebUI"窗口且**不再打开 Safari**；②窗口内 webui 页面渲染正常（聊天界面完整）；③输入一条指令→agent 回复出现；若指令产生 mutation（如选中 clip）→DAW 主界面同步变化（mutation 通道闭环）；④关闭该窗口→界面消失；再次打开 Ask Vit→窗口复现且内容仍在（hide/reshow 语义）；⑤退出整个 DAW 后终端执行 `ps aux | grep "Godot CEF Helper" | grep -v grep` 计数应为 0（无 CEF 残留）；⑥（预期降级项）webui 内 Ask Vit Browser 面板：显示等待/错误状态而非崩溃；⑦（边界项）聊天框中文输入法行为如实记录（上游 IME 未测面）。
- 验收：
