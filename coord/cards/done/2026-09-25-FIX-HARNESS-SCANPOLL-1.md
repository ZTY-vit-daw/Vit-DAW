# FIX-HARNESS-SCANPOLL-1：harness scanAvailablePluginRows 轮询落点修复——250ms 全量应答跑在内核 JUCE message thread 饿死出壳扫描收集

- 优先级 / 预估 / 依赖：P2（旅程/烟测驱动面性能缺陷，非产品面）/ 0.3 天 / PORT-PCBATCH-MAC-LEGS-1 腿 3 发现（取证=600s/1800s 双看门狗实证+3s 静默轮询对照，工件 leg3_certauth/）
- 模型分级：L2 / GLM-5.3（agent Go harness 层）
- **执行侧路由建议（2026-09-25 Mac 决策会话）：PC 侧领取**——与 mac 正在执行的 FIX-KERNEL-HYGIENE-BUNDLE-1（内核 C++）零文件域交集/零栈冲突，构成双端并行分工；mac 侧合入后补本卡目标 3 的 journey 扫描段实证腿
- **背景（取证在案）**：harness `scanAvailablePluginRows` 默认 250ms 轮询 `plugin_list_available`，跑在**内核 JUCE message thread** 语境（ZmqGateway 逐条全量应答+落日志 857KB/次）——出壳探测的结果收集被饿死，主壳 600s/1800s 双看门狗触发；静默 3s 轮询同内核 5 分钟 719 全量对照实证。驱动侧旋钮已落地（journey mac 脚本 `scan_poll_interval_ms=2000`，main=ebd0428，断言零变化）——**harness 侧默认值与轮询落点（应在轮询侧做节流/增量化/移出热路径）未修**。
- 目标：
  1. 定位 harness.go 轮询实现，修复方案二选一（执行侧按代码面最小改动定+回执说明）：默认轮询间隔提到合理值（≥2s，与驱动旋钮一致）/ 或应答增量化+日志降频（避免 857KB×4/s 级冲刷）
  2. 红测试：修前红=轮询间隔/应答量断言（形态按实现定）；修后绿
  3. 回归：agent 全量+webui 绿；mac journey 一轮（扫描 warm-up 不再看门狗超时，scan_poll_interval_ms 旋钮可回默认验证）
- 文件域：`agent/internal/harness/`（轮询点）+测试；不改内核/协议
- 验收：①红绿 ②全量绿 ③mac journey 扫描段实证（旋钮默认值下完成）④回执两端 HEAD
- 停止条件：修复需协议/内核配合（增量化需内核面改动）→ 上交拆卡
- 领取：2026-09-26 PC 执行侧（L2/GLM-5.3）/ origin/main=7563b235（领取时 HEAD=当前 main 顶）/ 分支 port/fix-harness-scanpoll；领取时工作树遗留：M CURRENT-STATE.md、M VitApp/Workspace/default_project.xml、coord/runs/ 多卡未跟踪工件——均他卡/运行时域，本卡零接触；本卡新增 diff 将限于 agent/internal/harness/
- 回执：**PC 执行侧完成（2026-09-26）**。实现 commit=**62b256bd**（分支 `port/fix-harness-scanpoll` 已推 origin；实现基线=main a44a8494）。**方案选 a）默认轮询间隔 250ms→2000ms**（落点 `agent/internal/harness/harness.go` `scanAvailablePluginRows`：新常量 `defaultPluginScanPollIntervalMS=2000`，与驱动旋钮 `scan_poll_interval_ms=2000`（ebd0428 双端已验）一致）；未选 b）的原因：应答增量化/日志降频需内核 ZmqGateway/协议面配合（本卡停止条件），且取证已证 2-3s 量级节奏足以消除饿死（mac 静默 3s 轮询同内核 5 分钟 719 全量对照），a) 最小改动已达成同等节奏。显式旋钮（`scan_poll_interval_ms`/`poll_interval_ms`）与 10ms 下限原样保留（既有测试以 10ms 显式加速不受影响）。**验收①红绿 ✓**：红测试=`TestScanAvailablePluginRowsDefaultPollCadenceSparesKernelMessageThread`（无旋钮走默认，桩内核恒 scanning，断言 2.2s 窗口 ≤1 poll 且首 poll ≥1500ms）——修前红=2.2s 窗口 **8 polls** exit1，修后绿 exit0（工件 `coord/runs/FIX-HARNESS-SCANPOLL-1/red_default_poll_cadence.txt`）；**②全量绿 ✓**：harness 包 15.3s exit0 / agent 全量 84 包 0 FAIL exit0 / webui 28 文件 324 测 exit0（工件同目录 `green_harness_package.txt`、`go_full_agent.txt`、`webui_tests.txt`）；**③mac journey 扫描段实证腿=转交 mac**（按卡面路由；验收口径=旋钮回默认值下完成一轮 journey，扫描 warm-up 不再看门狗超时——mac 腿过后 ebd0428 旋钮补丁是否撤除由决策侧定夺）；**④两端 HEAD**：领取 origin/main=7563b235 / 实现基线 main=a44a8494 / 实现分支=62b256bd。端测边界声明（§5）：PC 侧未起真栈——本改动的真栈验证即卡面③journey 扫描段（真实三件套+719 插件扫描），该腿按路由留 mac。事件记录：领取推送时远端 main 前移（mac HYGIENE 收口 1c24e455），`pull --rebase` 被工作树遗留改动阻断（§12 禁 stash），改走 merge 且手工解决 git 目录改名启发式误判（HYGIENE doing→done 触发，git 建议把本卡挪 done/，已驳回保留 doing/），merge commit=a44a8494。
- 验收：**pass（2026-09-26 PC 决策会话）**——①方案 a 采信（b 的弃取理由成立：增量化需内核/协议配合=卡面停止条件；取证已证 2-3s 节奏等效）；常量落点/旋钮保留/10ms 下限审查通过，与驱动旋钮 ebd0428 一致。②红绿采信（8 polls→≤1 poll 修前红实证）+**决策侧独立复跑**：合并树（cherry-pick cfad7f9b）harness 包绿×3（含新节奏测试 19.6s）+agent 全量 84 包 ok+webui 324（零 webui 文件采信回执）。**§11 flaky 登记（首次）**：决策侧首次全量中 `TestMixboardSnapshotConcurrentDualWriteNoUnannotatedFork`（F5 并发双写测试）负载下一次红——按纪律三步定性：隔离 -run×5 绿+整包复跑绿+与本卡 diff 零代码关联（本卡仅改扫描轮询节奏，不触快照双写路径；新增 2.2s 节奏测试仅改变并行调度时序暴露其固有竞态窗口）→**负载型 known-flaky 首例登记观察，重复出现开修复卡**。③mac journey 默认旋钮实证腿=转交（裁定：**mac 腿过后撤除 ebd0428 旋钮补丁**——默认值已 2000ms，显式旋钮变冗余，撤除保持驱动脚本最小面，mac 顺手或随下卡）。④收口事件注记（merge+目录改名误判驳回）采信。实现经 cherry-pick 入 main=cfad7f9b（分支 62b256bd 同内容）。
