# L2-2-SEG-SMOKE-1：SegmentationPrimitives 真栈烟测腿——feature snapshot 落盘断言+确定性复验

- 优先级 / 预估 / 依赖：P1 / 0.4 天 / L2-2-SEG-1 已验收合入（内核侧特征产出）；复用 run_project_stems_import_smoke.ps1 体系（§5 同模式）
- 模型分级：L2 / GLM 亲自或强督导（真栈操作+断言脚本）
- **执行侧（PC 会话；§9 真栈单会话所有权——领取前确认无栈运行）**
- 目标：
  1. **内核增量构建**：`cmake-build-pcverify1` 的 VitApp Release 增量（含 SEG-1 的 L3AcousticAnalyzer 改动）；烟测经 dev_agent_smoke `-KernelExe` 指定新二进制——**不动 Export/staging（演示面二进制）**；回执记录新二进制时间戳+sha。
  2. **真栈触发脚本**：`scripts/seg_primitives_smoke.ps1`（同模式新增）：真实三件套 + 真实 stems 导入（复用 stems import 体系的素材与导入路径）→ L3 分析跑真实管线 → feature snapshot 落盘。
  3. **断言组**：①落盘工件中 `dad_l3_segmentation_primitives.v1` 载荷存在 ②三子结构合法（onset_events 含 detected_count/published_count/truncated；onset_density 帧数组非空；energy_novelty 帧数组非空+汇总）③**确定性复验**：同素材两轮导入，两份 primitives 载荷排除时间戳字段后逐字节一致（单测 T1 的真栈版）④exit 0。
  4. 回执：完整命令/退出码/run ID/HEAD/新二进制 sha/工件（§9 契约）。
- **环境教训（TIM-SMOKE 腿实测）**：本机内核冷启动（994 条插件表加载）可能超 dev_agent_smoke 默认 20s UI 端口预算——脚本等待预算显式放宽（≥60s）或预热后复用；若仍环境失败，按 §8 归类环境中断并保留原始输出，不硬跑。
- 约束：零内核代码改动预期（纯验证腿；若发现缺陷取证上交不开修）；新 run 目录不覆盖；fixture 用临时/既有素材不动权威样例（§10）。
- 验收：四断言组 exit 0+新二进制 sha 记录在案
- 停止条件：真栈上 primitives 载荷缺失（L3 管线未触发特征请求）→ 取证上交（AudioFeatureService 路由接线的真栈形态疑点）；两轮确定性不一致 → 取证（这是功能红线，立即上交）
- 领取：2026-09-26 22:40 +0800 / origin/main=f7b60285（pull 后 HEAD，Already up to date）/ 领取前工作树仅运行时状态（M VitApp/Workspace/default_project.xml）+ 其他卡 untracked run 工件，无实现 diff；tasklist 核对无 VitApp/VitAgent/Godot 进程（§9 无栈占用）
- 回执：
  - **实现 commit**：`e7890ba1`（分支 `port/l2-2-seg-smoke-1`，已推 origin）；2 files +934/-0：`scripts/seg_primitives_probe.py`（新，ZMQ REQ/SUB 探针）、`scripts/seg_primitives_smoke.ps1`（新，三件套编排+断言复核+栈清理）。零内核/agent 代码改动（卡面约束达成）
  - **内核增量构建（目标 1）**：`cmake --build cmake-build-pcverify1 --config Release --target VitApp` exit 0——增量重编 L3AcousticAnalyzer.cpp+AudioFeatureService.cpp（SEG-1 的两个改动文件）后链接；仅历史存在的 C4819 第三方头警告（choc 头，与改动无关）。新二进制 `VitApp\cmake-build-pcverify1\VitApp_artefacts\Release\VitApp.exe`，mtime 2026-09-26T14:43:24Z，sha256 `47c4c3f5626c0649d73f56175afb3d162ce730978e40348ac57c21f2e6790583`（旧二进制 16:57 早于 SEG-1 合入 21:26，确认含改动必须重建）。Export/staging 零触碰（dev_agent_smoke 默认 KernelExe 即 staging，脚本显式 -KernelExe 覆盖且默认解析也指向 pcverify1）
  - **烟测设计（目标 2）**：dev_agent_smoke 拉起真实三件套（`-StartKernel -StartUI -NoChatSmoke -NoStripSilenceSmoke -Strict -KernelExe <pcverify1 二进制>`，`WaitSeconds=90` 落实卡面 ≥60s 等待预算）；probe（python+zmq）复用 stems import 体系命令链（new_project→save_as 隔离工程→project.import_preflight→project.import_folder_as_stems，素材=TrainingFolder `yingge - sattelites tracks out`）→ 真实导入自动排队后台 L3 分析（ImportService:588 路径）→ 显式 `warm_waveform_bake`+`feature_type=segmentation_primitives`（AudioFeatureService:199 SEG-1 路由）两轮 → 自开 SUB（5556）收 `audio_feature_data_ready` 载荷落盘
  - **正式 run**：`seg_primitives_smoke_20260926_231942`，命令 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\seg_primitives_smoke.ps1`，**exit 0**；HEAD=f7b60285+脚本未提交态（脚本在 port 分支 e7890ba1 提交，工作树同内容）；四断言组：**A1** 两轮均收到 `dad_l3_segmentation_primitives.v1` 载荷（status/quality=ready）；**A2** onset_events detected=26/published=26/truncated=false（method=rms_frame_local_peak_rise_v1，一致性 published==min(detected,1024) 与 truncated 语义均断言）+onset_density.frames 2679 帧非空+energy_novelty.frames 2679 帧非空且 max_novelty=0.018153/mean_novelty=0.00014019 汇总数值合法；**A3** 两轮载荷排除 updated_at+request_id 后 canonical JSON 逐字节一致（484,377 字节，排除集对齐 SEG-1 单测 T1 先例再 加烟测侧 request_id，理由：request_id 是探针生成的请求标识非计算产物）；**A4** exit 0。bake 目标=导入轨首个 stem（`sattelites 10-IS_percussion_tom_analog.wav`，228.57s 整曲，L3 帧网格 85.33ms 窗/hop）；后台管线观测=waveform 138 条+band/stereo/loudness 各 3 条（3/3 clips 全达）——stems 导入触发 L3 真实管线有事件流实锤
  - **复验 run（同代码第二轮）**：`seg_primitives_smoke_20260926_232440` 同样四组全过 exit 0，canonical bytes 同为 484,377（跨 run 也一致；重复成功 2/2，样本仅证存在稳定成功路径）
  - **真栈形态发现（对决策侧有情报价值，两项均非缺陷不开修）**：① **AudioFeatureService 30s merge 窗口**（AudioFeatureService.cpp:161→shouldMergeRecentAudioFeatureRequest）：merge key=track|subject|feature|offset|length|frame_width|revision，**不含 request_id**，同 key 且优先级不更高→静默丢弃无任何发布——卡面预警的"路由接线真栈形态疑点"确切答案：路由本身工作正常（SEG-1 改动真栈可达），疑点实体是此节流；重复请求者必须隔 ≥30s 或改变 key 组成（probe round2 等 35s 适配）。② **agent 侧落盘白名单不含 segmentation_primitives**（harness.go:3909 IngestKernelTelemetry 只认 band/stereo/loudness 三型→l3_feature_log.jsonl/mixboard_feature_snapshot.json 均不收录）——本卡"feature snapshot 落盘"由烟测探针落盘承担；若后续 DAD 层要消费该特征需扩 agent 白名单（域外，登记供决策侧排卡）。③ 61-stem 全量导入时每个 clip 排一个整曲 L3 bake，单线程串行池（L3AcousticAnalyzer.cpp:1091，K1 确定性契约）积压数十分钟——probe `--max-import-files` 走 run 目录 staged 子集（复制前 3 个 wav，§10 不动权威素材）；全量 61/61 tracks/clips 导入已在失败 run 224515 验证成功
  - **失败分类与止损（§8，3 个失败 run 全保留）**：①run 224515：61-stem 全量导入后 round2 bake 在串行池排队 600s 超时（A1 round2 FAIL）——排队延迟非功能缺失（round1 已收到且 A2 绿），分类=脚本预算缺陷（工具性），修复=staged 子集+后台消化等待；②run 230125：staged 子集后 round2 仍超时——根因即上发现①merge 窗口去重（round2 从未入队），修复=round 间隔 35s+后台摘要事件计数达标后再发；同 run 暴露 ps1 两个工具性 bug（PS5.1 Get-Content 默认 ANSI 读 UTF-8 JSON 崩溃+ConvertFrom-Json 拒绝空字符串键——probe 聚合键净化+显式 -Encoding UTF8+断言复核异常安全化修复）；③无同形态确定性失败两次，止损线未触发；两次失败均留下完整 probe summary+日志供复核
  - **边界与诚实声明**：①烟测入口是自动化脚本栈（AGENTS §5），非用户手测途径；②断言用 staged 3-stem 子集导入（时长可控），全量 61-stem 导入的证据在同日失败 run 224515（导入/后台管线部分成功，bake 等待超时）；③bake 用 track_id+allow_file_source 文件源（ImportService 显式文件源分支），track/clip 为导入真实轨，file 为该轨源文件——源由 file_path 决定，分析管线真实；④确定性排除集比 SEG-1 单测 T1 多排除 request_id（探针生成标识），其余全字段含数组逐字节比较；⑤运行时工程写 run 隔离 temp_project（new_project 后 save_as），default_project.xml 未被本卡触碰（其 M 态为领取前既有）；⑥A3 的逐字节一致是 canonical JSON（sort_keys+紧凑分隔符）意义下的一致，与 T1 单测同构
  - **工件**（coord/runs/L2-2-SEG-SMOKE-1/，目录不覆盖）：`seg_primitives_smoke_20260926_231942/`（run_report.json verdict=PASS、seg_probe_summary.json、payload_round1/2.json 各 ~484KB、canonical_round1/2.json、seg_probe_run.log、temp_project/、stems_staging/）；`seg_primitives_smoke_20260926_232440/`（复验同构）；失败取证 `20260926_224515/`（61-stem 全量导入+排队超时+ps1 编码崩溃现场）、`20260926_230125/`（merge 去根因现场）
  - **停止条件未触发**：primitives 载荷在真栈上存在（两 run 双轮 ready）；两轮确定性逐字节一致（功能红线未触碰）
