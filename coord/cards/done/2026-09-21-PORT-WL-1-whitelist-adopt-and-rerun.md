# PORT-WL-1：mac 实验白名单机器采纳 + ⑤ 复跑判定（放大器假说检验）

- 优先级 / 预估 / 依赖：P1 / 0.5 天 / 依据 [2026-09-21-pc-45-reply.md](../reports/2026-09-21-pc-45-reply.md) §五处置意见（白名单先行→复跑→pro 二线）；与 PC 侧 PORT-PS1-N5-1（已派）跨机并行
- 模型分级：L3 / GLM-5.3（字段实测映射 + 假说检验判定）
- 背景：PC 清点⑤ vocal focus 09-20 后 **7/7 提案面停车**（flash+白名单在位）vs mac **0/5**（flash+白名单缺失）；mac ⑤ R5 双跳前两跳全绿、死在 `~/.vit/free_state_experiment_plugins.json` 缺失（hop-2 `d1_execution_blocked`）。机制假说：实验路径死→提案无门→强化重试→无可采纳终态→诚实终止族。本卡=补白名单+复跑，一次检验假说与修复问题。
- 目标：
  1. **白名单构建**：按 PC 参照 schema（`vit.free_state_experiment_plugins.v5`，七族 `static_eq/broadband_compression/de_esser/transient_shaper/limiter/gate_expander/multiband`；字段 `plugin_name/manufacturer/format/plugin_identifier(VST3-名-hash)/plugin_path/bands[](center_hz+gain_param_id_ch1/ch2)`）构建 mac 机器态文件——**数据源=mac 自己的实测**：主体身份/path/identifier 取 C2 白名单草稿+`~/.vit` 语义索引（跨端逐字节一致已证）；bands 的 center_hz/gain_param_id 从 C3 reprobe 工件的参数面快照（`~/Documents/vit-c3-artifacts/*/probe/`）实测提取；**不得手编**——无法实测的字段如实标注留空并说明
  2. **落盘与校验**：写 `~/.vit/free_state_experiment_plugins.json`（机器本地，AGENTS §10）；schema 校验 + agent 启动读取无错；逐字段溯源表入工件（字段值↔实测来源）
  3. **⑤ 复跑判定（假说检验）**：白名单就位后 `run_vit_product_path_smoke_mac.sh` ×5（MAC-4 同口径：≥4/5 达标、失败逐轮归类）——**假说预测：停车率显著回升**；顺带跑 ④×5（同受提案路径影响的姊妹件）
  4. 结果三去向：达标 → ④⑤ 收口+端测收官声明（另行裁定）；停车率回升但 <4/5 → 残差归因上交（届时 pro 对照升一线，判别力已干净）；无变化 → 放大器另有其因，全部取证上交
- 文件域：`~/.vit/`（机器本地白名单）+ `scripts/`（如需构建/校验脚本，最小化）+ 工件目录；`agent/`、`VitApp/`、前端仓零触碰
- 验收标准：①白名单文件+逐字段溯源表（实测来源可回指）；②agent 读取校验证据；③④⑤ 各 ×5 运行账目（run ID/退出码/逐轮归类/成功率）+ 与 MAC-4 基线的对照表；④假说判定结论；⑤工件齐备（LLM key 零入工件，flash 口径）
- 停止条件：bands 参数面实测发现 C3 工件不含所需 param_id 粒度 → 改用 pluginprobe/reprobe 现跑提取（C3 驱动在 main），仍不行则上交；白名单就位后 agent 读取报错 → 取证上交；复跑暴露 mac 脚本新缺陷 → 修补列明该轮作废重跑
- 领取：2026-09-21 11:05 CST（Mac 执行流，用户直令「开工 PORT-WL-1」，BOOTSTRAP 口令表授权）/ origin/main `03dba62622474232f0d08b8d144d0a678f32d5be` / 主工作树直跑（本卡预期零仓库代码改动：白名单落 `~/.vit/` 机器本地，构建/校验工具与工件落 `~/Documents/vit-wl1-artifacts/`，仓库内仅本卡状态变更——"scripts/ 如需"按最小化裁定为不需要入仓）。领取时工作树已有 diff：`VitApp/Workspace/Settings/Settings.xml`、`VitApp/Workspace/default_project.xml`（MAC-4 时期运行时残留，非本卡产物，不触碰）。复跑归类对齐 117e930 五形态族谱（正路径=提案面停车；F1 能力边界/F2 中间态/F3 无可采纳/F4 done 不停车/F5 复合环境单列）
- 回执（2026-09-22 22:30 CST，mac 执行侧 GLM-5.3，零仓库代码改动，工件根 `~/Documents/vit-wl1-artifacts/`）：
  - **①白名单构建**：`~/.vit/free_state_experiment_plugins.json`（v5，五段：de_esser=Sibilance Stereo(threshold=2)/transient_shaper=Smack Attack Stereo(attack=2)/limiter=L1 limiter Stereo(ceiling=1)/gate_expander=PSE Stereo(range=6)/multiband=C4 Stereo([14,21,28,35])），`build_whitelist.py` 四源程序化提取零手编（S1 语义索引身份 / S2 双 attestation store 晋升+覆盖轴+身份交叉 / S3 C3 参数面双 run 零漂移 / S4 认证收据 role→param_id 锚定），逐字段溯源表 `provenance_table_final.json` 40 行。**两段如实留空**：static_eq（C2/C3 实测集六族无 EQ，无实测源不手编——EQ 提案 hop-2 仍将 not configured，如实声明）；broadband_compression（C1 comp 实测面单共享 Threshold，v5 要求 ch1≠ch2 双通道形态，无 mac 面满足）。选主规则与 transient_shaper 轴错配注记等详见 RUN_LEDGER §一。
  - **②落盘校验**：overlay 模式一次性 go test（C3 先例零源码树写入）：真实 loader Load PASS + 五段真实 hop-2 admission 谓词全 PASS（活 store 指纹重算+族晋升比对），`verify/verify_run.log`；`~/.vit` 前后态哈希（仅新增白名单一个文件，其余零变动）。栈上加载器执行证据=统计轮 hop-2 链（见④注记：本轮 tick 全为 track_gain 域，插件段执行面未被统计轮走到，加载器+admission 面以 overlay 测试为验收证据——边界如实声明）。
  - **③④⑤ 复跑（batch3 2026-09-22，白名单就位，10 轮遥测全 flash，key 零入工件）**：**④ 2/5**（R1/R3 正路径双跳全链；R2/R4=F2、R5=F4）——与 MAC-4 基线 2/5 持平；**⑤ 3/5**（R1/R2/R5 正路径全链：vocal focus 提案面停车→clarify 双跳→应用+回读；R3=F2 clarify ask、R4=F2 vocal focus）——MAC-4 基线 0/5、PC-N5 同口径 3/5；**⑤ vocal focus 提案面停车率 4/5**（MAC-4 0/5）。
  - **④假说判定：放大器假说成立**——停车率 0/5→4/5（同引擎/同断言/同 fixture），⑤ 通过率与 PC-N5 拉平（3/5=3/5），双端 ⑤ 差距闭合；机制注记：白名单作用面=提案可停性（admission/能力面），本轮 hop-2 tick 全为 track_gain 域，插件参数写路径端到端不在本统计面。三列对照表已拼入 PC-N5 骨架第三列（RUN_LEDGER §四）。
  - **卡面路由落点：分支 2（停车率回升但 ⑤ 3/5<4/5 → 残差归因上交）**：残差全部为 F2 中间态（settle 窗 limit_reached 未收敛），与 PC-N5 ⑤ 红轮主族同族——双端白名单在位时均在的 flash+settle 语义族；pro 对照（升一线）与 ④ 的同族残差处置上交决策侧。
  - **§8/§9 账目**：统计批 10 轮零环境失败；环境批（09-21）cmake 被墙×1+驱动缺陷空跑×10+kernel CoreAudio 卡顿×3+LLM 端点 >60s 客户端超时×5（原始证据=driver_logs+各 run workdir+agent timing；MAC-4 绿轮 LLM max 33s 对照）+2 个有效 F2 红轮作一致性旁证不计入 N=5；kernel 复用 MAC-4 同哈希二进制（fd0d3d84…）。上交：⑤ F2 残差处置、static_eq/broadband 补齐路径（需 EQ 主体的 reprobe+PCA 认证，另开卡）、LLM 60s 客户端超时与 CoreAudio 卡顿工程面知会。
- 验收：**pass（2026-09-22 决策会话，PC 决策侧）**——①白名单构建采信：四源程序化提取零手编+40 行溯源表，两族留空声明诚实（static_eq 无实测源/broadband 不满足 v5 双通道形态，不手编不凑）；②加载校验采信（overlay go test loader+五段 admission 谓词 PASS；插件参数写路径不在统计面的边界已如实声明——本轮 hop-2 全 track_gain 域）；③统计批 10 轮账目合规（零环境失败、flash 全程、key 零入工件；环境批另行分类不计入 N=5 处置正确）；④**假说判定=放大器成立，采信**：vocal focus 停车 0/5→4/5、⑤ 与 PC-N5 拉平 3/5=3/5，双端 ⑤ 差距闭合——对照口径干净（基线 296ad46 白名单为唯一功能差量）；⑤三列对照表第三列拼入核验（diff 仅 N5-1 卡 15+/15-，数字自洽）。**上交四项裁定**：a) ⑤ F2 残差处置=**不开新卡**——mac 残差与 PC 同族且 PC 侧 F2 修复（b184e5d9）已入 main，mac 拉取+移植 settle 兜底（转交包 §一.2 由"建议"升级为"随 main 一并做"）后应显著吸收，复测仍现族再开卡；b) pro 对照=**暂缓一步**——先吸收五补丁+脚本移植后复测 ④⑤，若仍有残差再升一线（判别力更干净）；c) static_eq/broadband 补齐=**开卡 PORT-WL-EQ-1**（todo/，mac 侧）；d) LLM 60s 客户端超时+CoreAudio 卡顿=**登记为 mac 环境已知项**（环境批非统计批），统计批复发再开卡。另注：④ R5 的 F4×1 按勘误口径读取（mac 基线 296ad46 在 GATE-FRESHNESS 修复前，可能带门滞后前缀——mac 可顺手查 RUN_LEDGER，不改表）。
