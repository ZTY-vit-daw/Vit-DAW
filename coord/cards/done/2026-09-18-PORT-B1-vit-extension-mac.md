# PORT-B1：vit_extension mac 目标（godot-cpp 就位 + SCons macos 构建 + gdextension 条目 + headless 加载验证）

- 优先级 / 预估 / 依赖：P2 / 0.5-1 天 / 无（层 B 首张实现卡）
- 模型分级：L2 / GLM-5.3（构建链接面 + Godot 扩展加载语义）
- 目标：仓内 `extension/` GDExtension 在 mac 可构建可加载：① repo 根取得 godot-cpp 源树（`extension/SConstruct:1` 引用 `../godot-cpp/SConstruct`；clone **godot-4.6.stable** tag，与本机 Godot `4.6.stable.official.89cea1439` 对齐——**未跟踪依赖不入仓**，确切 pin 记回执）；② `scons platform=macos arch=arm64` 双 target（template_debug/template_release）构建 exit 0 出 .dylib；③ `vit_extension.gdextension` 补 `macos.*` 条目（现仅 windows 两条）；④ 本机 Godot headless 加载验证（`--headless` import/等价机制，扩展加载零 error 日志）。**卡面口径注记**：PORT_AUDIT §1.3(c) 所记 `vit_webview_host` 源**从未入仓**（PC 本地未提交文件，决策侧 2026-09-18 git 考古核实：`git log --all -- '*vit_webview_host*'` 零命中）——仓内面仅 register_types + vit_waveform_reader，无 webview2 面，按仓内事实执行
- 文件域：`extension/`（SConstruct mac 适配如需、vit_extension.gdextension）；godot-cpp 为根级未跟踪依赖；不改 Godot 工程（仓外）、不改 `agent/`、`VitApp/`
- 验收标准：① godot-cpp pin 记录（tag+commit hash）；② scons 双 target exit 0 + 产物路径/大小/sha256；③ .gdextension macos 条目 diff；④ Godot 4.6 headless 加载 exit 0 且扩展注册无 error（日志入工件）；⑤ 工件目录：run ID + 构建/加载日志 + 产物哈希
- 停止条件：godot-cpp 4.6 与现有 SConstruct 构建面不兼容需非平凡改造 → 最小适配方案上交；headless 加载扩展报错 → 按类型记录证据转 blocked；需动仓外 Godot 工程才能验证 → 上交
- 领取：2026-09-18 20:58 CST / origin/main d67048535d8be85c547f34c0abcffdf11b912a60 / port/b1-vit-extension-mac
- 回执：实现 commit `bac98a9`（分支 `port/b1-vit-extension-mac` 已推；3 文件 +126/−6 全在卡面 `extension/` 域：SConstruct、src/vit_waveform_reader.cpp、vit_extension.gdextension；godot-cpp/ 为根级未跟踪依赖未入仓，extension/bin/ 产物被 gitignore `bin/` 挡下未入仓）。权威 run `b1-build-20260918-210251`，工件 `/tmp/vit-b1-artifacts/b1-build-20260918-210251/`（SUMMARY.md + 双 target scons 日志 + 全部 godot 运行/对照日志 + 测试工程与驱动脚本）。要点：**①pin 前提偏差**——上游无 `godot-4.6.stable` tag（最新 `godot-4.5-stable`/branch `4.5`），实际 pin master@`507ed9d`（shallow），其仓内 `extension_api-4-6.json` 自证 v4.6.stable.official（sha256 `00a3ad6d…`），SConstruct 显式 `api_version=4.6` 对齐本机 4.6.stable.official.89cea1439；**②双 target exit 0**，arm64 dylib 184688/184656 B，sha256 `d0c0be10…`/`f31ac752…`，ad-hoc 签名；**③macos 条目** `macos.debug.arm64`/`macos.release.arm64` 两行（与 windows 命名律对称）；**④headless 运行模式加载零 error**（CLASSDB 注册 ✓、实例化 ✓、缺段 ENOENT→空、float_count<=0→空、Python 跨进程 POSIX 段正向读回 8/8 逐位相等，全 exit 0）；`--script` 模式不加载工程扩展（以运行模式为准）。**SConstruct 适配（卡面"如需"授权内）**：godot-cpp v10 已除名 `modules_sources/add_source_files`→Glob 源；`api_version` 须经 SConscript 导出传入；v10 内部 `env.Default()` 抢占默认目标→补 `Default(library)`；产物路径 `#extension/bin` 会叠成 `extension/extension/bin`→改 `#bin/` 对齐 .gdextension 的 res://extension/bin 契约（PC 历史产物路径疑同病，验收时请一并裁量）。**POSIX reader 新增**（卡面注记"仅 register_types+vit_waveform_reader"之实现必然：原文件为纯 Win32 shared memory，mac 无法编译）：`shm_open(O_RDONLY)`+"/" 前缀规约对齐内核 SharedMemorySegmentPosix 发布名+fstat 尺寸守卫（防 SIGBUS，镜像 harness shm_darwin）+ENOENT 静默空/他错 warning 空；Win32 分支 `#ifdef _WIN32` 逐字未动；utility_functions 头 v10 迁至 variant/ 以 `__has_include` 双路径兼容 PC 旧 godot-cpp。**引擎侧事故定性（非本扩展缺陷）**：本机 Godot 4.6.stable 对任何含 GDExtension 的工程**首次** `--headless --import` 在扩展加载成功后的编辑器退出文档生成段 SIGSEGV（E1 134 两次复现；二次 import 0；无扩展对照 0）——环境事实上交决策侧知悉，后续前端工程首 import 会命中一次，不阻塞本卡（运行模式全绿）。**端测边界**：release 模板内 release dylib 加载未测（本机无 release player）；与真实内核段端到端读回属后续 B 层集成面。worktree `/private/tmp/vit-b1-worktree` 保留供复验
- 验收：pass（2026-09-18，ruling [2026-09-18-B1-pass.md](../../rulings/2026-09-18-B1-pass.md)：diff/工件全过；pin 前提偏差采认（上游无 4.6 tag，master@507ed9d 以 extension_api-4-6.json 自证）；产物路径改动裁定无 PC 破坏面（打包脚本零引用 extension 输出路径）；首 import SIGSEGV 定性环境事实采认并移交 B4 卡面注记；bac98a9 已 cherry-pick 入 main）
