# FIX-FRONTIER-FOLD-1：候选名单折叠确定性——合入 GATE-FRESHNESS 兜底 + 修折叠生成/选择不对称

- 优先级 / 预估 / 依赖：P1 / 0.5-1 天 / **FIX-F3-G4-SEMANTICS 分支合入 main（31d35f8d+51686265 待决策验收）**；若届时未合入，从 fix/f3-g4-semantics 分支开工并在回执注明基线。**用户已裁定 1+2 都做（2026-09-22 会话）**
- 模型分级：L2 / GLM-5.3（门语义+折叠语义=准入核心，红测试先行）
- **背景（已定分）**：候选名单（hypothesis_frontier）由 harness 从已入账观察折叠派生（chat/audio_closure_controller.go:865 audioClosureFrontier / :893 audioClosureCandidates / :1240 audioClosureSelectedCandidate；折叠时机=回合边界 recordAudioClosureRound:534-541），AI 无写名单通道，无强引导。已证缺陷：①折叠-准入错位——锁死轮/切片续跑时名单未折，提案撞 G5/G6/G8 三连拒（真栈 20260922_182036 clarify-ask，gap=[G4,G5,G6,G8]），FIX-GATE-FRESHNESS-1（分支 fix/gate-freshness-1 @8653a25a，**未合入 main**）的 G5/G6/G8 空前沿兜底正是解法（G8 兜底=空前沿时提案目标=新鲜轨道级观测轨即一致，其余轨道/跨轨污染照拒）；②折叠生成/选择不对称——audioClosureSelectedCandidate 认"轨道级观察盯住的候选"，但 audioClosureCandidates 要求结论行内嵌轨道 ID 才生成候选：轨道级观察（target_ref=track）结论行无轨道标签→选择器有据可认却无候选可认。
- 目标（两项，用户裁定 1+2）：
  1. **合入 FIX-GATE-FRESHNESS-1**（8653a25a → 现 main 含甲后的基线，冲突重放：甲改 G4 槽位/gateG4ImprovementProposal，其改 G5/G6/G8+新增 freeStateLedgerHasUsableScan/messageLoopFreshTrackObservationTarget，逻辑不重叠仅文本冲突）；合入后原卡 FIX-GATE-FRESHNESS-1（doing/）随本卡验收一并归档，其 ⑤ exit-0 上交项以本卡合并后 ⑤ 1 轮复核为准
  2. **修折叠不对称**：audioClosureCandidates 对 usable 轨道级观察（target_ref.kind=track）在结论行无内嵌轨道 ID 时仍折出候选（轨道=观察 target，候选 ID 沿用 observationID+viewID+issueType+region+trackIDs 派生式）——"证据已入账=事实已登记"，登记窗口与选择器对齐；红测试=轨道级 usable 观察行无轨道标签修前无候选/修后有候选且可被 audioClosureSelectedCandidate 选定
  3. 红测试：item2 修前红/修后绿；合入重放后 GATE-FRESHNESS 红测试组（free_state_gate_freshness_test.go）与 F3 甲/乙测试组（free_state_terminal_adjudication_test.go×2 包）同基线全绿=两侧契约共存证明
- §8：content-blind 红线（fold 派生与 G 门兜底只用结构性字段 target_ref/view_id/轨道 ID/freshness，不引入领域内容）；弹回词表零变动（GATE-FRESHNESS 只加兜底不改词表）；mac 跟随注记（G 门为两端共享 Go 代码，合入即双端生效；mac 侧脚本无 terminal/G 门词表消费，无强制跟随项）
- 文件域：agent/internal/agentloop/free_state_gate.go + free_state_gate_freshness_test.go（自 8653a25a 携带）+ agent/internal/chat/audio_closure_controller.go + 测试；冲突重放不得改动甲的 gateG4ImprovementProposal 语义
- 验收：①②红测试修前红/修后绿（含共存全绿）；③agent 全量+webui 回归绿；④⑤ 各 1 轮真栈（存在性，⑤ clarify-ask 为本族现场）；⑤回执记合入冲突清单与两侧测试组结果
- 停止条件：重放中发现 GATE-FRESHNESS 兜底与甲 OR 语义实质冲突（非文本冲突），或折叠修改需引入领域内容才能绿 → 域外上交裁定
- 领取：2026-09-22 20:23 / origin/main=e359260c / 分支 fix/frontier-fold-1（**基于 fix/f3-g4-semantics tip 51686265 开工**——F3 甲尚未合入 main，按卡注记） / 领取时 HEAD=51686265，工作树已有无关改动：M VitApp/Workspace/default_project.xml + 未跟踪 extension 构建产物、godot-cpp/、coord/runs/
- 回执：2026-09-22 21:00 / PC 执行侧（GLM-5.3） / 分支 fix/frontier-fold-1 已推 origin（merge 056b6d88 + 实现 3cd08f21），待决策验收合入。
  - **① 合入 FIX-GATE-FRESHNESS-1**：`git merge 8653a25a` → 056b6d88。**冲突清单仅 1 项**：coord 卡片文件 add/add（GATE-FRESHNESS 卡，分支侧=完整回执版 vs 8653a25a 侧=空回执卡面，取 HEAD 版）；`free_state_gate.go` **自动合并零冲突**（甲 G4 OR 语义与 G5/G6/G8 兜底逻辑不重叠，卡面预判成立），合入 diff=4 文件 +244/-17 与原卡回执一致。**共存证明**：合并基线上 agentloop+chat 两包全量 ok（GATE-FRESHNESS 红测试组 free_state_gate_freshness_test.go 与 F3 甲 TestG4ImprovementProposalAlignsSpineOrSemantics/乙 TestFreeStateTerminalAdjudicationParksProposalNotCapabilityBlocked 同基线全绿），未发现兜底与甲 OR 语义实质冲突，停止条件未触发。
  - **② 折叠生成/选择不对称修复**：实现 3cd08f21（agent/internal/chat/audio_closure_controller.go+测试，+54/-4）。提取 `audioClosureObservationTargetTrackID` 共享助手（kind/target_kind + id/track_id/target_id 读取词序与原选择器逐字一致），折叠侧行内轨道 ID 空集时回退观察 target_ref 轨道，候选 ID 沿用 observationID+viewID+issueType+region+trackIDs 派生式零变动；选择器改调同助手（语义等价，登记窗口对齐）；行内带轨道 ID 时行为不变。content-blind：仅结构性字段 target_ref，无领域内容；弹回词表零变动；mac 无强制跟随项（G 门/fold 均两端共享 Go 代码）。**红测试** TestAudioClosureFoldsTrackTargetRowsWithoutInlineTrackIDs：修前红（track-target 观察+无内嵌轨道行→0 候选）/修后绿（1 候选+TrackIDs=[观察target]+派生 ID 逐位断言+同观察被 audioClosureSelectedCandidate 选定）。
  - **③ 回归**：agent 全量 `go test ./... -count=1` exit 0；webui `npm run test` 324/324 exit 0。
  - **④ 真栈 3 轮**（mix_single_tick E2E，-StartKernel，二进制自工作树 3cd08f21 构建）：R1 exit 1 域外红——hop-2 `D1-S1 VSP base revision does not match the fresh admitted observation`（free_state_d1_runtime.go:360，观察新鲜度竞态族；hop-1 确认+pending tick stored 均绿）；R2 exit 1 域外红——vocal clarify F2 中间态「已完成 ccb_observation_request」（与 GATE-FRESHNESS ⑤R1 逐字同形态已知族；F2 修复当时不在本卡分支基线）；R1/R2 断点不同非同断点两败，R3 **exit 0**——双跳全链 conversation=mix_single_tick_e2e_20260922_204036（observe→提案面 needs_confirmation→hop-1 工具面→hop-2 d1_post_action_evaluation_required，Track 1007 回读 0→-0.5dB）。
  - **⑤ 真栈 1 轮 exit 0**：product path 全家桶（kernel+Godot 前端+agent，artifact product_path_20260922_204302），vocal clarification loop（本族现场）在内全阶段 PASS。
  - **执行期间 main 前进注记**：决策侧 da680c75/b15dae79 已将 F2(1177a9c0)/F5(4e4d850d)/F3 乙甲(31d35f8d/51686265→58469f23) cherry-pick 入 main 并完成 GATE-FRESHNESS 卡验收归档（done/，其 ⑤ exit-0 上交项已裁定替代认定成立）——本卡"原 doing/ 卡随本卡归档"条款由决策侧提前完成；本卡分支仍基于 F3 原分支 commit 开工（内容与 main 58469f23 相同，cherry-pick 等价），合并时无冲突预期。
  - 工件：coord/runs/FIX-FRONTIER-FOLD-1/（run1-3_mix_single_tick + run1_product_path 日志、baseline_head/status，按惯例不提交）；§8 账目：④ 3 轮+⑤ 1 轮，R1/R2 域外红已逐轮分类，环境中断 0，LLM key 零入工件。
- 验收：
