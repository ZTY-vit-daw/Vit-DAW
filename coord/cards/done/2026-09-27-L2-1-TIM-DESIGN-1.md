# L2-1-TIM-DESIGN-1：编译器第一层 TIM 结构断言器 v1 设计（断言清单+实现锚点）

- 优先级 / 预估 / 依赖：P0 / 0.4 天 / 路线图 D8 第一层（AGENTIC-OBSERVATION 蓝图）；实现卡（L2-1-TIM-1）依赖本卡产出
- 模型分级：L1 / flash 可接（只读勘察+设计文档，零代码改动）
- **执行侧（mac 会话，VitApp 域）**
- 背景：路线图 D8 编译器第一层=结构合法性断言（编译级、确定性、不用听），承载者"TIM 扩展为完整断言器"；关键验收=对现有收口版本工程即刻增值（监督报警路径端侧烟测 exit 0）。
- 目标：
  1. **TIM 现状盘点**：`tim.projection.v0_2` 现有结构、数据源、产出时机、消费方（agent 侧 contextruntime/CCB 怎么用它）——锚点到文件:行。
  2. **断言清单 v1 定义（3-5 个，范围钉死不贪）**，候选池按数据可得性选：路由完整性（意外反馈环/死路——若 v1 数据不足可延后）、信号卫生（削波/NaN/Inf/DC offset）、true peak ≤ 目标、采样率/块长一致性、插件合法装载状态。每个断言器给出：**谓词（精确到可写测试）+ 作用域（轨/总线/全局）+ 数据源（现成投影字段 or 需内核新增输出——新增的标注"内核腿"）+ 判定位置（内核 VitApp vs agent 侧）+ 失败动作（warn/block，v1 全部 warn）**。
  3. **实现锚点与测试计划**：每个断言器的主要落点文件（VitApp/Source/Service 的 TIM 扩展位 or agent/internal/tim 装配位）+ 红测试方案（修前应红的最小构造）+ 端侧烟测断言草案（监督报警路径：WARN 行出现在哪、谁消费）。
  4. 设计文档落 `docs/TIM_ASSERTER_V1_DESIGN.md`（现行索引登记：规划态）+ 摘要回执。
- 约束：零生产代码改动；断言器选择以**数据可得性优先**（现有投影/内核字段能支撑的先进 v1，需新增内核输出的一律标注"内核腿"留给实现卡评估）；裁判不下场原则写入设计（断言器只红绿灯，不做修正）。
- 验收：①TIM 现状锚点完备 ②断言清单 3-5 个每个五要素齐（谓词/作用域/数据源/判定位置/失败动作） ③每个断言器有红测试方案 ④设计文档入库+CURRENT-STATE 登记（回执注明，登记合入由决策侧收口）
- 停止条件：候选断言器数据可得性全部存疑 → 现状盘点成稿+数据缺口清单上交，不开空设计
- 领取：2026-09-26 19:51 CST / origin/main=911e331 / 分支 port/tim-asserter-v1-design（用户口令"接收PC新的任务"当场批准领取）
- 回执（2026-09-26 21:10 CST 执行侧自验完成）：
  - **产出**：`docs/TIM_ASSERTER_V1_DESIGN.md`（209 行）+ CURRENT-STATE.md 登记（现行 56→57，规划态）——均在分支 `port/tim-asserter-v1-design` commit **d29b357**，已推远端；登记合入由决策侧收口。
  - **事实更正**：TIM 现行 schema 为 `tim.projection.v0`（types.go:4-5），卡面 `v0_2` 是 TOM 版本号笔误——设计文档全文按 v0 表述。
  - **断言清单 v1（5 个，全部 agent 侧判定、v1 零内核改动）**：AS-SIG 信号卫生（nonfinite：nan/inf 计数已在透传键表 mixboard.go:2893 只是 TIM 未读；clipping/headroom 谓词升格并沿用现有码）、AS-PEAK 电平上限（true peak v1 单边版：sample peak>ceiling⇒必超；默认 ceiling -1.0，RLM profile 就位后接管；真峰值=内核腿）、AS-SR 采样率一致性（audio_settings 在 shadow 已有、ImportInput.AudioSettings 字段已预留未用）、AS-ROUTE 路由完整性（rack nodes/edges+内核预标注可达性 shadow.go:678 已保留、TIM 装配未消费；死路+环 DFS 两谓词）、AS-PLUGIN 插件结构合法性（plugin_path∈表，倾向 pluginsemantics 索引；per-instance 装载态=内核腿）。每器五要素齐+谓词精确到可写测试。
  - **关键设计约束**：裁判不下场（断言器无写路径）写入实现；三态 pass/fail/not_evaluable（数据缺失≠pass）；schema 兼容按 §11（旧记录无 assertions 字段=未运行、未知枚举 fail-closed）；CCB 裁剪面 v1 零改动（fail 经 risk_summary 汇总披露）。
  - **测试计划**：红测试 T1-T9（表驱动，含旧工件往返+fail-closed）+ 端侧烟测草案（mix_read JSON 断言 + agent_last.log `[tim.assert]` WARN 行差分，cont_stall/d1_stall 模式）。
  - **内核腿缺口清单 8 项**（true peak/DC offset/块长/per-instance 装载态/Hygiene 回传/BS.1770/削波持久化/aux send 拓扑）已列表带锚点，供决策侧排卡。
  - **端测边界声明**：本卡零生产代码改动（纯勘察+设计文档），无需端测；实现卡（L2-1-TIM-1）的端侧烟测方案已写入设计 §4.2，届时按 AGENTS §5 真栈执行。
  - **停止条件未触发**：五个候选断言器数据可得性全部核证成立，未走"盘点上交"分支。
  - 领取时工作树既有改动（VitApp/Workspace 运行时状态×2、untracked×3）与本卡零冲突，原样保留未动。
- 验收：**pass（2026-09-27 决策会话）**——分支 d29b357 merge 合入 main；决策侧亲核：①事实更正验证（tim v0/tom v0_2 锚点亲查吻合）②五断言器数据可得性逐条采信（关键发现：L3 nan/inf 已在透传键表 mixboard.go:2893 而 TIM 未读=AS-SIG 零内核腿的根据；ImportInput.AudioSettings 预留位=AS-SR 现成输入）③红测试 T1-T9 构造真实可红（T8 三态诚实不产生假 pass/T9 旧记录兼容与未知 asserter fail-closed）④AS-PEAK 单边语义明示+sample_peak_only 常驻 Limitation 的诚实处理采信；实现卡 L2-1-TIM-1 随即发池。
