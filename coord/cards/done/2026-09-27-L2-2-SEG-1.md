# L2-2-SEG-1：段落发现层 v1 实现——SegmentationPrimitives 特征产出（onset 密度+能量 novelty，确定性断言）

- 优先级 / 预估 / 依赖：P1 / 0.6 天 / L2-2-SEG-RECON-1 已验收（`coord/runs/L2-2-SEG-RECON-1/SEGMENT_DSP_INVENTORY.md` 为落点权威输入：K1 帧序列内存现算/K4 包络瓦片/K7 双渲染确定性范式/K8 SegmentationPrimitives 槽位）；VitApp 域（内核 C++）
- 模型分级：L2 / GLM 亲自或强督导（内核扩展，ctest 双配置门）
- **执行侧（PC 会话）**
- 目标（v1 范围钉死——只做 primitives 产出，不做边界切分/段落命名/marker 写入）：
  1. **红先行**：SegmentationPrimitives 特征产出的 ctest 红测试（修前该特征类型无产出=红）：①同一段真实音频分析两次，primitives 逐字节一致（确定性重放——K7 范式）②primitives 内容含 onset 事件序列与能量 novelty 曲线且结构非空 ③无音频/空轨 → 特征状态 missing/not_evaluable 语义（不静默空）。
  2. **实现**：`AudioFeatureType::SegmentationPrimitives`（槽已注册 AudioFeatureTypes.h:18）挂入 L3 分析管线：onset 密度事件（复用 K2 瞬态/频段事件存量）+ 能量域 novelty（K4 波形包络短窗能量差分）——帧粒度与既有特征对齐；序列级发布走既有 feature snapshot 通道（K1 所述"序列级发布"缺口即本卡补的点）；参数限幅（K7 先例）。
  3. **SSM 不做**（勘察结论：零存量需补算，v2）；边界切分/阈值算法不做（发现层 primitives 就位即可，切分=下一卡）。
  4. **门**：ctest Debug+Release 双配置全绿（含既有 6 项零回退）+ 新增测试全绿；`go test ./...` agent 侧零回退（若 agent 消费面未动则只需确认编译面无涉）。
  5. 回执：红绿退出码+diff stat+HEAD+新特征的一帧样例（JSON 摘录）；端侧烟测由决策侧验收后另开腿卡（真栈 feature snapshot 落盘断言）。
- 约束：零既有特征行为改动（DOM/MOM/COM 等消费面不碰）；确定性是硬验收（重放不一致=红）；文件域 VitApp/Source+VitApp/Tests。
- 验收：红绿 T1-T3+双配置 ctest 绿+既有零回退
- 停止条件：实现中发现 K1/K4 锚点与现实不符（帧序列实际不可得/包络瓦片粒度不符）→ 停止，冲突实证上交（勘察修订而非硬凑）
- 领取：2026-09-27 PC 执行侧（GLM-5.3 / L2 亲自）。领取时 HEAD=`68ce2fef`（origin/main 已同步）；`git status --short`：`M VitApp/Workspace/default_project.xml`（运行时工程状态，本卡不触碰）+ 若干 untracked `coord/runs/{FIX-PCA-*,PORT-WIN-HYGIENE-SMOKE-1}*` 工件目录（领取前已有，未触碰）。
- 回执（2026-09-27，PC 执行侧）：
  - **锚点核对（停止条件前置检查）**：全部亲验命中——K8 `AudioFeatureTypes.h:18` 枚举/`.cpp:18,46-47` 字符串+别名/`.cpp:85` `segmentation_primitives.v1` 版本串，全仓库零生产者零消费者（纯预留槽确认）；K1 `L3AcousticAnalyzer.cpp` FrameObservation 37-44 行/fftOrder=12 非重叠 4096 点 Hann（~92.88ms@44.1k）/串行单线程分析池（动机注释即重放确定性）/`analysis.frames` 帧序列仅在内存；K2 瞬态判据（RMS 帧 +3dB 局部峰）与 128 事件上限（248/254/263 三处）；K4 `WaveformEnvelopeBaker.cpp:31-33`（5s tile/1024 帧/~4.88ms/6 浮点步长）；K7 `VitProductionCoordinator.cpp` determinism_*_delta 系列。K1/K4 与现实相符，**停止条件未触发**。
  - **产物**（文件域 VitApp/Source+VitApp/Tests，零既有特征行为改动）：
    - `VitApp/Source/Service/L3AcousticAnalyzer.cpp`（+200/-2）：①`SegmentationPrimitives` 派生结构+三个固定常量参数（`kSegOnsetRiseDb=3.0` 与 K2 瞬态判据一致、`kSegOnsetPublishLimit=1024` 事件披露上限、`kSegDensityWindowSeconds=1.0` 滑动密度窗）——全部编译期常量并在发布载荷透出（K5/K7 限幅参数范式：无可变输入面+全参数透出为可比性键）。②`L3Analysis` 增 `frameRmsLinear`（帧线性 RMS 并行序列，novelty 源——dB 域 -160 clamp 会抹掉亚底噪差分）+`segmentation` 槽。③`deriveSegmentationPrimitives`：onset 全量无界扫描（判据镜像 K2 瞬态检测器：相对前帧 rise≥3dB 且局部峰≥次帧，独立容器独立上限——既有 `transientEvents` 的 128 上限与其消费面零触碰，勘察 G3 截断缺口以"密度直出不受披露截断影响"就地解决）+能量域 novelty（线性 RMS 半波整分差分，K4 波形包络短窗能量差分方法学落 L3 帧粒度）。④`publishSegmentationPrimitives`：`dad_l3_segmentation_primitives.v1` 载荷（stampCommon 现成身份戳链）——`onset_events`（detected_count/published_count/truncated 诚实披露）+`onset_density`（每帧指示数组+前缀和 O(1) 滑动窗，**逐帧一entry与既有 L3 特征同网格**）+`energy_novelty`（逐帧曲线+max/mean 汇总）；degenerate 源（reader 失败/空范围）**照常发布**，三个子结构显式 `not_evaluable`+空数组（不静默空）。⑤帧循环内 RMS 中间值提取（语义与原表达式逐位一致，仅消除重复求值）。
    - `VitApp/Source/Service/AudioFeatureService.cpp`（+2/-1）：`requestBake` L3 分发条件加 `SegmentationPrimitives`（原落入 `publishDeferredStatus` 的 not_yet_promoted 路径就此接通）。
    - `VitApp/Tests/SegmentationPrimitivesTests.cpp`（新，329 行）+`VitApp/Tests/CMakeLists.txt`（+26）：新 ctest 目标 `VitSegmentationPrimitivesTests`（JUCE-only 链接，照 PluginListHygieneTests 模式）。
  - **红绿（T1-T3）**：
    - 红（实现前，仅测试文件在）：`ctest -C Debug -R VitSegmentationPrimitivesTests --output-on-failure` → **Failed**（"0% tests passed, 1 tests failed out of 1"；测试输出 5 checks FAILED：T1 首跑/T1 二跑/T2 解析/T3×2 全部"no producer timeout"——失败原因正是卡面定义的"该特征类型无产出"；5 次分析各 30s 超时共 120.10s）。
    - 绿（实现后）：同命令 → **Passed 0.15s，CTEST_EXIT=0**；直接跑 exe 41 项 check 全 PASS。调试插曲：首版绿跑 SEGFAULT——根因在测试端 `payloadObject` 返回局部 `juce::var` 内 DynamicObject 的裸指针（var 析构引用计数归零→悬空）；实现路径经 stderr 诊断确认无恙后修复测试所有权（var 持有在调用方作用域），非实现缺陷。
    - T1 确定性口径：同源同请求两次分析，两份载荷 parse 后仅移除 `updated_at`（墙钟戳）再规范化序列化，**全字段含全部数组元素逐一相等**（实测两次 JSON 长度均 11995 字节）；同进程串行池+单线程顺序累加，无浮点归约顺序歧义（K1 范式）。
  - **门**：`ctest -C Debug` 全量 → **7/7 Passed，exit 0**（既有 6 项零回退+新测试）；`ctest -C Release` 全量 → **7/7 Passed，exit 0**；内核主工程增量编译 Release+Debug 双配置零错误（AudioFeatureService.cpp 路由改动经主 target 编译验证）；agent 侧 `cd agent && go build ./...` → **exit 0**（本卡零 Go 代码改动、agent 消费面未动，按卡面只需确认编译面无涉）。
  - **diff stat（vs 领取时）**：`AudioFeatureService.cpp | 3 +-`；`L3AcousticAnalyzer.cpp | 200 +++++…`；`Tests/CMakeLists.txt | 26 +`；新文件 `SegmentationPrimitivesTests.cpp` 329 行（合计 227 insertions / 2 deletions + 新测试文件）。工作树其余改动（`default_project.xml` 运行时状态+coord/runs 工件）领取前已有，未触碰、不入 commit。
  - **新特征一帧样例（绿跑实测摘录）**：onset 事件 `{"onset_seconds": 0.092879818594104, "frame_index": 1, "onset_dbfs": -14.3294380946773, "rise_db": 14.689194042645791}`；密度帧 `{"start_seconds": 1.393197278911565, "end_seconds": 1.486077097505669, "onset_count": 3, "onsets_per_second": 3.0}`；novelty 帧 `{"start_seconds": 1.486077097505669, "end_seconds": 1.578956916099773, "novelty": 0.044165052961411}`（正值帧恰为 1.5s 能量阶跃帧，测试信号设计意图命中）。
  - **范围钉死确认**：SSM 零计算（未触碰谱瓦片/未新增任何矩阵计算）；边界切分/阈值算法零实现（载荷显式声明 `primitives_scope: "onset_density+energy_novelty_v1_no_boundary_picking"`）；marker 写入零触碰（ProjectMarkerService 未动）。
  - **端测边界声明（AGENTS §5）**：本卡验证到 ctest 单元/组件级（真实 WAV 编解码+真实 L3 分析管线+真实发布回调）；`AudioFeatureService` 路由接线经内核主 target 双配置编译验证但**未跑真实栈端侧烟测**（真栈 feature snapshot 落盘断言按卡面由决策侧验收后另开腿卡）。
  - **提交**：卡回执直推 main；代码（VitApp/Source+VitApp/Tests）走 `port/l2-2-seg-1-segmentation-primitives` 分支推 origin，待决策侧验收后合入。
