# FIX-PCA-CERTAUTH-TOKEN-1：static_eq 认证授权入口补齐——解锁 104 例（PC 57+mac 47）load gate 循环依赖

- 优先级 / 预估 / 依赖：P2（升格：双端同机制阻断）/ 0.3 天 / FIX-PCA-EQCHANNEL-1 验收上交裁定（2026-09-24）；PORT-PCA-AUTOSWEEP-MAC-1 验收上交并入（2026-09-24）；sweep 幂等续跑就绪（retry 语义），解锁即自动吸收
- 模型分级：L1-L2 / GLM-5.3（授权面改动，红测试先行；~40-60 行）
- **背景（双端取证同机制）**：harness 认证授权白名单已含 static_eq（harness.go:1956）但**无入口可铸授权**——runner 入口 processor_certification_entry.go:419 拒 static_eq；full-access 路要求先晋升=循环依赖；HTTP invoke 强制 source=http（PC 锚 server.go:1463 / mac 取证补缺省 source 锚 server.go:1456）。**PC 57 例 + mac 47 例** static_eq 主体卡在 load_gate_blocked（mac 侧执行侧自举不动点核验：47 例零主体任一族有晋升，幂等重跑无可吸收——入口不修，重跑无用）。
- 目标：
  1. token-only 认证授权入口（本地 token 文件/环境变量触发，绕开 HTTP source 强制与 full-access 先晋升要求；授权仅限认证面不扩运行时权限——fail-closed 边界与既有授权纪律一致）
  2. 红测试：入口铸 static_eq 授权→runner 可跑 EQ 认证（修前红：入口拒/循环依赖；修后绿）；非 token 路行为不变回归
  3. 附带（同卡注记级）：v1 import 收据种类扩容评估（FabFilter/TDR vendor_kind_missing 3 例——如需改收据种类枚举=最小独立 commit 列明）
  4. 解锁验证：`--phase certify --families static_eq` 幂等续跑吸收 57 例（能认证者晋升、边界拒绝者记因——例外桶不要求清零，要求全处置）
- 文件域：`agent/internal/chat/processor_certification_entry.go` + `agent/internal/harness/harness.go`（如需）+ 测试；**分支开发走决策侧合入**（agent 代码卡纪律）
- 验收：①红测试修前红/修后绿；②agent 全量+webui 绿；③57 例续跑全处置（认证数+记因数=57）；④非授权路径行为零变化回归
- 停止条件：授权入口设计需动 full-access 语义或运行时权限面 → 上交裁定（不擅自扩权）
- 领取：2026-09-24 PC 执行会话 A（GLM-5.3，分支 port/fix-certauth-token）。领取快照：HEAD=6774b9fb；status=VitApp/Workspace/default_project.xml 修改+coord/runs 历史工件 untracked（均卡域外）；diff --stat 仅上述 1 文件。含搭车小卡 FIX-TEST-CTXSYMLINK-1（contextruntime/context_test.go 单文件）。
- 回执：已完成（PC 执行会话 A，分支 port/fix-certauth-token，开发 HEAD=6774b9fb）。**设计落点说明**：入口未落在 :419 的 generic runner 放行（generic runner 无 static_eq 工具对——processorCertificationToolPair 为空，放行也产不出收据；static_eq 认证通道=EQ-1 外部确定性通道，其卡点在 load gate），而是 **harness.go enforceAgentProcessorLoadGate 的 full-access 分支内 token 铸币回落**（harness.go:1937-1947）：authorizeFullProjectAccessLoad 以 sentinel errFullProjectAccessUnpromoted（%w 包装，错误文本逐字节不变）区分"未晋升"失败，仅此一类失败回落 authorizeCertificationTokenLoad；武装判定=env `VIT_PCA_CERTAUTH_TOKEN` 或 `~/.vit/pca_certauth.token` 非空（请求内容不可武装）；铸币身份全服务端解析（semantic index 唯一 identifier 匹配+路径绑定+指纹钉死+trackID 必填），产出 Certification=true+family=static_eq 授权（gate 白名单 harness.go:1959 原样消费）；歧义/store 故障/铸币失败均返回原错误（非 token 路零变化）。净改动 harness.go +89 行（含注释）/新测试 processor_load_gate_certauth_token_test.go 2 函数（HOME/USERPROFILE 隔离防真实 token 文件泄漏）。**验收证据**（工件根 coord/runs/FIX-PCA-CERTAUTH-TOKEN-1/20260924_pc/）：①红测试修前红=red_test_before_fix.txt（未修复 HEAD worktree 重放，armed 断言败于 no_promoted_current_pca_admission_for_identifier）/修后绿=green_test_after_fix.txt；②agent 全量 go test ./... exit 0+webui 324/324 绿；③真实栈烟测 dev_agent_smoke.ps1 -StartKernel -StartUI **exit 0**（构建含改动+三件套+全链路断言，console=smoke_console.log）；④sweep 续跑 `python scripts/pca_autosweep.py --phase certify --families static_eq` **exit 0**（console=certify_static_eq_console.log，run 20260924_005622）：targets=61（57 blocked+1 failed+3 vendor 重试），终态 **52 completed / 12 failed（记因 unplanned_parameter_change 等 EQ 纪律边界拒绝）/ 3 vendor_kind_missing（记因）/ 1 load_gate_blocked（记因上移为"resolves to 2 promoted identities"——TBTECH Cenozoix 已被 de_esser v2+broadband v1 双线晋升的歧义 fail-closed，非循环依赖，既有歧义纪律设计上未绕开，数据面上交决策侧知悉）**；57 例原 blocked 中 56 例转终态，v1 store static_eq promoted 6→60（能认证者晋升确证）；⑤非授权路径零变化=TestCertificationTokenDoesNotWeakenOtherLoadGatePaths（伪造 source 拒/manual 路不变/token 非授权授予拒/未知 identifier 保持原错误）+既有 gate 三测试全绿。**注记级第 3 项评估**：FabFilter/TDR 3 例 vendor_kind_missing 扩容需改 processorauthority ImportReceipts 收据种类枚举+schema（超出本卡授权面 ~40-60 行边界），列明为独立最小 commit 卡候选，本卡不扩。mac 47 例同机制解锁待 mac 侧同款续跑（token 文件武装+--phase certify --families static_eq）。
- 验收：**pass（2026-09-24 PC 决策会话）**——①落点偏移审查通过：入口落 harness.go enforceAgentProcessorLoadGate 的 full-access 分支 token 回落（非卡面锚 processor_certification_entry.go），理由成立（generic runner 无 static_eq 工具对，真卡点在 load gate）；设计冻结全守=sentinel errFullProjectAccessUnpromoted 唯一回落类（基础设施故障/身份歧义保持关闭）/token 武装仅操作员（env+本地文件，请求不可武装）/铸币身份服务端钉死/Certification=true 不涉晋升与运行时授权。②红绿+非授权路零变化测试采信。③决策侧复跑：合并树 agent 全量 84 包 ok/0 FAIL+真栈烟测 exit 0（一次覆盖本卡+CTXSYMLINK+KERNEL 三改动）。④增量吸收采信：57 blocked→56 终态（52 认证+记因），store static_eq promoted 6→60；TBTECH Cenozoix 双线晋升歧义保持 fail-closed 纪律知悉（数据面非缺陷）。**⑤决策侧 derive apply-live 闭环实证：EQ 白名单 6→48、全库 100 条目、overlay verify 103 检 0 败（双备份在案）**。vendor_kind_missing 扩容=独立小卡候选登记不阻塞。mac 47 例吸收=用户转交（token 武装+`--phase certify --families static_eq` 幂等）。实现经 cherry-pick 入 main=90766b71。
