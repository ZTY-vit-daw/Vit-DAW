# L2-1-TIM-1：TIM 结构断言器 v1 实现（按设计文档 T1-T9 红绿+agent 全量零回退）

- 优先级 / 预估 / 依赖：P0 / 0.6 天 / L2-1-TIM-DESIGN-1 已验收合入（设计文档 `docs/TIM_ASSERTER_V1_DESIGN.md` 为唯一权威规格）；mac 夜间/9-28 上午窗口
- 模型分级：L2 / GLM 亲自或强督导（TIM 是现行生产投影，§11 兼容硬约束）
- **执行侧（mac 会话；agent 侧纯 Go 实现，mac/PC 均可跑测试——按窗口就哪台开哪台）**
- 目标：按设计文档 §2（五断言器）/§3（实现锚点文件域）实现，§4.1 红绿 T1-T9 全绿：
  1. **红先行**：T1-T9 表驱动测试落 `agent/internal/tim/`（修前无 assertions 块=红）；
  2. **实现**：Projection 增 `Assertions` 结构（三态 pass/fail/not_evaluable+Limitations 码，按设计 §3 schema）；五断言器（AS-SIG P1P2/AS-PEAK/AS-SR/AS-ROUTE P1P2/AS-PLUGIN）在 tim.Build 内判定；agent 侧三腿（tim 读 nan/inf 透传键/timInputFromObservation 带 audio_settings+路由摘要/plugin 表走 pluginsemantics 索引）；
  3. **§11 兼容**：旧 observation JSON（无 assertions 字段）Unmarshal 不炸、Assertions==nil、未知 asserter 名 fail-closed（T9）；现有 10 个 TIM 测试+消费方（CCB project.structure 裁剪子集/ContextPack/TOM digest）零回退；
  4. **全量**：`go test ./... -count=1` exit 0（84 包 0 FAIL 基线）+`go vet ./internal/tim`+gofmt；
  5. 回执：红绿退出码+diff stat+HEAD；端侧烟测（设计 §4.2 监督报警路径）**不在本卡**——实现绿后由决策侧单独开烟测腿卡（真栈三件套，按窗口排）。
- 约束：零内核改动（设计 v1 裁定）；裁判不下场（断言器无写路径）；LLMContext 的 assertions 披露面按设计 §3（CCB 裁剪子集是否扩 assertions 字段以设计文档为准，不擅自扩披露面）。
- 验收：T1-T9 全绿+旧测试零回退+agent 全量 exit 0+§11 兼容测试在案
- 停止条件：实现中发现设计规格与现实冲突（如透传键实际缺失）→ 停止，冲突实证上交决策侧（设计卡修订而非执行侧改设计）
- 领取：2026-09-26 21:02 +0800 / origin/main=68ce2fef92241eb4b2ec8df6af92295dd7071650 / 分支 port/l2-1-tim-1-asserter-v1（领取时工作树 HEAD=68ce2fef 与 origin/main 一致，仅运行时状态/工件未跟踪项，无实现 diff）
- 回执：
  - **实现 commit**：`113d573120cd2e043c3ab3ca51b467a6650a9ecd`（分支 `port/l2-1-tim-1-asserter-v1`，已推 origin）
  - **红→绿**：红 phase `go test ./internal/tim/ -count=1` **exit=1**（10 个新测试全红：Build 无 assertions 块，T1-T9+LLMContext 计数）；实现后同命令 **exit=0**。红相留档：断言器空壳+类型先行，行为红（`assertion … not found in []tim.AssertionResult(nil)` ×10）
  - **全量**：`go test ./... -count=1` **exit=0，84 包 0 FAIL**（与基线一致）；`go vet ./internal/tim ./internal/mixboard` 通过；gofmt 本卡 7 文件干净（`internal/mixboard/waveform_freshness_residual_test.go` 的格式问题是领取前已提交状态，非本卡 diff，未越域整理）
  - **diff stat**：7 files changed, **+1282 / -10**（新 asserter.go 485 行 + asserter_test.go 451 行 + tim_asserter_legs_test.go 142 行；projection.go +126/-10、types.go +19/-10、mixboard.go +68、import.go +1）
  - **落点核对（设计 §3.1）**：Projection.Assertions `json:"assertions,omitempty"`；Evaluate 纯函数挂 Build 内、在 buildRiskSummary 之前（fail 计入 warning 计数与 primary_codes，T1 断言）；issue 生成对 possible_clipping_or_no_headroom 按 track+code 去重（不双码，T2 断言恰好 1 条）；Limitations 收 NE 码 + 常驻 assert_level_ceiling_sample_peak_only；LLMContext technical_risk fact 增 assertion_counts；ContextPack ContextProjection 增 assertion_summary（counts+fail_codes≤8）；BuildFromImportRows 接通 ImportInput.AudioSettings（设计点名的预留输入位）
  - **agent 三腿**：① nan/inf——timInputFromObservation 从 `obs.GlobalSummary.feature_snapshot.track_waveform_envelopes`（透传键表 mixboard.go:2893 同源行）提取 per-track nan/inf，tim 侧 acoustic 行优先、evidence map 回退（band/stereo 意图下快照不含 track envelopes → 诚实 NE）；② audio_settings+rack——req.ProjectState（内核 get_project_state 顶层 audio_settings、每轨 rack，均亲验 CommandDispatcher.cpp:3377-3382/1793-1897）→ timAudioSettings + tim.RackSummariesFromProjectState；③ 插件表——pluginsemantics.Load 索引转 map[pluginPath]bool，缺文件→nil→NE（不假 fail）
  - **§11 兼容**：旧投影 JSON（无 assertions 字段）Unmarshal 不炸、Assertions==nil、往返一致；未知 asserter/check 经 ReconcileLoadedAssertions fail-closed（limitation `assert_unknown_entity_skipped`）；现有 issue 码零改动；CCB project.structure 8 字段裁剪未动（selectFields 显式清单不含 assertions，零泄漏）
  - **零回退**：现有 tim 包 7 测试 + mixboard 2 个 TIMInput 测试 + 消费方（tom/CCB/contextruntime/agentloop/harness 全在 84 包内）全绿
  - **新增测试**：tim 10（T1-T9 + LLMContext assertion_counts）+ mixboard 2（三腿端到端：热数据 fail 到达投影；缺索引 NE 不假 fail）
  - **端测边界声明**：① 本卡无真栈烟测（卡定义：烟测腿由决策侧另开卡）；② 设计 §1.4「logx.Warn」实为 Logger 方法非包级函数——断言 WARN 行走 `tim.AssertWarnLogger` 注入钩子（已实现 cap 20/asserter，默认 nil 静默），**接线到 harness logx 属域外未做**，烟测腿需一并接线；③ 设计 §3.3 的 message_loop TIMLines 用户面汇总行与 catalog.go digest 断言计数块在卡文件域（设计 §6）之外未动——fail 信号已随 issues→risk_summary.by_severity/primary_codes 进入现有披露面（含 digest，catalog.go:151 引用同一 risk_summary）；④ CCB 披露面按设计未扩
  - **停止条件未触发**：设计锚点全部核实成立（透传键/audio_settings/rack/pluginsemantics 索引 API 均在位）；唯一规格-现实出入即上述 logx 形态（§1.4），以注入钩子适配，未构成需停止的冲突
