# PORT-REALSTEMS-MAC-1：真实 stems 到 mac——旅程烟测真实素材验证 + 插件族动机面观察

- 优先级 / 预估 / 依赖：P1（mac 演示前置链）/ 0.5 天 / 用户裁定 2026-09-23（mac 一直没有真实测试 stems，旅程烟测跑在合成替身上）；反哺 PORT-WL-EQ-1 优先级判定
- 模型分级：L1 / GLM-5.3 flash 可接（驱动+记账，断言零改动）
- **执行侧（mac 侧卡）**：素材由**用户从 PC 本机转交**（机器本地目录，**绝不入仓**——§10 纪律，仓库脚本 --stems-dir 语义即为此设计）；回执写回 D:\Vit_DAW coord
- **背景（已核实+2026-09-23 三次核实修正）**：④⑤ 夹具=仓内合成音（test_100hz_10s.wav 100Hz 正弦+test_target_3s.wav 3s 目标音，双端同源）——**统计数字口径=工作流机制证明，不含内容质量证据**。**旅程/展示层素材真相（修正）**：912.vit 引用的六轨 20s"问题 stems"= **spv1 受控问题素材**（`D:\Vit_DAW\temp\semantic-processor-agent-project-smoke-v1\fixtures\semantic_processor_project_smoke_v1_80085263a651cf20\`，3 案例 spv1_p01/p02/p06 ×6 轨×20.0s/44.1kHz）——由仓内脚本确定性生成（Goal5 干净受控素材 `scripts/semantic_mix_goal5_fixtures.py` → 配方化故障注入 `scripts/semantic_processor_open_fixtures.py`，种子化 RNG、波峰因数 14.5-21dB、可检测性门全过、盲密封真值）——**具备工程条件（有注入问题可观察可修复）**，与 ④⑤ 两个纯音不是一个层次；mac 旅程跑在自造替身上=从未用过这套真素材。商业歌曲 stems（茉莉花/Jade Bridge）与自动链无关，不入传交清单。
- **传交清单（用户打包，2026-09-23 敲定）**：①`fixtures\...\cases\`（61MB，3 案例 spv1_p01/p02/p06 ×6 轨=18 个问题 stems）②`912.vit`（12KB，`D:\Godot\project\vit-daw-frontend\`）——合计 ~61MB；③可选 `sealed\`（+122MB，仅 mac 要跑盲评测时）；**`projects\`（875MB PC 构建 .vit 副本）不需要**。**双文件夹说明（防困惑）**：fixtures 下两套集合（80085=08-30 原始构建/02be=09-01 重建）同名案例音频逐字节相同（构建器确定性，p01 bass sha256 两边一致 `02039e43…`）——传 **80085 套**（912.vit 引用它）；三个案例目录=三个盲测问题场景（各 6 轨，问题配方密封），p01=旅程演示场景，p02/p06=盲测备用。备选零传输方案：mac 用仓内两脚本重建（配方等价，但跨平台浮点舍入不保证逐字节同 sha256，盲评测哈希核对会失败——演示用途传输仍是最稳路径）。**路径重映射注记**：912.vit 引用 Windows 绝对路径，mac 侧 provisioning/--stems-dir 摆位适配由本卡执行面处理（踩不动走停止条件上交）。
- 目标：
  1. 用户打包传交上述清单到 mac（机器本地目录，不入仓），目录路径+文件 sha256 清单记回执；**文件名映射**：mac journey `--stems-dir` 约定 `<stem>.wav`——spv1 案例目录已是 bass.wav/drums.wav/guitar.wav/other.wav/piano.wav/vocals.wav 命名，直接可用（此前"茉莉花数字前缀改名映射"条目作废）
  2. `journey1_demo_journey_smoke_mac.sh --stems-dir <真实目录>` 跑通完整旅程（工程打开→权限→实验→装载→A/B）——**mac 首次真实素材端到端**
  3. **插件族动机面观察（反哺 EQ-1）**：记录真实素材旅程中模型提案/观察实际触及的插件族（EQ/broadband comp 是否被动机化、观察层是否出现频段占用/masking 事实）——PORT-WL-EQ-1 的优先级以此为准：若真实素材确动机化 EQ/comp → EQ-1 升 P1（演示剧本含插件场景时必须）；若仍无 → 维持 P2
  4. 顺带按 demo-capture SOP 采集 1-2 个真实素材成功轮的演示素材（若时机合适）
- 约束：stems 零入仓（.gitignore 无需动——放仓库外）；LLM key 零入工件；断言零改动（旅程断言即被测契约）；④⑤ 合成夹具**不换**（统计连续性——合成夹具是机制统计的特性不是缺陷，真实素材证据层由旅程承载）
- 验收：①真实素材旅程整轮 exit 0 工件；②插件族动机面观察表（哪族被动机化+观察事实指针）；③素材转交记录（来源/文件清单/哈希，不含文件本体）；④回执记两端 HEAD
- 停止条件：真实素材旅程断言红且红点在素材相关断言 → 如实记录红点与形态上交（素材与断言契约的适配是决策问题）
- 领取：2026-09-23 12:31 +0800 / origin/main=ead078d636d6ef484078fcae6a65ef06ea5e19b6（与决策侧所见一致）/ 分支=main（本卡预期零代码改动，无 port/* 分支；仅 coord 卡片状态变更直推 main）
  - 领取时工作树：HEAD=ead078d，`git status --short` 仅 `M VitApp/Workspace/Settings/Settings.xml` + `M VitApp/Workspace/default_project.xml`（前旅程运行时残留，不属本卡 diff，不回退不提交）；`git diff --stat`=2 files changed, 14 insertions(+), 142 deletions(-)（全部落在该两文件）。pull 注记：`pull --rebase` 因该两文件未暂存被拒，核验入站 8 提交（5678a79→ead078d）未触及此两文件后改用 `git merge --ff-only origin/main` 等价达成（纯 fast-forward，未 stash 未回退）
- 回执：（执行侧 2026-09-23 12:52 +0800，commit 见验收前续）
  - **① 真实素材旅程整轮 exit 0**：run 目录 `~/Documents/vit-realstems-artifacts/run1_20260923_1232/`，报告 `journey1_report.json`——verdict=**all_green**，9/9 断言绿（a1-a4+s1-s5，red=0/not_observable=0），`assertions_summary.txt` 同。provisioning 取证：**6×"copied"**（真实素材复制，stems_provisioning.txt），零 "wrote"。exit 0 证据链：脚本 verdict→EXIT_CODE 映射（journey1_demo_journey_smoke_mac.sh:459 `all_green) EXIT_CODE=0`）+ 控制台末行 `JOURNEY1_MAC_VERDICT all_green`（journey1_console.log）；注记：nohup 后台启动未捕获 $?，如上双证推断 exit 0。全程 ~8.5min（12:32:5x→12:41:23）。
  - **② 插件族动机面观察表**：`~/Documents/vit-realstems-artifacts/run1_20260923_1232/plugin_family_motivation_table.md`。要点：**static_eq(EQ)=动机化成立且被 capability_blocked 实证**——模型假设『250Hz 附近 -1.5dB 衰减降低低音→低中频过渡能量堆积』（锚定实测带台阶 bass 56.5dB→low_mid 42.5dB），turn.failed=`static_eq experiment is not configured: experiment plugin whitelist: static_eq plugin is not configured`（task_state=capability_blocked）；broadband comp=未被模型动机化（S4 的 C1 comp=脚本确定性探针）；观察层**频段占用出现**（timbre_frequency 六带全就绪+MOM conflict_candidate_count=8+band_occupancy 证据引用），**masking 未出现**（显式限制 masking_analysis_not_ready_on_current_project_cut），MOM bundle=partial（missing_track_count=1）。与合成基线对照：合成轮同样 EQ-blocked 但假设=55Hz 字面基频；真实素材增量=实测带结构+多轨关系支撑。**裁定输入：按卡面规则支持 EQ-1 升 P1（模型想做的实验做不了=演示剧本硬缺口）；masking 不 ready 为独立缺口。**指针：phase_a/actions.json（5 动作）、events.json seq10-18、~/.vit/free_state_experiment_plugins.json（v5 五族已配，static_eq/broadband 留空）。
  - **③ 素材转交记录**：来源=PC 用户人工转交（`~/Desktop/cases/`，机器本地不入仓）。18 文件 sha256 清单=`~/Documents/vit-realstems-artifacts/run1_20260923_1232/stems_sha256_manifest.txt`；规格=全 2ch/44.1kHz/Int16/20.000s/3,528,044B（afinfo 抽核 p01/bass+p06/vocals）；p01/bass sha256=`02039e43…b34f` 与 80085 套基准逐字节吻合 ✓；注记=p01/p02=80085 套(08-30)、p06=02be 套(09-01 重建，决策侧裁定逐字节同)。912.vit=`~/Documents/vit-daw-frontend/912.vit` sha256=`f6ca36fa…4d8bd`（run_meta 前后指纹同值，只读未动）。不含文件本体（run workdir 内副本为机器本地工件）。
  - **④ 两端 HEAD**：领取时 origin/main=**ead078d**（与决策侧一致）；领取提交=9050785（coord-only 已推）；完成时本地 HEAD=9050785（回执提交前）；决策侧派卡时 origin/main=ead078d。
  - **可选目标 4（demo-capture SOP）**：未定位 SOP——coord/docs 目录不存在，coord+docs+scripts 全文无 demo-capture/演示素材采集条目 → 按卡面跳过（不阻塞）；本轮全量工件（报告/控制台/动作/轨迹/音桥回复）已留档可作后续采集原料。
  - **约束核查**：断言零改动 ✓（journey 脚本零 diff，git status 仅两残留文件）；stems 零入仓 ✓（仅 ~/Desktop/cases+run workdir）；LLM key 零入工件 ✓（`sk-` 模式全工件 0 匹配，run_meta 仅 has_api_key 布尔）；④⑤ 合成夹具未动 ✓；隔离契约 ✓（912.vit/.vit_history 只读指纹同值，内核假根运行）。
  - **运行纪律执行**：N=1 主跑（run_discipline.txt 跑前落盘）；成功条件达成（exit 0+9 断言全绿）；无失败轮次（no_candidate_found/capability_blocked/断言红/崩溃/环境中断分类面：本轮唯一 capability_blocked=模型轨迹内 static_eq 白名单缺口，即目标②的裁定输入本体，非旅程断言失败）；已知环境项（LLM 60s 超时/CoreAudio 卡顿）本轮未出现。
  - **交接注记（决策侧裁量）**：旅程断言面全绿与模型轨迹 capability_blocked 并存是本轮最重要的结构事实——S3/S4 断言语义不依赖模型完成 EQ 实验（S4=确定性探针），故 all_green ≠ 模型实验路径通。EQ-1 若升 P1 并配置 static_eq，真实素材轮的模型路径才可能走完『假设→实验→装载→A/B』全链。
- 验收：**pass（2026-09-23 Mac 决策会话，[rulings/2026-09-23-PORT-REALSTEMS-MAC-1-pass.md](../../rulings/2026-09-23-PORT-REALSTEMS-MAC-1-pass.md)）**——决策侧独立复算非采信回执：6×copied+字面路径音频与桌面 stems 6/6 逐字节一致+9/9 断言全绿+verdict→exit 0 映射实核；ff-only 改道理由复核成立；观察表引用文本与 events.json 逐字吻合。三项裁定落地：EQ-1 升 P1（卡面已修订+旅程回归验收）；masking 独立缺口登记；开源就绪 backlog 两件登记（decisions/2026-09-23）。流程备忘：领取提交应一次含完整 mv（本次 todo 删除悬到回执提交才闭合，终态正确）
