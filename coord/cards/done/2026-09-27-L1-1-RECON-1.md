# L1-1-RECON-1：现存 evidence refs 格式全库盘点（G1 评审核心输入）

- 优先级 / 预估 / 依赖：P0 / 0.5 天 / 无（AGENTIC-OBSERVATION 路线图 L1-1 前置勘察；AGENTIC-OBSERVATION 蓝图 2026-09-25 已入库）
- 模型分级：L1 / flash 可接（纯只读勘察+报告，零代码改动）
- **执行侧（PC 会话）**
- 背景：路线图 D1 统一 ref schema 是关键路径第一块（G1 质量门=决策侧亲自评审）；"现存 evidence refs 由各环节自行生成、格式不一（近期修 bug 已撞上此痛点），需逐投影层归一"。本卡把"格式不一"变成精确事实清单。
- 目标：
  1. **全库盘点 evidence ref / evidence id / 引用句柄的生成点**：覆盖观察投影九包（dom/mom/tim/tom/fxm/com/epm/rlm/acousticpackage）+ DAD 证据层（audioclosure/frequencycleanup）+ CCB（contextruntime view 披露）+ mixboard/journal/trajectory/pendingmanager 等消费方。每个生成点记录：文件:行、字段结构（真实样例，从代码或测试 fixture 摘录）、生成时机、唯一性来源（什么保证它不撞）、不变性（内容变 ref 变吗）、可重拉性（拿 ref 能否取回内容）。
  2. **跨层不一致矩阵**：同类语义不同格式的对照（如时间窗表达、轨道引用、内容哈希有无）、命名差异、缺 D1 五元组（projection_kind/scope_id/time_window/snapshot_id/content_hash）中哪些字段的位置。
  3. **痛点实证回溯**：近期因格式不一撞坑的实例（F5 snapshot freshness、snapshot id 相关修复卡的 diff 里找引用）各摘 1-2 个代码锚点。
  4. 报告落 `coord/runs/L1-1-RECON-1/EVIDENCE_REFS_INVENTORY.md`（矩阵+生成点清单+不一致清单+痛点锚点）。
- 约束：**零代码改动**（只读勘察）；样例必须真实摘录（带文件:行），不得凭空构造；报告不改任何生产文件。
- 验收：①矩阵覆盖上述全部包，生成点 ≥15 处逐一带样例 ②不一致清单 ≥5 条（每条两端锚点） ③痛点实证 ≥2 例 ④报告入库 coord/runs（回执记录路径）
- 停止条件：某包结构复杂到单卡盘点不完 → 已完成部分成稿+剩余包列明，上交拆卡
- 领取：2026-09-27 PC 执行侧（GLM-5.3 / L1）。领取时 HEAD=911e3312（=origin/main）；`git status --short`：` M VitApp/Workspace/default_project.xml`（运行时工程状态，不动）+ 12 个 untracked `coord/runs/{FIX-PCA-AUTOSWEEP-1,FIX-PCA-EQCHANNEL-1,PORT-WIN-HYGIENE-SMOKE-1}*` 工件目录（领取前已有，未触碰）。零代码改动承诺兑现：本卡唯一新增产物为报告文件。
- 回执（2026-09-27，PC 执行侧）：
  - 报告路径：`coord/runs/L1-1-RECON-1/EVIDENCE_REFS_INVENTORY.md`
  - 验收对照：①✅ 覆盖九投影包（dom/mom/tim/tom/fxm/com/epm/rlm/acousticpackage——acousticpackage 无 EvidenceRefs 字段，以 SourceHash/Fingerprint 身份面入矩阵）+DAD（audioclosure/frequencycleanup）+CCB（contextruntime/capabilitycontext view 键）+消费方（mixboard/journal/trajectory/pendingmanager/agentprotocol）；生成点 **31 处**（超 ≥15 线），分 8 类（A 投影 ID / B 观察身份 ID / C URI ref 字面量 / D 内核 C++ / E 执行回执 / F CCB view 键 / G 快照 request_id 族），每类带真实代码摘录。②✅ 不一致 **7 组**（超 ≥5 线）：投影身份方案三态+无 ID、时间窗四态、轨道引用三位置、实例段有无、分隔符三方案、snapshot 身份四载体（`snapshot_id` 字段名全库非测试代码零命中）、内容哈希有无。③✅ 痛点 **4 组**（超 ≥2 线）：F5 双 request_id 族分叉（含 harness.go:7337 前缀判族锚点）、pluginPrepWorkerNormalizeObservationID 五形态防御解析、frequencycleanup `.`/`_` 别名归一器、normalizeObservationRef+rel_ 前缀手术派生。④✅ 报告入库。
  - 核心事实：全库 refs 落在 `EvidenceRefs []string` 自由字符串（agentprotocol/types.go:131 等），格式约定全部隐含在生成点拼接里；D1 五元组无一字段有全库统一名；结构化程度最高的键是 audioclosure.ObservationKey（types.go:133）但 TimeWindow 为自由 string 且未外溢。
  - 边界上交（报告 §6）：Godot 前端仓第三写者（telemetry_manager.gd 的 request_id 族）未跨仓取证；PCA 层（processorattestation/pluginsemantics）未逐文件展开，预计 ≤0.5h 补查——是否拆补查卡由决策侧裁定。
  - 验证方式：全部锚点经 grep/read 逐条核对，行号以 HEAD 911e3312 为准；报告内注明 FIX-F5 卡记录行号与当前 HEAD 的漂移。
- 验收：**pass（2026-09-27 决策会话）**——四线全超线（31≥15 生成点/7≥5 不一致/4≥2 痛点/报告入库）；锚点抽查四发四中（agentprotocol types.go:131 自由字符串论断/audioclosure ObservationKey 结构/harness.go:7337 F5 前缀判族/snapshot_id 全库零命中 grep 复验）；§1"自由字符串+prompt 无格式定义→下游防御解析"因果链与 §5 痛点互证成立；ObservationKey 六段最接近物的发现直接作为 G1 演化锚点。**边界两项裁定**（报告 §6 上交）：前端仓第三写者（telemetry_manager.gd request_id 族）与 PCA 层补查均**并入 9-30 G1 评审预备的决策侧亲读范围**（量小不拆卡）。
