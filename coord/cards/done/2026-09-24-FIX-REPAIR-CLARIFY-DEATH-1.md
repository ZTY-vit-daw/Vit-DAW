# FIX-REPAIR-CLARIFY-DEATH-1：repair-clarify 协议违约判死策略软化——557 二次强化 repair + raw 诊断落盘

- 优先级 / 预估 / 依赖：P2（新失败族 10 轮 1 例，鲁棒性非演示阻塞）/ 0.3 天 / FORENSIC-MAC-CLARIFY-CHAIN-1 取证结论（[rulings/2026-09-24-FORENSIC-CLARIFY-1-pass.md](../rulings/2026-09-24-FORENSIC-CLARIFY-1-pass.md)，用户裁定修复另卡）
- 模型分级：L2 / GLM-5.3（chat 协议处理层，红测试先行）
- **背景（取证定责在案）**：`audio_closure_controller.go:557`（`repairCount>0 && res.NeedsClarification`→"model protocol failure" 判死，ee0fac4 入库）在「原始输出畸形+repair 违令返回 clarify」双方差同时发生时直接判死整轮——repair 明明解析成功（`message_loop.go:4322-4327` 闭包变体禁令在 prompt 里），模型却违令；策略把协议违令直接升级为任务终态，无缓冲重试。取证工件 `~/Documents/vit-forensic-clarify1-artifacts/`（秒级时间线+代码锚点）；证据边界=repair 原文未落 durable（反推高置信）。
- 目标：
  1. **557 软化**：repair 解析成功但返回 needs_clarification 且闭包在案时——**不直接判死**：先追加一次「禁-clarify 强化 repair」（在 repair prompt 显式重申禁令+指明上一轮违令形态，或按实现最小化：剥离 needs_clarification 字段按不可恢复解析降级，二选一由执行侧按代码面最小改动定，卡面倾向前者）；仍违令才走 557 判死原路径。红测试：修前=违令一次即死（取证形态复现）；修后=违令两次才死、二次合规则轮继续
  2. **repair 原文落盘**：repair 成功分支补 raw 输出诊断落 durable/遥测面（正对取证证据边界——下次同类事件可直证不再反推）
  3. 回归：F4 判死路径行为不变（真畸形不可修复仍判死）；PC 双端共用（chat 包），单边开发两端合入；agent 全量+webui 绿
- 文件域：`agent/internal/chat/audio_closure_controller.go` + `agent/internal/chat/message_loop.go`（repair 分支）+ 测试；不改 durable schema（诊断落既有遥测/日志面）
- 验收：①红测试修前红/修后绿（双方差复现+二次合规继续两态）②agent 全量+webui 绿 ③10 轮 spot（或与下轮统计批合并观测）零新形态 ④回执两端 HEAD
- 停止条件：软化需动 durable schema 或 prompt 协议层大改 → 上交拆卡；与 settle/F2 面发现纠缠 → 上交
- 领取：2026-09-24 / origin/main=56f0ef9（本地 main 与远端同步 0/0，=发卡基线）/ port/fix-repair-clarify-death；领取时 status 两 Workspace 残留（Settings.xml + default_project.xml 运行时状态）+ .zcodeignore/VitApp/Workspace/Artifacts/ 未跟踪，均不碰
- 回执：实现 commit `54bcfec`（port/fix-repair-clarify-death，origin 已推，仅 message_loop.go +94 / message_loop_audio_closure_test.go +131，chat 层零 diff）；领取提交 main `f65ab63`；工件 `~/Documents/vit-fixrepair-clarify1-artifacts/`（README 索引+红绿/全量/webui 输出 5 件，key 扫描零命中）。**实现选择=强化 repair（卡面倾向项）**，理由：①取证建议与裁定原文均把追加修复点放在 message_loop repair 分支（唯一持有 LLM client 与会话状态的层），chat 557 判死路径保持原样作二次违令终态（行为由既有 `TestAudioClosureRepairClarificationBecomesProtocolFailure` 锁定）；②剥离降级作为强化 prompt 的诚实出口保留（真不可恢复时返回 failure_reason 形态→既有 603 行路径判死），两方案在最深处汇合。三个执行面决定：**a)** 违令+强化计一个 repair episode（`ModelProtocolRepairs` 不二次累加——`MaxModelProtocolRepairs` 默认=1，计 2 会让无 contract 的合规恢复被 `RecordProtocolRepair` 预算超限分支误 settle 为 protocol failure）；**b)** 强化调用遥测 source=`message_loop_repair_clarify_reinforce`（与首次 repair 可区分，取证可直接定位）；**c)** 卡面文件域路径笔误更正：message_loop.go 实际在 `internal/agentloop/`（非 internal/chat，取证报告引用同文件）。红绿：红1（违令一次即死）修前 FAIL exit=1→修后恢复完成 PASS；双违令修前 FAIL（calls=2 无强化）→修后 calls=3 暂停待 557 PASS；红2（真畸形不可修复）修前 PASS exit=0（F4 判死锁）→修后不变 PASS。全量：`go build ./...` exit=0；`go test ./...` agentloop/audioclosure/chat 全 ok，唯一 FAIL=contextruntime `TestDefaultSnapshotPathFindsVitAppFromNestedWorkdir`（/private/var vs /var 路径断言——origin/main 基线 worktree 复核同样失败，FIX-TEST-CTXSYMLINK-1 域，非本卡引入）；webui vitest 28 files/324 tests 全过 exit=0。**端测边界声明**：本卡验证=单元红绿+全量编译测试；10 轮 spot 按卡面③许可与下轮统计批合并观测；未跑真实栈烟测（协议层改动无渲染面/用户旅程面）。停止条件未触发（零 durable schema 改动——诊断走既有 messageLoopDiagnostic JSONL 面；prompt 协议层未大改——仅既有 repair 机制内新增一个变体调用；settle/F2 面零纠缠——chat 层零 diff）。两端 HEAD：决策侧发卡=56f0ef9；执行侧交付=port/fix-repair-clarify-death 54bcfec + main f65ab63→本提交。
- 验收：**pass（2026-09-24 Mac 决策会话，[rulings/2026-09-24-FIX-REPAIR-CLARIFY-DEATH-1-pass.md](../../rulings/2026-09-24-FIX-REPAIR-CLARIFY-DEATH-1-pass.md)）**——红绿独立核证（红测试以 calls=2 want=3 精确红在缺失强化调用；F4 不可恢复判死锁双向绿；分支 tip 决策侧亲跑 ok）；实现选择（强化 repair）理由链成立（chat 557 保持二次违令终态+既有测试锁定）；episode 计数防预算误 settle 与遥测 source 可辨两决定采信；路径笔误更正（message_loop.go 实为 agentloop 包）采纳——取证报告同更正随附；chat 层零 diff 实证与 settle/F2 零纠缠声明一致；contextruntime /var 既有项基线复核归属正确。实现 54bcfec 经决策侧 cherry-pick 入 main
