# PORT-C2：mac 校准链脚本（探针→白名单草稿→PCA 重认证→冒测，23 主体口径）

- 优先级 / 预估 / 依赖：P1 / 2-3 天（PORT_AUDIT 层 C，"把移植永久降级为重校准"的关键件）/ 依赖 A5（已验收：真栈冒测与 WaveShell 枚举就位）
- 模型分级：L3 / GLM-5.3（跨探针/白名单/PCA/冒测四段的链路设计；先读 PC 侧既有模式再动工）
- 目标：PC 侧校准链（参考 `scripts/b1_2_source_calibration_agent_smoke.py` 与 `b1_2_strict_reference_calibration_agent_smoke.py` 的模式）在 mac 可执行的等价物：一键 起栈 → 插件探针（23 个 Waves 晋升主体，U2 收窄口径，A5 的 719 清单为超集源）→ 语义白名单草稿 → PCA 重认证 → 只读冒测收尾；两端可重复（脚本尽量平台中立，mac 首跑）
- 文件域：`scripts/`（新脚本，命名对齐既有惯例）；如需 agent 侧端点/最小接线 → 列明并最小化（涉 `agent/` 的改动按域外上报流程）；不改 `VitApp/Source`
- 验收标准：① mac 本机一键全链 exit 0（复用 A5 冒测脚本拉栈或其模式，运行态独立临时目录）；② 产出 23 主体的白名单草稿工件（JSON，主体名/path/探针结论对齐 PC 侧 schema——以 PC 侧现有白名单文件结构为准并附对照说明）；③ PCA 重认证在 mac 落盘（机器本地状态目录，AGENTS §10：不写源码树/真实工程）；④ 收尾只读冒测 PASS；⑤ 工件目录含 run ID、各段日志、最终白名单草稿
- 停止条件：探针发现某晋升主体实例化/加载失败（非枚举）→ 按 R2 预案记录证据转 blocked（升级 tracktion/JUCE 另开卡）；PC 侧 schema 与 mac 实测不兼容 → 上交对照证据由决策侧裁定 schema 取舍
- 领取：2026-09-18 11:04 CST（Mac 执行流，用户直令「开工 PORT-C2」，BOOTSTRAP 口令表授权）/ origin/main `ad5d2e2acea3db264c7e27949646c0d6df56d763` / 分支 `port/c2-mac-calibration-chain`（独立 worktree `/tmp/vit-c2-worktree`，PROTOCOL §3）
- 回执：实现 commit `309e640`（分支 `port/c2-mac-calibration-chain`；文件域内：`scripts/pca_calibration_chain_mac.sh` 新增 917 行 + `scripts/SMOKE_TESTS.md` 补条目；**agent/ 与 VitApp/Source 零触碰**——全链复用既有 agent 端点：`plugin.semantic_build_index`、`/agent/processor-certification/{candidates,start,status}`、C4 GET 白名单）。验证（本机 darwin/arm64，源在独立 worktree `/tmp/vit-c2-worktree` @HEAD ad5d2e2；运行态全部落独立 mktemp workdir，机器本地校准态按验收③落 `~/.vit/`，前后哈希入工件）：
  - **验收①（mac 一键全链 exit 0）** 权威 run `pca_calibration_chain_mac_20260918-115112`（workdir `/var/folders/.../T/pca_calibration_chain_mac.vulCBEUv`）：cmake 内核构建（经 `--cmake-proxy` 拉 FetchContent tarball）→ 内核起（5555/5556/5557 属主=内核 pid）→ agent /health → `project.state` agent→内核往返 ok → 探针 `plugin.semantic_build_index`（内核 child-process 扫描 **719 主体 0 失败**，语义索引落 `~/.vit/plugin_semantics.json`）→ U2 主体选择（12 族 Mono/Stereo **24 主体 0 问题**，719 清单为超集源）→ 白名单草稿 → **PCA 重认证 24/24 全部 certified+promoted**（每主体 ~2s：临时轨→consent token 授权 load（真实 WaveShell 实例化加载）→ typed inspect×2（generation 稳定）→ apply/readback/restore→全参数快照还原→删轨）→ 只读冒测 PASS → 停栈（kernel `SIGTERM:143:1` A1 发现复现、agent `SIGTERM:0:1`）；summary `pca.calibration_chain.mac.v1` 六门全 true、`overall_status=PASS`、脚本 exit 0。
  - **验收②（白名单草稿）** `whitelist_draft.json` + 认证后终稿 `whitelist_draft_final.json`（24 主体 × 6 族：broadband_compressor 2 / de_esser 6 / gate_expander 2 / limiter 4 / multiband_dynamics 6 / transient_shaper 4）：行结构对齐 PC `processor_certification_entry.v2` candidate schema（id/name/manufacturer/format/identifier/plugin_path/family）+ `plugin_semantics` Entry 身份字段 + probe/pca 证据字段；卡内要求的"对照说明"以 `schema_alignment` 内嵌（含与 PC `free_state_experiment_plugins.v5` 域钉视图的关系说明）。**口径差异上报**：U2 记 23 主体（"12 族 Mono/Stereo 变体"），mac 枚举 12 族纯变体=**24**；草稿保全集并内嵌 `reconciliation_note`，裁到 23 由决策侧在采纳时定（C1 comp-gate/comp-sc、C6-SideChain、Sibilance-Live、TransX Multi 已按非晋升变体排除）。identifier 跨内核会话稳定已核对（PSE Mono `b943518d`、C1 comp Stereo `65ffd264` 与 A5 枚举一致）。
  - **验收③（PCA 重认证落盘）** `~/.vit/`（机器本地，源码树零写入）：`processor_control_attestations.v1.json`（broadband_compressor：2 promoted；1 stale 为 run2 崩溃遗留同主体旧收据，被 run3 新收据按 store 取代语义正确压为 stale，非异常）、`processor_control_attestations.v2.json`（**22 promoted**，5 族齐）、`pca_certifications/` 25 个 job 收据目录；前后态哈希 `vit_state_before/after.txt` 入工件。
  - **验收④（收尾只读冒测）** C4 白名单 4 GET 全 2xx + `GET /agent/processor-certification/candidates?family=all` 断言 **24/24 promoted、0 missing、0 not_promoted** + PCA 库只读解析（`readonly_verification.json`）；只读段零 mutation。
  - **验收⑤（工件）** run workdir 含 run ID、内核/agent 全量日志（含 configure/build）、各段 JSON（invoke 应答、语义索引副本、选择清单、逐主体认证记录）、白名单草稿两版、`readonly_verification.json`、`summary.json`、`run_meta.txt`（HEAD ad5d2e2 + dirty 计数 + 双 bin sha256）。
  - **失败分类（AGENTS §8）**：run1 环境中断类——FetchContent 拉 libzmq 直连被墙失败（代理注记过时，curl 实测 7890 已通）→ 脚本加 `--cmake-proxy`（仅内核构建注入，平台中立保留默认直连）；run2 脚本缺陷——Go 结构体双 import 字段必序列化致 v2 零值 `promoted:null` 被 `len()` 击杀（首个主体已认证成功后崩溃，留 v1 store 1 条记录，即上 stale 来源）；run3 全绿。
  - **端测覆盖边界声明（AGENTS §5）**：已覆盖=真实双件栈（内核 mac 起停/ZMQ/扫描）+ agent HTTP 全链 + 24 Waves 主体**真实实例化加载**与 typed 控制面 + PCA 重认证/晋升/只读断言；**未覆盖**=Godot 前端（不在卡面）、webui 渲染与用户旅程（本卡无相关改动）、pluginprobe 原生观察宿主（`PluginProbe/native-host` 现仅 Windows——`module_win32.cpp`+bcrypt；本链以认证 runner 的真实加载+typed inspect 为加载证据，观察级参数面探针归 C3 口径）、experimentplugins v5 域钉白名单的 mac 采纳（草稿已附对照，待决策侧裁定）。
  - **平台可重复性**：脚本 mac 首跑；PC 侧可经 Git Bash + `--vst3-dir "C:/Program Files/Common Files/VST3"` 复用同一模式（lsof/stat -f 为 darwin 依赖，PC 跑通前不宣称两端已验证）。
- 验收：pass（2026-09-18，ruling [2026-09-18-C2-pass.md](../../rulings/2026-09-18-C2-pass.md)：工件级+状态级核验全过（diff 域/六门 summary/`~/.vit` 双 store/25 收据/identifier 锚点）；**23vs24 裁定：采纳 24 为 mac 校准面不裁剪**（U2 语义=全部 Waves，23 系 PC 计数事实）；`309e640` 已 cherry-pick 入 main；复验 worktree `/tmp/vit-c2-worktree` 已清理）
