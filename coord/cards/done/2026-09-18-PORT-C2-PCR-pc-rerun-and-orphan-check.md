# PORT-C2-PCR：C2 校准链 PC 复跑（两端可重复性实证）+ 孤儿改动归属核对

- 优先级 / 预估 / 依赖：P2 / 0.5-1 天 / 依赖 C2 已验收（脚本已在 main：`scripts/pca_calibration_chain_mac.sh`）
- 模型分级：L2 / GLM-5.3（跨平台脚本适配 + 双端校准态语义判断）
- 目标：两段——
  - **前置义务（先做，未完成不得开工第二段）**：核对 PC 主工作树孤儿改动 `agent/cmd/vitagent/main.go`（领取前已存在，10+/3-，PC-VERIFY-1 ruling 移交项）的归属：git log/diff/reflog 考古（人工改动 or 遗留卡残留），结论与证据回执本卡；结论为"未知归属人工改动"时保持不触碰并在回执标注
  - **PC 复跑**：在 PC（Git Bash）用 C2 同一脚本（或最小平台分支适配，保持单脚本两端可跑）跑通全链：起栈→探针→白名单草稿→PCA 重认证→只读冒测，产出 PC 侧主体清单与草稿工件，与 mac 24 主体草稿逐主体对照（含 Windows 侧 12 族实际 Mono/Stereo 体数定位——U2 记 23 的机器事实核对）。**PC 现网 `~/.vit` 是权威校准基线**：脚本自带的 before/after 态哈希必须入工件；任何超出"同主体收据取代更新"语义的状态 delta 立即停止、按快照回滚、转 blocked
- 文件域：`scripts/pca_calibration_chain_mac.sh`（平台分支适配，保持两端可重复；**不得**借机重构）；如适配量超出平台分支级别（需结构性改写）→ 停止上交，不自行新起脚本
- 验收标准：① 孤儿改动归属结论 + 考古证据（命令与输出）；② PC 全链 exit 0 + run ID + 各段日志 + PC 白名单草稿 JSON；③ PC↔mac 逐主体对照表（体数差异定位到族）；④ PC `~/.vit` before/after 哈希与 delta 语义说明
- 停止条件：脚本 PC 适配需结构性重写 → 上交由决策侧裁定拆卡；探针/认证失败按 AGENTS §8 分类记录；`~/.vit` 出现非取代语义 delta → 快照回滚 + blocked
- 领取：2026-09-18 18:45 +0800 / origin/main db55f861（领取时 HEAD=db55f861，工作树仅 M agent/cmd/vitagent/main.go 即前置义务调查对象）/ 分支 port/c2-pc-rerun（独立 worktree D:/Vit_DAW_worktrees/c2-pc-rerun）
- 回执：（执行侧自验完成 2026-09-18 19:55 +0800，四项验收标准全闭合）
  - **① 孤儿改动归属：已归属，非"未知归属人工改动"** —— 结论：`agent/cmd/vitagent/main.go` 的 required 注册有界重试（10+/3-，`requiredRegistrationAttempts=30`×2s）是 **2026-09-17 晚 ZCode 会话 `sess_655bbfcf`（用户直令的 v0.93 参赛报名打包会话）** 写入的"安装包启动卡死"修复，非遗留卡残留。证据链（秒级闭合）：a) tool artifacts `call_c6c2abd3...`（2026-09-17T13:25:11Z）与 `call_8589bd019...`（13:25:26Z）= 北京时间 21:25:11/21:25:26，与文件 mtime `2026-09-17 21:25:26` 精确吻合（ReadSessionContext 直读该会话正文确认任务与修复上下文）；b) `agent/bin/VitAgent.exe` 21:27 重编译、`Export/_build/Vit_DAW_v0.93_release` 21:32 重新装配（exec 日志 `[2/6] Building VitAgent`）；c) 功能验证存在：`%LOCALAPPDATA%/Vit-DAW/Logs/agent_last.log`（21:33 打包版试跑）显示 required 模式 attempt=1 收 502（kernel dial refused）后 WARN pending、2 秒后注册成功——旧代码 required 首败即 stop() 不可能产生该序列；d) `git log --all -S requiredRegistrationAttempts` 零命中（从未提交）、当晚 reflog 最后一笔 14:41 pull、coord/ 与调度中枢 queue/ 零记录、该会话自记"三笔改动待分别 commit"但未提交。**处置**：按卡面纪律全程未触碰、未提交、未回退（工作树领取时/完成时均保持该 diff 原样）；修复语义合理（launcher 先拉 agent 后内核绑 ZMQ 的冷启动窗口），建议决策侧补一张正式 commit 卡收编该改动+v0.93 iss 重写（iss/redist 在 Export/ 不入库）。
  - **② PC 全链 exit 0**：run `pca_calibration_chain_win_20260918-190731`（workdir `D:/Vit_DAW_run/c2pc-20260918-190207`，HEAD db55f861 独立 worktree，repo_dirty=1 即脚本适配 diff 自身）：内核构建（MSVC VS18 默认生成器，configure 29.6s）→ 内核起（5555/5556/5557 属主=内核 winpid 断言过）→ agent /health → project.state 往返 ok → 探针 `plugin.semantic_build_index`（**991 主体**，语义索引重建落 `~/.vit/plugin_semantics.json`）→ U2 选择（12 族 **24 主体 0 问题**）→ 白名单草稿两版（`whitelist_draft.json` + `_final.json`）→ **PCA 重认证 24/24 certified+promoted**（每主体 ~4s，24 个 job 收据落 `~/.vit/pca_certifications/`）→ 只读冒测（4 GET 2xx + candidates 断言 24/24 promoted + store 只读解析）→ 停栈（kernel/agent 均 `SIGTERM:143:1`，与 mac A1 发现同型）→ summary 六门全 true、`overall_status=PASS`、**脚本 exit 0**。内核 sha256 `7981e72f…`、agent sha256 `3ab13c17…` 入工件。各段日志/JSON 全在 workdir（kernel_configure/build、agent_stdout、http/、probe/、cert/、summary.json、run_meta.txt）。**run1 环境中断记录（AGENTS §8 env 类）**：github.com:443 连接超时/重置致 FetchContent 拉 libzmq 失败（codeload 直连可通但 FetchContent 先跳 github.com 重定向；本地 7890 代理已死）→ 处置：脚本新增平台中立可复用参数 `--fetch-src NAME=PATH`（`FETCHCONTENT_SOURCE_DIR_*` 覆盖，与既有 `--cmake-proxy` 同性质的环境旁路），源树取 PC-VERIFY-1 今日同 URL pin 拉取的 libzmq v4.3.5/cppzmq v4.10.0 复制到本 run 目录隔离使用（`fetch_cache/`，845 文件）；`~/.vit` 在 run1 中零触碰（前后哈希核对一致）。run2 全绿。
  - **③ PC↔mac 逐主体对照**（工件 `pc_mac_subject_comparison.json`）：**主体名单 24/24 完全一致**（name_set_identical=true）；12 族每族 Mono+Stereo 齐全，族分布与 mac 逐项相同（broadband_compressor 2 / de_esser 6 / gate_expander 2 / limiter 4 / multiband_dynamics 6 / transient_shaper 4）；全部 24 体 identifier 均挂 WaveShell 17.1 标记（`695648d`）；mac 侧仅有的两个已记录 identifier 锚点跨平台命中（C1 comp Stereo `65ffd264`、PSE Mono `b943518d`——VST3 uid 跨平台稳定）。plugin_count PC 991 vs mac 719（PC 另装 PA/FabFilter/其他厂商，属超集）。**U2"23"机器事实核对结论**：PC 当前扫描与 run 前 2026-07-28 旧基线索引**都是 24 体**（每族 Mono/Stereo 齐、均出自 17.1 shell）——语义索引层面不存在 23 计数；唯一自洽推导是收窄算术（31 全场面 − 5 PA − 3 FabFilter = 23）。23-vs-24 的跨机指纹级收口归 PORT-C3。
  - **④ ~/.vit 前后哈希与 delta 语义**：before/after 快照入工件（`vit_state_before/after.txt`），另做 run 前全量备份（`vit_state_backup_prerun/` 8 文件，回滚快照）。delta：**+48**（24 job 收据 × summary+evidence）、**−0**、内容变更 5（`plugin_semantics.json` 设计内重建 993→991 条；双 attestation store 及其 .bak 轮换）。**逐主体语义判定通过**：v1 store 29→31 主体（仅新增本次 C1 comp Mono/Stereo 两条，旧主体零变更零消失）；v2 store 31→36（新增本次 5 条 Mono 收据：L1/L2/PSE/Smack Attack/TransX Wide Mono——旧基线这五族只认证过 Stereo；另 17 个同主体旧收据按取代语义压 stale、新收据 promoted）。新增/取代收据合计恰=本次 24 主体，**无任何超出"同主体收据取代更新"语义的 delta，未触发回滚**。
  - **适配量与两端可重复性声明**：脚本 `scripts/pca_calibration_chain_mac.sh` 平台分支适配 diff **184+/41-**（单一 uname 分支守卫：sha256sum/netstat 工具面、混合路径 D:/ 形态、winpid 端口属主断言、MSVC 多配置构建、`--fetch-src` 环境旁路、win 侧选择语义（多 WaveShell 同名去重+单变体记为机器事实，darwin 严格成对逻辑逐字未动））；无结构性改写，mac 路径行为保持（bash 3.2 安全空数组惯用法沿用）。注：win 去重分支本次未触发（内核扫描已按 17.1 shell 单源枚举，enumeration_notes=0），作为防御性平台分支保留。**端测覆盖边界（AGENTS §5）**：已覆盖=真实内核+agent 双件栈 PC 端起停/ZMQ/HTTP 全链+24 Waves 主体真实实例化加载与 typed 认证+晋升+只读断言+脚本 exit 0；未覆盖=Godot 前端（不在卡面）、webui 渲染与用户旅程（本卡无相关改动）、pluginprobe 原生观察宿主与 24 体全量指纹对照（PORT-C3 口径）。
  - **Git/推送状态**：port/c2-pc-rerun 实现提交 `802c616d`（脚本 206+/41-，worktree `D:/Vit_DAW_worktrees/c2-pc-rerun`）；卡片状态变更 commit 于 main。**GitHub push 自 18:46 起对本机不可达**（github.com:443 连接超时/重置，多轮重试未果；codeload 可通）——领取 commit `1accfa20` 与 done commit `6cd79bf0`（rebase 于 Mac 侧 B6/C3 两笔之上，孤儿 diff 经 autostash 原样往返）已随 port/c2-pc-rerun `802c616d` 一并推送。
- 验收：pass（2026-09-18，ruling [2026-09-18-C2PCR-pass.md](../../rulings/2026-09-18-C2PCR-pass.md)：四项全闭合；**U2"23"出处修正**=收窄算术产物（31−5−3），双端机器枚举皆 24 体，C2 ruling 的"23 系 PC 计数事实"表述作废、结论不变更硬；孤儿 main.go 裁定收编（下一张 PC 卡正式 commit）；802c616 已 cherry-pick 入 main 8b09480；PC 指纹生产并入下一张 PC 卡）
