# PORT-B7A：VitWebViewHost 源码收编（Windows WebView2 宿主，从未入仓的运行时依赖）

- 优先级 / 预估 / 依赖：P1 / 0.5 天 / 无（B7B 的契约前置）
- 模型分级：L2 / GLM-5.3（C++ 收编纪律 + API 契约文档化）
- **背景（2026-09-19 用户实测根因，修正 PORT_AUDIT §1.3(a)）**：Ask Vit WebUI 面板（`vit_dock/scenes/vit_dock_root.gd:2543`）运行时经 **ClassDB 字符串引用** `"VitWebViewHost"` 实例化 webview 宿主——审计当年按文件名/preload 搜索判其"休眠"系盲区；该类源码 `extension/src/vit_webview_host.{cpp,h}` 与 PC 本地 SConstruct 的 webview2 接线**从未提交**（仓内 register_types.cpp 仅注册 VitWaveformReader）。mac 上类不存在 → `_ensure_agent_webui_host()` 假 → `OS.shell_open(AGENT_WEBUI_URL)` → **Safari 兜底**（用户所见）。
- 目标：
  1. **原样收编**（AGENTS §12：作为-is 权威输入，不重构不改语义）：`extension/src/vit_webview_host.{cpp,h}` + PC 本地 SConstruct 的 webview2 守卫接线（`VIT_WITH_WEBVIEW2` 宏/WebView2.h 存在性守卫/平台链接库）→ 分支 `port/b7a-webview-host` 单 commit
  2. **Windows 构建验证**：干净 worktree 用收编后树 `scons platform=windows target=template_debug`（或既有 PC 构建口径）exit 0，产物含 VitWebViewHost 导出（符号/类注册证据）
  3. **API 契约文档**：`extension/src/vit_webview_host.md`（或头注内文档块）——逐方法/逐信号清单（对照 vit_dock_root.gd 的 has_method/has_signal 调用面：create/create_window/create_overlay_window/navigate/close/set_bounds/set_visible/focus/eval_js/post_web_message/set_user_data_subdir/debug_state/get_selected_plugin_*/vit_open_strip_silence_dialog + ready/navigation_started/page_state_changed/load_error/web_message_received/new_window_requested 等），供 B7B 实现 mac 适配
  4. 收编前核对工作树该文件与 2026-09-15 审计所记形态一致性（异常漂移→上报）
- 文件域：`extension/`（新增 cpp/h/SConstruct 接线/契约文档）；不改 Godot 工程、不改 `agent/`、`VitApp/`
- 验收标准：①分支单 commit diff=工作树原样（无重排无重写）；②Windows 构建 exit 0 + 类注册证据；③契约文档覆盖调用面全集；④审计一致性核对记录
- 停止条件：源码含不该入仓内容（凭据/密钥/绝对路径密写）→ 剥离后上报；构建失败且原因在收编源码内语义（非接线）→ blocked 上交；发现 webview host 有第二份变体（overlay/别的宿主类）→ 一并列明上报决策侧定收编范围
- 领取：2026-09-19 09:58 / origin/main=ec6460cbe2a58222cff7860066ebd5349f4b1c65（pull --rebase 后 Already up to date） / 分支 port/b7a-webview-host
- 回执：实现 commit `300b510b`（分支 `port/b7a-webview-host` 已推；8 文件 +66270 全在 `extension/` 域：src/vit_webview_host.{cpp,h}、third_party/webview2/{LICENSE.txt,include/WebView2.h,include/WebView2EnvironmentOptions.h}、SConstruct、src/register_types.cpp、src/vit_webview_host.md；.obj/bin/.sconsign/godot-cpp 未入仓）。**验收①**：5 个收编文件 git blob hash 与前端仓 HEAD 逐位一致（c06643fc/c592a088/45f02978/5b3f17f9/c403f4fd）+ 工作树 sha256 双验通过；register_types/SConstruct 为卡面授权的最小接线改动（+2/+16 行）。**验收②**：Windows 构建 `cd extension && scons platform=windows target=template_debug` exit 0（godot-cpp 上游无 4.6 tag，按 B1 同 pin master@507ed9d840c0，extension_api-4-6.json 自证 4.6 API）；产物 `bin/vit_extension.windows.template_debug.x86_64.dll`（485888 B）含类注册三重证据：`VitWebViewHost` 字节串 12 处、`vit_extension_library_init` 导出入口 1 处、WebView2 真后端特征（`WebView2Loader.dll`/`CreateCoreWebView2EnvironmentWithOptions` 命中，stub 串零命中，UTF-16 宽串 VitDAW/ChildWindow 在）。4.2.2 时代源码对 4.6 godot-cpp **零 API 漂移**（一次编译通过）。**验收③**：契约文档 `extension/src/vit_webview_host.md`——21 方法/5 信号逐项（签名/语义/错误路径/stub 行为），4 消费方调用面全集（vit_dock_root `_agent_webui_host`+`_agent_browser_host`、browser_panel、artifact_panel）；117 处 has_method/has_signal 已分界：落在 VitWebViewHost 上 19 处守卫，其余（get_selected_plugin_*/vit_open_strip_silence_dialog/new_window_requested）**非本类成员**，已在文档 §3 显式标注防 B7B 误实现。**验收④**：审计一致性核对入文档 §0——源码与前端仓 HEAD 无漂移，漂移的是审计引用判定（4 消费方均 ClassDB 字符串引用，文件名搜索不可见）。**停止条件扫描**：无凭据/密钥/绝对路径密写（路径全运行时动态解析）；无第二宿主变体（overlay 为同类内模式）。**端测边界**：dll 仅静态证据（字符串/导出），未做 Godot 运行时加载验证（PC 本机 Godot 4.6 可执行文件未在验证路径内；真栈加载属 B7B/后续集成面）
- 验收：**pass**（2026-09-19 PC 决策侧，[rulings/2026-09-19-B7A-pass.md](../../rulings/2026-09-19-B7A-pass.md)）——四验收项全部决策侧亲验：①5 文件 blob hash 与前端仓 HEAD 逐位一致（接线 diff 最小且保留 B1 适配）②干净 worktree 检出 300b510b 从头构建 exit 0，dll 真后端证据/stub 零命中亲验 ③21 方法/5 信号与源码逐项一致，"非本类成员"分界独立复核成立 ④审计一致性结论复核成立；停止条件扫描干净。实现已 cherry-pick 入 main（见 ruling）。B7B 契约前置解锁
