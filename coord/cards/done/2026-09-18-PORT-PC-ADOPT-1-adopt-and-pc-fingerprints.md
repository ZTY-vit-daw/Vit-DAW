# PORT-PC-ADOPT-1：孤儿修复收编 commit + PC 参数面指纹生产（闭 C3 双端对照）

- 优先级 / 预估 / 依赖：P2 / 0.5 天 / 依赖 C3 已验收（双平台代码在 main，PluginProbe win32 面原样保留）
- 模型分级：L2 / GLM-5.3（指纹语义对照 + 收编纪律）
- 目标：三段——
  - **A（收编 commit）**：将主工作树孤儿 diff `agent/cmd/vitagent/main.go`（required 注册有界重试，10+/3-，归属 2026-09-17 晚 `sess_655bbfcf` v0.93 打班会话的"安装包启动卡死"修复——C2-PCR ruling 裁定收编）正式提交：分支 `port/pc-adopt-1` 单 commit，message 注明归属溯源与功能语义；**提交前逐行核对 diff 与 C2-PCR 回执所记一致**（requiredRegistrationAttempts=30×2s 有界重试），零夹带
  - **B（PC 指纹生产）**：用 C3 双平台代码在 PC 产参数面指纹：PluginProbe native-host MSVC 构建（win32 面应原样编译——C3 构造级等价论证的实证点）+ reprobe driver 对 12 族（Mono/Stereo 共享同面，24 体任选代表或全跑）探针，产出 `pc_fingerprints.json`；driver（`PluginProbe/reprobe/reprobe_parameter_surface.sh`）如需 Git Bash 适配 → 域内最小 uname 守卫（对齐 C2-PCR 脚本模式），列明
  - **C（双端对照收口）**：与 transfer 到达件 `inbox-mac/2026-09-18-mac_fingerprints.json`（mac 权威 run 20260918-202010 的 24 主体指纹）逐族对照，产出对照终稿（12 族逐族 verdict=identical/different，different 附参数面 diff 摘要），**闭 C3 验收②的 pending_pc_reference**；终稿判定"参数元数据哈希跨平台稳定"预期成立与否
- 文件域：A=`agent/cmd/vitagent/main.go`（仅收编既有 diff）；B=`PluginProbe/`（driver 平台适配如需）；C=工件落 PC 本地目录，对照终稿 JSON 投 transfer 仓 `inbox-pc/`（带日期）回传 mac
- 验收标准：A：分支单 commit、diff 逐行=孤儿 diff、message 含溯源；B：MSVC 构建 exit 0 + 12 族（或 24 体）指纹 JSON + run ID/各段日志；C：12 族对照表全有 verdict + 终稿已投 transfer
- 停止条件：MSVC 编译 win32 面失败（C3 等价论证失效）→ 附原始错误转 blocked；不一致族 >1/3 → 停止上交对照证据由决策侧裁定口径；孤儿 diff 在开工前已被改动/消失 → 停止上报
- 领取：2026-09-18 21:20 CST / origin/main dddf6455（领取时 HEAD=dddf6455，工作树仅 M agent/cmd/vitagent/main.go 即 A 段收编对象，diff 已逐行核对与 C2-PCR 回执一致 10+/3-）/ 分支 port/pc-adopt-1（独立 worktree D:/Vit_DAW_worktrees/pc-adopt-1）
- 回执：（执行侧自验完成 2026-09-18 21:55 CST，三项验收标准全闭合）
  - **A（收编 commit）**：领取时 HEAD=dddf6455、工作树仅 `M agent/cmd/vitagent/main.go`（停止条件核对：diff 完好）。提交前逐行核对与 C2-PCR 回执①记录**完全一致**：10+/3-（4 行冷启动注释 + `required && attempt >= requiredRegistrationAttempts` 判定 + Error/Warn 日志增强 + 常量块 3 行），`requiredRegistrationAttempts=30`（main.go:186）× `time.After(2 * time.Second)`（main.go:169）=60s 有界宽限，diff 内注释语义（launcher 先拉 agent、内核后绑 ZMQ）与归属证据链（sess_655bbfcf v0.93 打包会话）吻合，零夹带。**移植提交**：独立 worktree `D:/Vit_DAW_worktrees/pc-adopt-1`、分支 `port/pc-adopt-1`；主工作树导出 patch（只读）→ worktree apply → 双端 diff sha1 一致（`f0548cdd…`）→ `go build ./...` exit 0 → 单 commit **`b502e362`**（message 含归属溯源+功能语义）。主工作树全程未动：完成时仍 `M agent/cmd/vitagent/main.go`、diff sha1 `f0548cdd…` 不变（含 push 前 autostash 往返后复核）。
  - **B（PC 指纹生产）**：
    - **MSVC 构建（win32 面原样编译实证成立，C3 构造级等价论证闭合）**：cmake 4.3.0-rc2 默认生成器（VS18），源=worktree @85e69695（PluginProbe 与 main 逐字节同），`-DPLUGINPROBE_TRACKTION_ENGINE_DIR=D:/Vit_DAW/tracktion_engine`（本地子模块树 b439749，零网络旁路——同 --fetch-src 性质的环境输入）；configure exit 0（33.3s）+ worker Release **exit 0 零 error** + witness Release **exit 0 零 error**（logs: `cmake_configure.log`/`worker_build.log`/`witness_build.log` @ `D:/Vit_DAW_run/pcadopt1-20260918-213331/native-build/`）。
    - **driver 适配**：commit **`85e69695`**（141+/35-，域内 uname 守卫，对齐 C2-PCR 8b094805 模式）：①工具面 sha256sum 替 shasum、python 替 python3（WindowsApps shim 静默退出不可用）；②路径形态 BUNDLE `cygpath -u`、go overlay JSON 内 REPO_ROOT `cygpath -m`；③netstat -ano+awk 替 lsof；④host 产物名 .exe；⑤R4 darwin 红绿段（mac bundle 目录概念）→ win 臂记录环境事实（单文件 shell 形态 + Go `os.Symlink` 特权不可用原始错误，AGENTS §8 环境类证据入 `r4/win_r4_environment_record.json`，go overlay exit=1 记录在案非门控——行为面 fail-closed 证据由 C3 mac 侧已交付）；⑥指纹清单按 RUN_TAG 分支 pc_fingerprints.json；⑦PC-reference 缺席证据/对照段守卫为 darwin-run。darwin 臂行为等值保持（PY=python3/SHASUM=shasum 原 token）。
    - **权威 run `pluginprobe_reprobe_pc_20260918-213539` driver exit 0，overall PASS 七门全 true**：24/24 主体探针全成（JUCE uid 路径，~2.5s/主体）；installation 一致性 vs 本地 PCA v2 store **22/22 一致 0 mismatch**（覆盖数与 mac run 同为 22）；R4 单文件 shell symlink 扫描 count=0；win 环境事实记录就位；`~/.vit` semantics index 前后哈希不变（只读保证）；`pc_fingerprints.json` 24 主体落盘。工件根 `D:/Vit_DAW_run/pcadopt1-artifacts/pluginprobe_reprobe_pc_20260918-213539/`（run_meta.txt 含 HEAD/branch/dirty=0/worker sha `83d53741…`+host sha `91a511ac…`、driver.log、probe/ 24 主体 load/snapshot/unload、r4/、installation_consistency.json、summary.txt）。PC 侧同样 **12/12 族 Mono/Stereo 共享同一参数面**（与 mac 及文法设计一致）。
  - **C（双端对照收口）**：对照脚本 `compare_pc_vs_mac.py`（C 段文件域=工件目录，不入仓）对 transfer 到达件 `inbox-mac/2026-09-18-mac_fingerprints.json`（mac 权威 run 20260918-202010，24 主体）：**12/12 族 identical、0 different；主体级 24/24 identical**——逐族 parameter_surface 哈希与 mac 逐字节一致、参数计数全同（C1 comp eee94e12/139、C4 c76f38d9/167、C6 ca81b102/185、DeEsser 2bbfd976/136、L1 0b3c7d59/135、L2 ddd757d0/138、LinMB 7f3be155/178、PSE da8655c1/142、RDeEsser 115d9ab4/137、Sibilance 2dff3f50/137、Smack Attack d4f02853/142、TransX Wide 61c802a0/136）。**判定："参数元数据哈希跨平台稳定"预期成立**（`cross_platform_parameter_metadata_hash_stable=true`；停止条件 0/12 不一致 << 1/3 阈值未触发）。终稿已投 transfer：`inbox-pc/2026-09-18-pc_fingerprints.json` + `inbox-pc/2026-09-18-pc_vs_mac_family_comparison.json`，commit `d3e42c3` 已推送回传 mac。**C3 验收② pending_pc_reference 就此闭合（待决策侧终验）**。
  - **端测覆盖边界声明（AGENTS §5）**：已覆盖=真实 MSVC 编译（worker+witness 双目标）+ 真实 WaveShell 单文件 shell 24 主体实例化加载 + 观察级参数面/安装指纹（观察宿主独立进程，不经内核/ZMQ 栈）+ 与本地 PCA v2 store 一致性 + driver exit 0；未覆盖=webui 渲染与用户旅程（本卡无涉）、witness GUI 人工目检（仅编译验证，无 GUI 操作）、真栈烟测（本卡改动均为离线面：agent 启动重试逻辑收编为代码入库+编译验证，运行时行为属 v0.93 打包会话已验序列，如决策侧要求可另排真栈复核）。
  - **Git/推送状态**：`port/pc-adopt-1` 已推（b502e362 收编 + 85e69695 driver）；main 领取 commit `722a7ba6` 已推（rebase 于 B1 done `b509037b` 之上，coord-only diff 核对后推送，PROTOCOL §3 增补纪律遵守）；transfer `d3e42c3` 已推。21:20-21:33 github 连接重置间歇（领取 push 失败两次），21:40 起恢复，全部推送完成。
- 验收：
