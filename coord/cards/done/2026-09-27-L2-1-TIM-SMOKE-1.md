# L2-1-TIM-SMOKE-1：TIM 断言器端侧烟测腿——AssertWarnLogger 接线+真栈监督报警路径断言

- 优先级 / 预估 / 依赖：P1 / 0.4 天 / L2-1-TIM-1 已验收合入（边界声明两项归本卡：AssertWarnLogger 接线+监督报警真栈断言）；设计文档 §4.2 烟测草案为规格
- 模型分级：L2 / GLM 亲自或强督导（harness 接线小改+真栈操作）
- **执行侧（PC 会话；§9 真栈单会话所有权——领取前确认无其他栈运行）**
- 目标：
  1. **接线（实现小改）**：`tim.AssertWarnLogger` 注入钩子接到 harness 的 logx 实例——WARN 行格式按设计 §1.4 惯例 `[tim.assert] key=value`（对齐 cont_stall 等既有前缀过滤断言模式）；默认 nil 静默语义保持（单测断言注入与静默两态）；文件域 agent/internal/harness+tim 接线点，红绿先行（注入后 WARN 行出现的单测）。
  2. **真栈烟测（设计 §4.2）**：复用现有 ps1 体系按同模式新增（或扩展 dev_agent_smoke 参数）：真实三件套上对样例工程触发 `mix_request_observation` → 断言 ①mix_read `observation.tim_projection` JSON 的 `assertions[]` 存在且每项三态合法（pass/fail/not_evaluable）②fixture 构造一个触发项（近满幅轨触发 AS-PEAK 或 AS-SIG P2——素材可用 temp 生成或既有样例工程轨）→ 预期 fail 码出现在 assertions+risk_summary ③agent 日志差分出现 `[tim.assert]` WARN 行 ④**脚本 exit 0=PASS**。
  3. 回执：接线红绿+烟测完整命令/退出码/run ID/HEAD/工件（§9 真栈回执契约全项）。
- 约束：接线零行为扩散（仅日志面）；烟测脚本进 scripts/ 体系（ps1，Windows 形态；mac 侧复跑需要时按 _mac 同模式翻译另办）；新 run 目录不覆盖。
- 验收：接线红绿+真栈烟测 exit 0（四断言组全过）
- 停止条件：真栈上 assertions 块为空/缺失（说明装配链未触发）→ 取证上交（不修装配链——域外）；两次同形态确定性失败 → 停止重跑取证
- 领取：2026-09-26 22:00 +0800 / origin/main=4426903e31b9bf3bbf1e30b3eb5460c8a0d4738b（pull 后 HEAD）/ 领取前工作树仅运行时状态（M VitApp/Workspace/default_project.xml）+ 其他卡 untracked run 工件，无实现 diff；领取时 tasklist 核对无 VitAgent/VitApp/Godot 进程（§9 无栈占用）
- 回执：
  - **实现 commit**：`cf668e58`（分支 `port/l2-1-tim-smoke-1-wiring`，已推 origin）；4 files +657/-0：harness.go +6（接线）、tim/asserter_test.go +62（钩子两态）、harness/tim_assert_wiring_test.go +53（新，接线断言红绿）、scripts/tim_assert_smoke.ps1 +536（新，真栈烟测脚本）
  - **接线（目标 1）红绿**：红——`TestHarnessWiresTIMAssertWarnLoggerThroughLogx` 修前 `tim.AssertWarnLogger not wired by Harness construction` FAIL（exit 1，留档跑过）；绿——`newWithSender` 内 `logger != nil` 时注入 `tim.AssertWarnLogger = func(line){ logger.Warn("%s", line) }`，同测试过；两态单测 `TestAssertWarnLoggerInjectedEmitsFailLines`（注入态：`[tim.assert] ` 前缀+status=fail+code=assert_signal_nonfinite+track/value/threshold 键值全）与 `TestAssertWarnLoggerNilStaysSilent`（静默态：钩子不被改写、verdict 不变）全绿；`TestHarnessWithoutLoggerKeepsTIMHookUntouched`（nil logger 保持 nil 静默）绿。零行为扩散：diff 仅 6 行接线+测试，nil 语义保持
  - **回归**：`go test ./internal/tim ./internal/harness ./internal/mixboard -count=1` 全 ok（harness 17.2s 全包）；gofmt -l 报的 tim/harness 文件均为 CRLF 行尾假阳性（Windows checkout 既有状态，已合入文件同样报）；`go vet` 报的 shm_windows.go unsafe.Pointer 为领取前已有、域外未动
  - **真栈烟测（目标 2，设计 §4.2）**：脚本 `scripts/tim_assert_smoke.ps1`（复用 dev_agent_smoke 三件套拉起：`-StartKernel -StartUI -NoChatSmoke -NoStripSilenceSmoke -Strict`；日志差分=基线行集合差分，对齐 cont_stall:440/d1_stall:99-110 模式）；fixture=临时轨 track.add+0.999 振幅 440Hz 正弦 wav（peak=-0.009 dBFS，采样率跟随工程 audio_settings=48000 防 AS-SR 误触发）+clip.import_media_to_track 导入；触发=mix_request_observation scope=full_project；结束 track.delete 清理（status=ok）
  - **正式 run（最终轮）**：run_id `tim_assert_smoke_20260926_222429`，命令 `powershell -NoProfile -ExecutionPolicy Bypass -File D:/Vit_DAW/scripts/tim_assert_smoke.ps1 -ReuseStack`，**exit 0**；HEAD=4426903e（接线 diff 未提交态，dev_agent_smoke 现场构建 `bin/VitAgent.exe` 含接线——构建由 run `tim_assert_smoke_20260926_220853` 的 dev_agent_smoke 完成，其输出"build passed/installed agent bin"留档）；四断言组：A1 assertions[] 25 行全三态合法；A2 fixture 轨（track_id=2957）双 fail 码到位——`level_ceiling/ceiling fail value=-0.009 threshold=-1 code=assert_level_ceiling_exceeded` 与 `signal_hygiene/clipping_headroom fail value=-0.009 threshold=-0.1 code=possible_clipping_or_no_headroom`，两码均在 risk_summary.primary_codes；A3 日志差分 4 条纯 `[tim.assert]` WARN 行（例：`[WARN] [tim.assert] asserter=level_ceiling check=ceiling status=fail track=2957 value=-0.009 threshold=-1 code=assert_level_ceiling_exceeded`）；A4=exit 0。**同代码第二轮 PASS**：run `tim_assert_smoke_20260926_222047` 同样四组全过 exit 0（重复成功 2/2，样本仅证存在稳定成功路径）
  - **工件**（coord/runs/L2-1-TIM-SMOKE-1/，目录不覆盖）：`tim_assert_smoke_20260926_222429/`（run_report.json verdict=PASS、tim_projection_final.json、log_diff_tim_assert_lines.txt、import_response.json、script_stdout.log）；`tim_assert_smoke_20260926_222047/`（第二轮）；另有 3 个失败取证目录（220624 参数绑定环境失败/220853 栈拉起超时/221606+221850 脚本 bug 修复前）
  - **失败分类与止损纪律**（§8）：①run 220624 dev_agent_smoke 参数绑定失败（数组展开传参错位）——脚本工具性失败，修复后不重现；②run 220853 栈拉起"UI did not expose kernel command port within 20s"——内核冷启动超默认预算，判**环境中断**（内核进程活着且事后 5555/5556 正常监听），非功能断言失败；③run 221606/221850 脚本自身两个 PowerShell 陷阱（`-Uri`/`$Url` 参数名不匹配被静默吞 + `$Args` 自动变量）——工具性失败逐个修复；无同形态确定性失败两次，止损线未触发
  - **边界与诚实声明**：①烟测入口是自动化脚本栈（AGENTS §5），非用户手测途径；②A3 首轮过滤 `-like "*[tim.assert]*"` 方括号是通配字符类（工件混入 VSP 噪声行，判定仍正确因二级过滤无特殊字符），已改 `.Contains()` 修复并在最终轮产出干净差分——**既有 cont_stall_repro_smoke.ps1:440 同模式存在同问题，域外未动**，供决策侧知悉；③观察日志中有 VSP publish 对 127.0.0.1:8787 的连接拒绝 WARN（vsphub 无宿主订阅时的既有噪声），与本卡路径无关；④三轮失败目录未删除，供复核
  - **停止条件未触发**：assertions 块非空（25 行）装配链正常触发
- 验收：**pass（2026-09-27 决策会话）**——①接线 diff 亲核（6 行闭包注入/tim 保持 nil 静默纯函数）+分支亲跑接线三测试+tim 包绿②真栈复跑两次同形态环境失败（内核 994 条插件表冷加载超脚本 20s UI 端口预算；与执行侧 220853 同形态=环境中断归因一致；-ReuseStack 前置 agent 未起与失败点死锁=脚本改进点登记：等待预算参数化）③**工件亲核替代采信**：两轮独立 run（222429+222047）verdict=PASS；assertions 25 行三态分布诚实（12 pass/11 NE/2 fail）；fixture 轨 2957 精确触发双 fail 码（assert_level_ceiling_exceeded+possible_clipping_or_no_headroom）且入 risk_summary.primary_codes；日志差分 [tim.assert] WARN 行实锤——四断言组证据互证④§8 失败分类（3 类工具/环境，止损未触发）与边界四点诚实声明（cont_stall 通配符问题域外上交）采信。**音频编译器第一层设计→实现→真栈监督报警全链闭环**。附带登记：run_report.json UTF-8 BOM（BLIND-BOM-1 同族，工件可读性小疵）+dev_agent_smoke 20s 预算偏紧三例同形态（下次触碰参数化）。
