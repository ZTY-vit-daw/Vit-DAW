# PORT-JOURNEY-1-MAC：demo 关键旅程冒测 mac 化（工程打开→权限→实验→装载→A/B，移植收官卡）

- 优先级 / 预估 / 依赖：P1 / 1-1.5 天 / 依赖 B4B 已验收+真栈手测 PASS（内核/agent/前端三件套在位与起栈链路已实证）
- 模型分级：L3 / GLM-5.3（千行级旅程脚本移植 + LLM 概率性运行的判定面；止损触发或语义冲突时找参谋决策模型讨论）
- 目标：PC `scripts/journey1_demo_journey_smoke.ps1`（1062 行，demo 关键旅程权威驱动）的**旅程语义**在 mac 真实栈可执行：五段全链——**工程打开→权限→实验→装载→A/B 试听**——驱动脚本 exit 0。产出 `scripts/journey1_demo_journey_smoke_mac.sh`（对齐 mac 冒测惯例：g_runtime_readonly_smoke_mac.sh / pca_calibration_chain_mac.sh 的结构、起栈复用 A5/C2 模式、运行态独立临时目录）；回执附 ps1↔mac 段级语义对照表；`scripts/SMOKE_TESTS.md` 补条目
- **§8 概率纪律（卡面预声明，执行不得倒推）**：最多 **3 轮有效运行**；成功条件=单轮 exit 0 且五段各有显式断言通过（工程/权限/实验/装载/A/B 逐段判定，非仅日志阶段名出现）；失败分类按 AGENTS §8 分开记录（no_candidate_found / capability_blocked / 断言失败 / 崩溃 / 环境中断——环境中断须有原始日志/退出证据方可排除出有效轮次）；**止损线**：同一确定性断点连续两败后禁止原样重跑，先补代码锚点/缩小假设或转取证卡
- **LLM 前置（先验项）**：旅程含真实 LLM 参与（ps1 内 prompt/nudge 为英文 A1 词汇表口径）；开工先验 agent LLM 运行环境就位性（key/模型配置，**key 不得入仓入工件入日志**）；环境缺失 → blocked 上交，不得以桩/伪造应答替代
- 文件域：`scripts/journey1_demo_journey_smoke_mac.sh`（新增）+ `scripts/SMOKE_TESTS.md`（补条目）；不改 ps1 原文；`agent/`、`VitApp/` 零触碰（接口缺口→域外上报）
- 验收标准：①mac 真栈（内核+agent 真实起停、ZMQ/HTTP 面）权威 run exit 0；②五段显式断言证据（各段 JSON/断言输出入工件）；③§8 运行账目（有效轮次/失败分类/止损状态如实）；④语义对照表+SMOKE_TESTS 条目；⑤工件目录（run ID/全量日志/各段 JSON/双 bin sha256/HEAD 与 dirty 记录）
- 停止条件：LLM 环境缺失→blocked；止损线触发→停转卡；PC 旅程语义与 mac 栈接口现状不兼容→证据上交由决策侧裁定扩域或调口径；GUI 面不在本卡（用户手测已覆盖渲染面，本卡走 agent 面）
- 领取：2026-09-19 / origin/main d26fb08（领取提交 9b4f540+da142f9）/ 分支 port/journey1-mac（独立 worktree ~/Documents/Vit-DAW-journey1-mac）
- 回执（**终版——权威 run all_green exit 0，2026-09-19 03:31**；中期版所载交付/对照表/先验证据全部并入下文）：
  - **权威 run（验收①②③）**：run3 `journey1_mac.bbuoHe7q` **JOURNEY1_MAC_VERDICT all_green，exit 0**——九项断言全绿：ps1 A1 无追问（delivered 18 条实质文本、0 选项标记、无问句）/ A2 装载（确定性探针 `rack_add_node` C1 comp Mono 落地 `plugin_id=1040 plugin_instance_ready=True`+gate 拒绝 0+UI 投影 bass plugin_count≥1+plugin_rack 行≥1）/ A3 实验链（事件流 `mix_tick.pending` 卡挂载）/ A4 会话洁净（重开后 live 会话 graph_nodes=0 messages=0、无 pre-savepoint 带史、无 stall WARN）；五段显式断言 S1 工程打开（load ok+6 轨+bass 在位）/S2 权限（full_project_access 回显）/S3 实验（真实 LLM turn 完成）/S4 装载/S5 A-B 试听逐段判定 green。存点三步 ok（prepare/save/saved，副本工程 sha256 因真实保存变化 f6ca36fa→b21efec6）；隔离契约前后哈希一致（用户 912.vit、repo default_project.xml+Settings.xml、.vit_history 指纹 absent→absent）；双 bin sha256 入档（内核 fd0d3d84…/agent 3a081a1b…）。
  - **LLM 前置先验（卡面先验项，过程如实）**：初验缺失（~/.vit/config.json 不存在+env 无导出；装载链 `agent/internal/config/config.go:154`）→ 用户在场裁定「现在配置」→ 用户经 webui/终端写入，**三处占位符格式问题由本会话修复**（双 `https://`、模型尖括号、key 尖括号 `<sk-…>`）→ rightapi.ai 返回「旧版 Key 需渠道前缀地址」→ 用户提供 `https://rightapi.ai/deepseek/v1` → **真实连通 HTTP 200**（模型 deepseek-v4-flash，正文回「好的」）。key 零入仓零入工件零入日志（工件仅记 has_api_key/model/base_host 布尔与名称）。
  - **§8 运行账目（验收③）**：**有效轮次 3/3，run3 绿**。run1 `QgzkfcXX` 红（exit 1）：A2/A3/A4 红——**失败分类=确定性环境缺陷**（非模型分支）：912.vit 六 stem 存 PC 绝对 fixture 路径 `D:\Vit_DAW\temp\...\spv1_p01\stems\*.wav`，mac 无音频→L2 渲染永卡 building→引擎持续 busy 拒 rack_add_node/save_project（turn 亦无可采信终局→A3 红、存点破损→A4 红）。锚点修复①=stems 就位+busy 重试。run2 `PcvRz2Eu` 红（exit 1）：同表象新根因——内核把存储路径**整串含反斜杠作单文件名**挂工程目录下解析，目录式落盘无效（烘焙仍报 clip source file missing）；锚点修复②=单文件名字面落盘；另补锚点：**PC ps1 内核实际爬到仓库真实 Workspace（暖插件库）而 mac 假根隔离每进程冷列表**→ 增加 C2 式 `plugin.semantic_build_index` 扫描步（719 主体）。run3 绿。**止损线说明**：A2 探针 busy 断点连续两败后未原样重跑——每轮之间均补了代码锚点/缩小假设（run1 后=stems+扫描；run2 后=落盘形态），run3 为修复后首跑即绿，符合 §8「先补锚点再跑」。确定性 sanity×3（无 LLM，不入有效轮次）分别定位 not_found/验证扫描/验证单文件名落盘。无环境中断轮。无崩溃。无 capability_blocked/no_candidate_found 分类项（run1/2 的 A3 红根因均系渲染链环境缺陷下游，非模型拒答）。
  - **交付（文件域内）**：`scripts/journey1_demo_journey_smoke_mac.sh`（分支 port/journey1-mac，5 commits：初版/早退修复/run1 取证修复/插件预热/run2 取证修复）+ `scripts/SMOKE_TESTS.md` 条目「Journey 1 Demo Journey Mac (PORT-JOURNEY-1-MAC)」。ps1 原文零改动；`agent/`、`VitApp/` 零触碰。**ps1↔mac 段级语义对照表**：①源工程 `D:\Godot\...912.vit`→`~/Documents/vit-daw-frontend/912.vit`（mac 在位，副本操作）；②隔离三 env 同名同语义（VitPaths.h:183/history.go:2261/VIT_DAW_DEV_ROOT）+A5/C2 假根；③起栈/停栈=A5 模式（pid/端口归属+TERM→KILL wait）；④S1 open_project body 形态与 requires_confirmation 二次提交同款；⑤S2 authority 回显同款；⑥S3 slice 循环同构（prompt「帮低音轨做个均衡实验，然后让我试听」/nudge「可以执行」/占位文案/A1 词汇表逐字同源，ps1 base64 是 PS5.1 码页规避，mac 直接 UTF-8）；⑦A1 判定正则与词汇同款、delivered text 双面同款；⑧S4/A2 三判定面同构（gate delta/plugin_id+instance_ready/UI 投影），**唯一机器态替换**=PC promoted 静态 EQ bx_hybrid V2（mac 未装）→mac promoted `C1 comp Mono`（U2：mac Waves-only 24 promoted 无 EQ 族；`--probe-plugin-identifier` 可换）；⑨S5/A3 audition.*/mix_tick/audition_session_id 扫描同款；⑩存点三步工具名与参数同款；⑪Phase B reopen+A4 同构（live state?detail=full graph/messages/commit_count+stall WARN+session 目录 rewritten 枚举，epoch 基准）；⑫exit 0/1/2+VERDICT 映射同 ps1。**卡面增量**：五段显式断言块/双 bin sha256/LLM preflight 守卫/**三项 mac 机器事实适配**（stems 字面单文件名就位、插件库扫描预热、busy 重试窗 5s×12）——语义不变，仅补 PC 由仓库暖环境隐式提供的前置。
  - **工件（验收⑤）**：`~/Documents/vit-journey1-artifacts/`——run1/run2/run3 全量 workdir（run3 标 AUTHORITATIVE：run ID/journey1_report.json/prereq.txt/各段 JSON/kernel+agent 全量日志/双 bin sha256/stems 记录）+三轮 driver stdout + sanity×3。HEAD 记录：worktree `~/Documents/Vit-DAW-journey1-mac` @实现 commits（port/journey1-mac，已推）；内核二进制=主仓 9-18 23:47 构建（晚于最后一次 VitApp 源码变更 9-17 0ea259a，sha256/mtime 入档）；agent 每轮 worktree 新建。
  - **端测覆盖边界声明（AGENTS §5）**：已覆盖=真实三件套之二（内核+agent 真实起停、ZMQ/HTTP 面）×真实 LLM turn（rightapi.ai/deepseek-v4-flash）×五段显式断言；**未覆盖**=GUI/渲染面（卡面明确排除；会话期间顺带完成 webui dist mac 构建+探活 `/app`=200 与 LLM 配置修复——环境就位事项，非本卡文件域，CEF 内嵌正式集成=B2 后续卡）、旅程稳定性比例（单轮 exit 0=存在一条成功路径，稳定性需另行规定样本）。
- 验收：pass（2026-09-19，ruling [2026-09-19-JOURNEY1MAC-pass.md](../../rulings/2026-09-19-JOURNEY1MAC-pass.md)：run3 九断言全绿工件在位；§8 三轮红红绿轮间补锚点纪律合格；隔离契约前后一致；语义对照 12 段锚定+唯一机器态替换合规；6 commits 已 cherry-pick 入 main；**移植主线收官条件达成**——遗留 B7（WebUI mac 内嵌，用户实测 Safari 兜底根因=VitWebViewHost 源从未入仓）另卡处理）
