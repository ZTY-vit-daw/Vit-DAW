# PORT-C3：pluginprobe 原生观察宿主 mac 化 + 24 主体参数面重探针（指纹对照）

- 优先级 / 预估 / 依赖：P1 / 1-2 天 / 依赖 C2（已验收：24 主体认证晋升、白名单草稿、语义索引就位）
- 模型分级：L3 / GLM-5.3（C++ 平台等价改写 + 跨端指纹语义对照；加密原语替换方案如有多解，关键点找参谋决策模型讨论）
- 目标：`PluginProbe/native-host`（Windows-bound：`src/main.cpp` 的 windows.h/bcrypt 面向）在 mac 可构建可执行；用其对 C2 晋升的 24 个 Waves 主体跑**观察级参数面重探针**，产出每主体 ParameterSurface 指纹，与 PC 侧现存指纹逐主体对照——验证 PORT_AUDIT §1.4 的"参数元数据哈希跨平台稳定"预期；顺带确认 R4（`FingerprintPath` 拒绝 bundle 内 symlink）在 mac 实测的表现
- 文件域：`PluginProbe/`（native-host 平台等价实现）；`agent/internal/pluginprobe/`（如需宿主调用适配，列明最小化）；不改 `VitApp/Source`、不改 `scripts/`、不改 `agent/` 其余包
- 验收标准：① native-host mac 构建产物（CMake/Make exit 0）+ 24 主体参数面探针 exit 0；② 指纹对照工件：每主体 mac 指纹 + PC 侧指纹 + 一致/不一致标注，不一致者附参数面差异摘要；③ R4 symlink 遇遇记录（遇到/未遇到，遇到附原始错误）；④ 工件目录含 run ID、各段日志、指纹 JSON（对照前后两台机各一份）
- 停止条件：某主体探针崩溃或参数面解析失败 → 按类型记录证据转 blocked；指纹大面积不一致（跨平台稳定预期失效，如 >1/3 主体不一致）→ 停止并上交对照证据，由决策侧裁定口径；发现需动 `agent/` 大面接线的适配 → 域外上报流程
- 领取：2026-09-18 18:42 CST（Mac 执行流，用户直令「开工 PORT-C3」，BOOTSTRAP 口令表授权）/ origin/main `7b4f51d84d3c70b3daee3884674749391697a66f` / 分支 `port/c3-pluginprobe-mac-reprobe`（独立 worktree `/tmp/vit-c3-worktree`，PROTOCOL §3；主工作树当日被并行 B6 流占用后已归还，本流全程不触碰主工作树）
- 回执：实现 commit `8051f01`（分支 `port/c3-pluginprobe-mac-reprobe`，已推；文件域内 5 文件：`PluginProbe/native-host/` main.cpp/CMakeLists/README + `PluginProbe/reprobe/` 新增 driver+README；**`agent/` 与 `VitApp/Source` 零触碰**——Go 链路（cmd/pluginprobe→adapter→vst3host）mac 本可用，无需任何适配）。验证（本机 darwin/arm64 AppleClang 21，源在独立 worktree `/tmp/vit-c3-worktree` @6a561ed 基线；工件根 `~/Documents/vit-c3-artifacts/`）：
  - **验收①（mac 构建 + 24 主体探针 exit 0）**：cmake configure exit 0 + `pluginprobe_vst3_worker` 构建 exit 0（worker-build5.log 零 error）+ `pluginprobe_vst3_witness` GUI 目标同样构建 exit 0（.app 产出）；权威 run `pluginprobe_reprobe_mac_20260918-202010` driver exit 0，**24/24 主体参数面探针全部成功**（每主体 stable_id 参数面 + ParameterSurface + Installation 指纹；每主体 load/snapshot/unload JSON 入工件）。
  - **验收②（指纹对照）**：mac 侧 `mac_fingerprints.json` 24 主体齐备（C1 comp `eee94e12…`139 参、DeEsser `2bbfd976…`136、RDeEsser `115d9ab4…`137、Sibilance `2dff3f50…`137、PSE `da8655c1…`142、L1 `0b3c7d59…`135、L2 `ddd757d0…`138、C4 `c76f38d9…`167、C6 `ca81b102…`185、LinMB `7f3be155…`178、Smack Attack `d4f02853…`142、TransX Wide `61c802a0…`136；全部 `all_parameter_ids_stable=true`）。**[等待决策侧] PC 侧对照指纹不存在**：主仓 `git grep parameter_surface`（唯一在案=Lindell MBC `d0835c2e…` 一条，非 Waves 主体）、transfer 仓 inbox（空，remote head 已录）、本机全盘排查均无 24 主体 PC 参数面指纹；证据固化 `pc_absence_evidence.json`。`fingerprint_comparison.json` 24 主体全标 `pending_pc_reference`——**验收②按"mac 半边+缺口证据"交付，跨平台一致/不一致判定待 PC 侧用本卡双平台可编译代码（win32 面原样保留）产指纹后补**。附加强证据（同机可对照面）：worker 的 mac bundle Installation 哈希与 C2 落盘 `~/.vit` v2 store `binary_fingerprint` **22/22 覆盖主体逐字节一致**（`a3e58f3f…`，证明 C++ CommonCrypto 实现与 Go FingerprintPath 文法逐字节等价）；**跨 run 双跑（202010/202104，48 个独立 worker 进程）24/24 指纹零漂移**；**12/12 族 Mono/Stereo 变体共享同一参数面指纹**（参数面描述符捕获元数据而非声道拓扑，与文法设计一致）。
  - **验收③（R4 symlink）**：真机 bundle `find -type l` **未遇到**（count=0，`r4/bundle_symlink_scan.txt`）；行为验证红绿闭合——含 symlink 副本（cp -R 真 bundle + 注入 `Contents/stray_symlink`）load 正常（135 参照常披露）但指纹 **fail-closed 拒绝**（installation/parameter_surface 双空），原始错误经协议日志逐字采集：`installation fingerprint rejected: bundle contains symlink …/Contents/stray_symlink`（`r4/symlink_verdict.json`）；Go 侧 `FingerprintPath` fail-closed 经 `go test -overlay`（零源码树写入的一次性测试）在 mac 行为验证通过（`r4/go_fingerprint_test.log`）。
  - **验收④（工件）**：三个 run 目录 + `build-and-timing-evidence/`（configure/五轮构建日志、直连冒烟、10m19s 冷扫描计时）均含 run ID/各段日志/指纹 JSON/summary/run_meta（HEAD+分支+dirty+双 bin sha256+`~/.vit` 语义索引前后哈希断言不变）。权威 run=20260918-202010（PASS），稳定性 run=20260918-202104（PASS），201912=driver 断言形状缺陷的失败 run（保留存档；缺陷=按 worker 原始形状而非归一化形状解析 GET snapshot，已修）。
  - **失败分类（AGENTS §8）**：run1 类环境/探针路径成本——name-only 扫描路径冷启实测 10m19s（101s user/18% cpu，WaveShell 17.1 逐类探针等待）致 curl 180s 超时×24；处置=改走 worker 契约内的 `plugin_uid` 路径（README 既有设计），0.7s/主体、参数面与扫描路径一致（L1 limiter Mono 双路径各 135 参同面），计时证据入 build-and-timing-evidence。mac 首编暴露既有代码两处可移植性缺陷（`pluginFile={}` AppleClang 歧义、空文件 readIntoMemoryBlock 返回 0 误判失败——mac bundle 根部 0 字节 `Icon\r` 标记文件），最小修复已入实现 commit。
  - **Windows 回归面（构造级论证）**：全部平台差异在 `#if defined(_WIN32)` 分支内，win32 编译面唯一变化=`pluginFile = juce::File()`（语义等价显式化）、`snapshot()` 去 const（调用方均非 const）、fileHash 默认参数与失败原因外送（可移植）；bcrypt 调用序列保持原语义（SHA256 固定 32 字节替代 GetProperty 查询，规范常量）。PC 侧可用既有工程直接复验。
  - **端测覆盖边界声明（AGENTS §5）**：已覆盖=真实 native worker 进程 × 真实 WaveShell bundle 实例化加载 × 24 主体观察级参数面 + 指纹计算（Go 归一化链）+ R4 行为面；**未覆盖**=跨平台指纹对照（PC 侧指纹不存在，见②）、render 面与 witness GUI 人工目检（本卡无渲染/GUI 改动）、webui/用户旅程（无涉）。
  - **执行侧自报（推送范围事故，[等待决策侧: main 上 0a39d66 处置]）**：本卡收口推 `HEAD:main` 时，因推送前 `pull --rebase`（PROTOCOL §3 冲突规则）把实现提交 rebase 为 `0a39d66` 并连带进入 main——违反"执行侧仅卡片状态变更可直推 main"。事实核验：①`PluginProbe/` 内容 8051f01↔0a39d66 diff 为空（逐字节一致，纯 rebase 无内容变化）；②验收基准分支 `origin/port/c3-pluginprobe-mac-reprobe` @`8051f01` 完好；③无任何历史改写（main 上的 0a39d66 为新增提交）。处置选项留决策侧：裁定等值于提前 cherry-pick 保留（ruling 时只需形式收口），或 `git revert 0a39d66` 恢复"main 仅卡片变更"不变量（后续按验收结果重新合入）。执行侧不再对 main 做额外补救提交。注：C2-PCR 同窗并发完成（PC 全链 PASS、PC↔mac 24 主体名单全同），但其产出为认证链 runner 面，PC 侧 pluginprobe 参数面指纹仍不存在——验收②的 pending_pc_reference 口径不变，PC 侧可复用本卡双平台代码补产。
- 验收：pass（2026-09-18，ruling [2026-09-18-C3-pass.md](../../rulings/2026-09-18-C3-pass.md)：工件核验全过（24/24、22/22 安装一致、R4 三面、双跑零漂移）；验收②按"mac 半边+缺口证据"采认，PC 对照转后续件（12 族级指纹即可）；**推送事故裁定：main 上 0a39d66 保留**（与 8051f01 逐字节一致，等值提前 cherry-pick，diff 空已独立复核），违规事实入档、PROTOCOL §3 增补推送纪律；PC 指纹生产并入下一张 PC 卡）
