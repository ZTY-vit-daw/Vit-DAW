# PORT-PS1-SETTLE-1：PC ps1 chat settle 等待补齐（F⑤ 跑绿收口，PS1-SYNC-1 拆卡）

- 优先级 / 预估 / 依赖：P1 / 0.5 天 / 依赖 PORT-PS1-SYNC-1（已合入 main，断言已升级）
- 模型分级：L2 / GLM-5.3（chat 驱动语义对齐 + 真实栈跑绿）
- **背景（PS1-SYNC-1 ruling 裁定 1，两轮工件+日志实证）**：PC `run_vit_product_path_smoke.ps1` 的 `Invoke-AgentChat` 为单发 POST 无 settle 等待；vocal focus 回合在升级后终态路由（`ccb.observation_catalog`）下确定性超 max_turns 切片进 durable continuation（两轮 `stop_reason=limit_reached`/`completed_steps=1`/`continuation.arm durable=cont_*` 实证）。mac 侧同现象已以 `--chat-settle-seconds`（默认 300s）解决并文档化于 SMOKE_TESTS.md mac 条目；PC 体系内 settle 先例：`scripts/d1_audition_gap_smoke.ps1` 事件轮询+deadline 形。
- 目标：
  1. PC ps1 `Invoke-AgentChat`（及必要调用点）补 chat settle 等待：等待回合收敛到终态（非 `limit_reached` 切片态）或超时（默认 300s，可参数化）再返回，语义对齐 mac `--chat-settle-seconds`
  2. F⑤ 全脚本权威 run **exit 0**（vocal focus 回合含升级后别名组断言全绿）
  3. SMOKE_TESTS.md PC ⑤ 条目补 settle 语义描述
- 文件域：`scripts/run_vit_product_path_smoke.ps1`（函数级最小改动）；`scripts/SMOKE_TESTS.md`；**不动** `_mac.sh`（mac 已有 settle）与其他 ps1/探针
- 验收标准：①diff 函数级最小（不重构不动其他段）；②F⑤ 权威 run exit 0（§8 ≤3 轮、两败止损，AGENTS §8/§9 工件纪律）；③SMOKE_TESTS.md 同步；④工件（run ID/日志/退出码）
- 停止条件：settle 等待就位后 vocal focus 仍不收敛（`limit_reached` 持续）→ 取证上交（可能涉 agent 续跑/settle 语义，域外）；需改 agent/内核代码才能绿 → 立即域外上报；环境中断如实记录
- 附注（PS1-SYNC-1 ruling 备案）：A② legacy 模板耦合 `VitApp/Workspace/default_project.xml` 须含 VIT_AUDIO_SETTINGS，本卡不处理，跑 A 脚本无关
- 领取：2026-09-19 / origin/main bf61eb9941b0638a94948690073c916304fd6648 / 分支 port/ps1-settle-1（主工作树领取前已有：VitApp/Workspace/default_project.xml 烟测运行时改写〔已入账保留项〕+ extension 构建产物 + godot-cpp/ untracked，均非本卡 diff）
- 回执：2026-09-19 23:50（PC/GLM-5.3 执行侧止损上交，待决策侧裁定）
  - **实现 commit**：port/ps1-settle-1 @ 9f4cef2c（2 文件 +167/−4：`scripts/run_vit_product_path_smoke.ps1` +143/−3、`scripts/SMOKE_TESTS.md` +14/0；agent/、VitApp/、_mac.sh、其他 ps1 零触碰）。coord 领卡 commit：main @ 5070e66d。**push 暂挂**：GitHub 连接故障（Connection reset / 443 不可达），本地 commit 完好，网络恢复补推 port/ps1-settle-1。
  - **settle 实现（mac agent_chat_ctx 逐句对齐，_mac.sh:443-537）**：`Invoke-AgentChat` 尾部接线新函数 `Wait-ChatTurnSettled`——POST 响应 `goal_status≠waiting_continue` 原样返回（正常回合零开销）；切片时 deadline+while+5s 间隔轮询 `GET /agent/runtime/status` 的 `goal.status` 至终态集合（completed/failed/stopped/cancelled/waiting_confirmation/waiting_clarification），`GET /agent/events?conversation_id=&since=0&limit=500` 合成 settled 对象：stop_reason 映射（completed→done / waiting_confirmation→needs_confirmation / waiting_clarification→needs_clarification，其余保留 raw）、reply 取 events 去占位符（"我还在继续处理这个任务，完成后再向你汇报。"）末条、`raw_stop_reason`/`raw_reply`/`settled_from`/`delivered_event_count` 保留、`executed_kernel_reply` 缺失时由 events 工具行补齐、`typed_events` 缺失时补空数组（mac `.get() or []` 对齐）。参数 `-ChatSettleSeconds` 默认 300。调用点适配两处：vocal focus no-op 词表补中文诚实边界词（能力边界/无法安全/无法可靠/不能可靠/证据不足——settle 终态新形态必需，语义与 mac 英文词表等价）。端点字段事先亲核：server.go:540/543 handler、runtime.go:20-31 GoalStatus 常量、events.go:18-45 AgentEvent 结构、server.go:195-240 ChatResponse 字段。
  - **离线验证**：PowerShell Parser PARSE OK；settle 合成逻辑离线三案例全绿（非切片零开销原样返回 / 切片合成 stop_reason+reply+raw_*+executed_kernel_reply / 超时保留 raw stop_reason+goal_status 保留末次轮询值）。
  - **F⑤ 权威 run（§8/§9 纪律，跑前端口栈 5555-5557/7878/8787 核对空闲三轮均执行）**：
    - run 1 `product_path_20260919_233041`（console `coord/runs/ps1-settle-1/run1_console.log`）：前置段全绿；vocal focus 切片→settle 轮询 running→**failed**→stop_reason 保留 limit_reached→断言红 exit 1。取证：turn1 `ccb.observation_catalog` 切片 arm cont_b769…→turn2 `ccb.observation_request` 再切片 arm cont_1fbd…→turn3/4 终局 `needs_experiment`（EQ presence 实验，raw 完整落 debug jsonl）被 free_state admission gate 拒（failed_gate_ids=G4_dimension_closed/G5_frontier_established/G6_target_evidence/G8_target_consistency）→强化重试仍 needs_experiment→terminal fallback failed（`free_state_terminal_turn_unparseable`，BOUNDARY-1 §1.3 诚实回退设计行为）。
    - run 2 `product_path_20260919_233524`（run2_console.log）：vocal focus 切片→settle→**completed**（诚实能力边界声明"任务已到达明确的能力边界；没有把能力不足解释为改善完成"）→stop_reason=done 断言**通过**、observe 别名组断言通过；随后崩于 `$focus.typed_events` PropertyNotFoundStrict（切片 raw 响应 omitempty 无该字段；mac 用 `.get() or []` 容错，PC 直访不容错）→**脚本缺陷非断言红**，exit 1。已修（settle 合成补空数组+中文 no-op 词），修复后为 run 3 代码版本。
    - run 3 `product_path_20260919_233932`（run3_console.log，agent 日志 `run3_agent_last.log`）：vocal focus 切片→settle→**failed**（机制与 run 1 逐字相同：needs_experiment×2 被 gate 拒→fallback failed，`agent_message_loop_debug_thru_run3.jsonl` 三轮 vocal focus 段共 10 行留档）→断言红 exit 1。
    - **两败止损触发**：run 1/run 3 同断点（goal=failed→limit_reached 断言红）两次，禁止原样重跑。
  - **止损定性（上交决策侧）**：settle 等待本身被证明有效——三轮全部正确等到终态，run 2 走通 completed 路径的 stop_reason/别名组断言（证明脚本侧链路已通）。红的根因在域外：deepseek-v4-flash（PC 引擎，telemetry 亲核）在 vocal focus 续跑轮 2/3 概率给出结构完整的 `needs_experiment` 终局，被 free_state admission gate 按设计结构性拒绝（G4/G5/G6/G8 对两轨 fixture 场景不可满足）→诚实 fallback failed；needs_confirmation/needs_clarification/done(含 derive) 三条可采纳终局路径 PC 引擎下未采到。**不改 agent 终局门语义 / 不换引擎 / 不放宽 F⑤ 断言（failed 不在 PS1-SYNC-1 契约终态集），PC F⑤ 无法稳定 exit 0**——三选项均在卡片文件域之外，按停止条件上交。可选方向（决策侧裁定）：①换强引擎跑 F⑤（mac 绿路径同构）；②开 agent 侧卡审视 needs_experiment 在两轨 fixture 的 admission gate 可满足性；③接受 PC F⑤ 为"settle 就位+engine-dependent 终态"降级口径。
  - **工件**：`coord/runs/ps1-settle-1/`（run1/2/3 console log、run3_agent_last.log、裁剪版 message_loop_debug jsonl）+ `VitApp/Workspace/Artifacts/smoke/product_path_20260919_{233041,233524,233932}/`（脚本工件，含各轮 chat_vocal_focus.json settled 对象）。烟测遗留：VitApp/Workspace/default_project.xml 本卡运行时改写（既有已知项，不提交不还原）。
  - **SMOKE_TESTS.md**：PC ⑤ 条目 Purpose 补 chat settle waiting 语义（参数/端点/raw_* 保留/零开销）、Notes 补 `-ChatSettleSeconds`。
  - 卡片处置：验收②F⑤ exit 0 未达成（止损上交路径），**留 doing 待决策侧裁定**，未 mv done。
- 验收：**pass（交付面，settle 驱动交付）**（2026-09-19 PC 决策侧，[rulings/2026-09-19-PS1SETTLE1-pass.md](../../rulings/2026-09-19-PS1SETTLE1-pass.md)）——三轮实证 settle 有效（run2 completed 路径断言全过=链路已通）；F⑤ exit 0 域外阻塞（deepseek needs_experiment×2/3 被 admission gate 结构性拒→诚实 failed），止损纪律合格；run1 console 尾码 0 矛盾已入 ruling 注记（前置版本退出码传播缺陷，现行版本 run3 实证失败→1）。实现已 cherry-pick 入 main。F⑤ 收口方向=用户裁定【待回填】
