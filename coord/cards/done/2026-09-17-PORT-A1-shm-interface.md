# PORT-A1：ISharedMemorySegment 接口抽象 + POSIX mmap 实现（内核 shm 发布侧）

- 优先级 / 预估 / 依赖：P1 / 2-3 天（PORT_AUDIT 层 A）/ **依赖 A3**（POSIX 实现需 mac 编译验证）
- 模型分级：L3 / GLM-5.3；接口方案（命名/世代/平台映射）有疑义先上交参谋讨论，不自行定契
- 目标：内核共享内存发布抽象为 `ISharedMemorySegment` 接口；Windows 实现 = 现 OpenFileMapping/CreateFileMapping 语义搬迁；新增 POSIX 实现（shm_open + ftruncate + mmap + munmap/close）；3 使用点改写：`VitApp/Source/Service/WaveformEnvelopeBaker.cpp`、`TiledSpectrogramBaker.cpp`、`SharedMemoryTester.cpp`
- **硬约束（A2 验收采纳，违反 = 返工，见 rulings/2026-09-17-A2-pass.md）**：
  1. macOS shm 段名**含前导 `/` 上限 31 字符**（PSHMNAMLEN）——现命名 `Vit_AudioFeature_waveform_<bakekey>_g<gen>_<tile>` 光前缀即 27 字符必然超限；mac 发布名须短前缀或哈希，**≤30 正文字符**
  2. shm 存储按 16KiB 粒度向上取整——`ftruncate` 一次到位即可，尾部空间浪费可接受，勿反复截断（第二次返回 EINVAL）
  3. Go 侧读取契约已冻结（agent 已验收，`shm_darwin.go` posixShmName 仅补前导斜杠）：Windows 用原名、mac 用规约后短名，**两侧发布名映射关系必须显式**（接口内注释 + 回执说明）
- 文件域：`VitApp/Source/Service/SharedMemory*`（新接口 + 双平台实现 + 3 使用点）；涉 `VitApp/Tests/**` 列明；不改 `agent/**`
- 验收标准：① Windows 行为逐项对照零回退（段创建/写入/世代更替/清理路径）；② mac 编译过（A3 就位）；③ SharedMemoryTester 在 mac 真机跑通一次发布 → Go 侧 `shm_darwin` 读回的**跨进程真实验证**（run ID、命令、工件目录入回执）；④ 段名方案与映射关系记录在案
- 停止条件：接口设计需要改 Go 侧读取契约 → 停（契约冻结）；对照中发现 Windows 侧语义本身缺陷 → blocked 上交
- 领取：2026-09-17 晚（Mac 执行流，用户直令「开工 A1」，BOOTSTRAP 口令表授权）/ origin/main `57ee038cde78ab851259113319e79b0c5c832f0b`（工作树干净）/ 分支 `port/a1-shm-interface`
- 回执：实现 commit `25617af`（分支 `port/a1-shm-interface`，已推送待验收合并；10 文件 +527/−157，全部在卡面文件域：`SharedMemorySegment.h`+Windows/Posix 双实现+3 使用点改写、`VitApp/CMakeLists.txt`、`VitApp/Tests/SharedMemorySegmentTests.cpp`+Tests CMake；**agent/** 零触碰**）。验证（本机 darwin/arm64，构建目录 `/tmp/vit-a1-build/*` 与工件 `/tmp/vit-a1-artifacts/*` 独立临时目录，AGENTS §10；源在隔离 worktree `/tmp/vit-a1-worktree` @`25617af`，保留供决策侧复验）：
  - **验收②（mac 编译）** VitApp：`cmake -S <worktree>/VitApp -B /tmp/vit-a1-build/vitapp -G "Unix Makefiles" -DCMAKE_BUILD_TYPE=Debug -DVIT_TRACKTION_ENGINE_DIR=<主仓submodule b439749>` exit 0（45.4s）+ `make -j8 VitApp` exit 0，产物 Mach-O 64-bit arm64 106MB（sha256 `ae9b1048…7b48dc`）；Tests：configure exit 0（42.1s）+ `make -j8` 全目标 exit 0；**5/5 测试运行 exit 0**（State/PersistPolicy/**VitSharedMemorySegmentTests 新增**/AudioPlane/RenderWatchdog，后两者为真实 tracktion Engine headless 运行）。VitSharedMemorySegmentTests 覆盖：段名规约（短名直用/长名 VAF_ 摘要/确定性/互异）+ 真实段 create→write→读回→unmapView→释放→同名重建（小载荷 + 24576B 波形 tile 尺度载荷）。
  - **验收③（跨进程真实验证）** run ID `a1-xcheck-20260917-192247`，工件 `/tmp/vit-a1-artifacts/a1-xcheck-20260917-192247/`：以 `VIT_ENABLE_SHARED_MEMORY_TEST=1` 拉起**真实 VitApp 内核进程**（pid 1957，Main.cpp:40-47 既定 Tester 路径）→ Go 侧**冻结读链路**（A2 已验收 shm_darwin，本次零改动）读回：`go test ./internal/harness -run TestA1CrossProcessKernelSegmentReadback -count=1 -v` exit 0 + `./internal/vsphub` 同构 exit 0（期望 10 浮点 1.1..10.0 逐位相等；跨进程 shm_open O_RDONLY+fstat+mmap 全真实系统调用）；lsof 证据 `PSXSHM 16384 /Vit_Waveform_Test`——40B 逻辑请求发布侧实证 16KiB 取整（硬约束②镜像确认）。验证用 Go 临时文件写在隔离 worktree、用后即删、**未入任何 commit**。
  - **验收①（Windows 零回退对照，自检面）** Windows 实现=逐 API 语义搬迁：CreateFileMappingA（page-file/PAGE_READWRITE/同名同尺寸，发布名经 platformPublishedName 恒等）→ MapViewOfFile(FILE_MAP_ALL_ACCESS, bytes) → memcpy 全量（spectro 保留前置 memset）→ 写后质检读回 → UnmapViewOfFile 保活 → 句柄入 gHandles → 世代失效拒绝=立即 CloseHandle+终止 bake → 世代更替退休宽限（120s/5s）到期 CloseHandle；payload `shared_memory`/`shm_bytes`/`handle_count` 字段与语义不变。已知差异仅诊断面：create/map 两行失败日志合并为 `segment_create_failed`+`detail=`（detail 保留原阶段标签与 win_error 码）、spectro `store_handle*` 日志键更名 `store_segment*`、`SIZE_T`→`size_t`（64 位同宽度）。**PC 复编译确认归决策侧**（同 A3 口径）。
  - **验收④（段名方案记录）** 映射关系显式固化于 `SharedMemorySegment.h` 头注释 + 接口单测断言：Windows 发布原名（恒等）；POSIX 正文 ≤30 且无 `/` → 原名直用（Tester `Vit_Waveform_Test` 17 字符即此形态）；超限 → `VAF_`+FNV-1a-64 十六进制 16 字符（正文 20、含前导 `/` 21 ≤31，硬约束①），确定性 1:1，两 Baker 描述名（前缀 27/13 字符）必然落入摘要形态。POSIX 段生命周期：析构=munmap+close+**shm_unlink**（新读者 ENOENT、已打开读者不受扰——对齐 Windows 最后 CloseHandle 段消失）；stale 段（崩溃遗留）ftruncate EINVAL（A2 实证）→ unlink+重建一次自愈；同权重发全量覆盖、无正确性风险。
  - **平台语义发现（供 A5/决策侧）** ①SIGTERM 下内核无优雅退出（实测 exit 143，JUCE shutdown 不触发）→ releaseTestMemory 不执行 → POSIX shm 段存留至重启/覆盖；内核 CommandDispatcher/VitHeadlessService 无 shutdown 命令面（grep 无果）——真实栈的内核停止方式与优雅退出归 A5 定义；②macOS POSIX shm 不落 `/private/var/tmp` 可见文件（伪文件系统，lsof PSXSHM 可见），跨机核验须用 lsof/读链路而非目录列举。
  - **并发事故记录（协作层须知）** 领取提交 `7ceff63` 因**另一会话在同一主工作树并行领取 A4**（其 claim `2baabd2` 先落 main、其 checkout port/a4 致我的提交落错分支、我随后 checkout 又抢走共享 HEAD）——已无损失修复：a4 分支还原 `2baabd2`、main fast-forward 至 `7ceff63`、主树 checkout 交还、本卡全部工作转隔离 worktree `/tmp/vit-a1-worktree` 完成。**建议**：同机多执行流并存时各用独立 git worktree（协议未禁止并行流，但主工作树单 HEAD 是客观踩踏面）。
  - 端测覆盖边界声明（AGENTS §5）：本卡验证面=内核 darwin 编译链接 + Tests 运行 + **内核发布↔Go 冻结读回跨进程**；未含：真实音频 bake 全旅程（audio_feature_data_ready 经 ZMQ 至 agent 材料化；baker 发布块与 Tester 共用同一接口与 POSIX 实现，接口级已覆盖）、Windows 真机复验——前者归 A5 真栈卡，后者归决策侧 PC 复编译。
- 验收：
