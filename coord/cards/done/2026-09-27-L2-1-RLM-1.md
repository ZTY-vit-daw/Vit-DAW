# L2-1-RLM-1：交付 profile 参数化 v1——参数向量+内置 profile+一 render 一 profile（数据结构层）

- 优先级 / 预估 / 依赖：P1 / 0.5 天 / 路线图 D9（AGENTIC-OBSERVATION 蓝图）；与 L2-1-TIM 线并行零交叉
- 模型分级：L2 / GLM 亲自或强督导（RLM 是现行生产投影，改结构须保旧记录兼容）
- **执行侧（PC 会话，agent/internal/rlm 域）**
- 背景：路线图 D9——各行业标准=参数向量（integrated LUFS / short-term max / momentary max / true peak / 声道配置）+各特殊规则；一个 render 挂一个 profile（响度标准是目标带，"按最严的做自动满足其他"不成立）。
- 目标（v1 范围钉死=**数据结构+选择/校验层，不接渲染链**）：
  1. **profile 数据结构**：参数向量定义（字段+单位+区间+带语义：target band 而非单向门槛）+ 特殊规则扩展位（如 headroom 策略标记）；序列化格式（旧状态缺省语义：无 profile 字段=现行为不变，fail-closed 未知枚举——§11 纪律）。
  2. **内置 profile 集（4-6 个）**：Apple Music / Spotify / AES streaming / EBU R128 / 广电（数值按公开标准落，来源注记）。
  3. **选择与校验**：render 级 profile 绑定接口（一 render 一 profile，多交付物=分支管理不属本卡）+ 参数合法性校验（带内一致性：short-term max ≤ momentary max 等）。
  4. **红绿测试**：修前红=profile 结构/校验/往返序列化断言（新测试，无旧断言可红则红=构造非法 profile 必须被拒+旧记录无 profile 加载不炸）；旧 RLM 测试全量零回退。
- 约束：零渲染链改动（计量/监督挂接=后续卡）；agent 全量 84 包 0 FAIL；旧记录兼容测试至少一份真实旧工件往返。
- 验收：①结构+5 个内置 profile+校验落地红绿 ②旧记录往返测试 ③agent 全量 exit 0 ④回执带 HEAD+diff stat
- 停止条件：RLM 现结构与 profile 向量语义冲突（发现旧字段语义不可兼容）→ 停止实现，冲突实证上交决策侧裁定
- 领取：2026-09-26 PC 执行侧（GLM-5.3 / L2 亲自）。领取时 HEAD=3faf7093；`git status --short`：` M VitApp/Workspace/default_project.xml`（运行时工程状态，不动）+ 12 个 untracked `coord/runs/{FIX-PCA-AUTOSWEEP-1,FIX-PCA-EQCHANNEL-1,PORT-WIN-HYGIENE-SMOKE-1}*` 工件目录（领取前已有，未触碰）。领取时先跑基线：`go test ./...` exit 0，84 包 ok / 0 FAIL。
- 回执（2026-09-26，PC 执行侧）：
  - **产物**（文件域 `agent/internal/rlm/`，零渲染链改动）：
    - `profile.go`（新，324 行）：①`DeliveryProfile` 参数向量——`LoudnessBand`（target band 语义：Max 必填=带上限，Min 可空=单侧门槛如"≤−16 LUFS"类平台规则）+ integrated band / short-term max / momentary max / true peak dBTP / 声道配置（mono/stereo/surround_5_1 枚举）+ headroom 策略扩展位（v1 仅 `true_peak_bound`，空=未指定）；short/momentary 字段存在但内置集全留空——五标准均未设节目级窗上限，不发明数值。②内置 5 profile（数值经 2026-09-26 web 逐一核实，Source 字段注记来源）：`builtin:ebu_r128`[−23.5,−22.5]LUFS/−1dBTP、`builtin:aes_td1004`[−20,−16]/−1、`builtin:apple_music`(无下界,−16]/−1、`builtin:spotify`(无下界,−14]/−1、`builtin:gy_282_2014`[−26,−22]/−2。**勘误**：广电标准号核实为 **GY/T 282-2014**（−24 LKFS ±2 LU、TP ≤−2 dBTP，对齐 ITU-R BS.1864）；GY/T 268.1-2013 是数字音频广播信道标准与响度无关，卡片起草时无标准号，本回执落定。③校验 `ValidateDeliveryProfile`：band min≤max、short-term max≤momentary max（3s 窗均不可能超 400ms 窗均最大值）、TP≤0 dBFS、枚举成员、全数值字段 NaN/Inf 拒绝。④绑定 `RenderProfileBinding`+`RenderBindingIndex`：一 render 一 profile（同 render 换 profile 必拒、同 profile 幂等；多交付物=分支，不属本卡）；v1 绑定只解析内置 id（自定义 profile 注册=后续卡）。⑤序列化 fail-closed：`ParseDeliveryProfile`/`ParseRenderProfileBinding` 未知 schema_version、未知枚举、非法向量一律报错。
    - `types.go`（+5 行）：`Projection` 增 `DeliveryProfile *DeliveryProfile json:"delivery_profile,omitempty"` 可选槽；**Build 不填**（零渲染链改动、零现行为改动），nil 时 JSON 逐字节不变。
    - `profile_test.go`（新，343 行）：5 个新测试。
    - `testdata/real_old_c1_project_observe.json.gz`（新，63KB）：真实旧工件快照（见下）。
  - **红绿**：红=测试先行，修前 `go test ./internal/rlm -run '<5 个新测试>'` exit 1（build failed：undefined: BuiltinDeliveryProfiles/ValidateDeliveryProfile/LookupDeliveryProfile/ChannelStereo/RenderBindingIndex… 12+ 符号——新 API 无旧断言可红，按卡面定义红=非法 profile 必拒+旧记录无 profile 加载不炸两族断言，修前不可编译即红）；绿=实现后 rlm 包 15 测试全 PASS（5 新 + 10 旧零回退）。
  - **旧记录往返（验收②）**：真实旧工件=`VitApp/Workspace/Artifacts/c1_saved_project_state_diag.json`（2026-07-31、17MB、sha256 `3358de4b…`；该目录被 `.git/info/exclude` 排除且含用户对话历史，整件不入库）→ 只读提取 `result.project_observe`（71 条真实轨道）→ minified 内容 sha256 `fbe495a7…` → gzip(9) 入 testdata；测试内 pin 该哈希防 fixture 漂移。测试断言：真实数据喂 `Build` 不炸且 Rows 非空；产物 marshal 无 `delivery_profile` 键；旧形状记录 Unmarshal 后 `DeliveryProfile==nil`、ProjectionID/Status 不变、`ContextProjectionMap` DeepEqual；另加合成最小旧记录加载不炸。提取链（原件哈希→内容哈希→gz）记于测试注释与本回执。
  - **全量（验收③）**：`cd agent && go test ./... -count=1` → **exit 0，84 包 ok，0 FAIL**，与领取时基线一致。`go build ./...` 通过、`go vet ./internal/rlm` 干净、`gofmt -l internal/rlm/` 为空。
  - **diff stat（本卡新增，vs 领取时）**：`agent/internal/rlm/types.go | 5 +++++`；新文件 profile.go 324 行 / profile_test.go 343 行 / testdata/real_old_c1_project_observe.json.gz 63KB（`git status agent/` = 1 M + 3 ??，领取前已有改动未触碰）。
  - **端测边界声明（AGENTS §5）**：本卡纯数据结构层，`Build`/`ContextProjectionMap` 行为零改动（唯一调用方 capabilitycontext 在全量测试中绿）；未跑真实栈烟测——是否需补由决策侧裁定。
  - **停止条件未触发**：RLM 现结构与 profile 向量无语义冲突（Projection 仅增可选 nil 字段）。
  - **未提交**：按 AGENTS §12（验收通过后形成 commit），工作树改动留待决策侧验收后收口；卡片已移 `done/`。
- 验收：**pass（2026-09-27 决策会话）**——①types.go +5 行亲核（纯可选槽/Build 不填/nil 时 JSON 逐字节不变=§11 零现行为改动）②profile.go 亲读：fail-closed 语义遍布（schema_version/未知枚举/未知绑定 ID/非法向量全拒）③我方复跑 rlm 包 PASS+vet 干净+**全量 84 包 ok/13 no-test-files/0 FAIL 精确一致**（首次 grep 计数 83 系口径差，awk 分类复核实为 84 ok）④内置 profile 数值决策侧抽核（R128 −23±0.5→[−23.5,−22.5] ✓、GY/T 282-2014 −24 LKFS±2→[−26,−22]+TP−2 对齐 BS.1864 ✓——标准号勘误采信）⑤真实旧工件往返（17MB 原件不入库+内容哈希 pin 防 fixture 漂移+提取链在案）合规；short/momentary 内置集留空不发明数值的克制采信；端测边界（纯数据结构层零行为改动）按 §5 采纳无需真栈烟测。
