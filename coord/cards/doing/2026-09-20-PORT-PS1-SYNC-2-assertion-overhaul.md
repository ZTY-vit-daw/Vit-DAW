# PORT-PS1-SYNC-2：断言大修（⑤ schema 族 + needle 扩容 + settle 窗口），吸收 SETTLE-2

- 优先级 / 预估 / 依赖：P1 / 1 天 / 无（与 PORT-SMOKE-MAC-3 跨机并行）
- 模型分级：L2 / GLM-5.3（断言语义对实际 schema 的重设计 + 双端同步）
- 背景（三源汇流）：①SMOKE-MAC-2 取证——⑤ kernel-prepared/feature_snapshot 断言族**双端从未真正执行过**（PC 绿轮走 else 分支），与实际快照 schema 三层错配（rows=每 request 历史轨迹行、全称 ready vs per-track ready、DAD L3 行 provenance 族无 request_id）；②2026-09-20 决策侧 pro A/B 实验——**flash 短探针 15/15 能力充足，"换强引擎"前提被推翻**（pro 无能力溢价仅 6 倍延迟；配置已留 flash），④⑥⑦-product-path 的真实挡路者=needle 词面窄（"执行/继续"两词，PC 原版设计，ps1:571-578 与 mac 同款）+ settle 窗口；③SETTLE-2（强引擎重跑）前提失效，本卡吸收其 F⑤ 跑绿目标，SETTLE-2 作废。
- 目标（四段）：
  1. **⑤ 断言族重设计（双端）**：对 `run_live_material_observation_smoke.ps1` + `_mac.sh` 的 kernel-prepared/feature_snapshot 断言块做整块静态审读（对照实际快照工件：SMOKE-MAC-1/2 两代工件在 `~/Documents/vit-smoke-mac1-artifacts/`、`~/Documents/vit-smoke-mac2-artifacts/`，mac 侧 per-track ready 语义修复已先行验证推进方向）——按实际 schema 一次修齐（per-track ready、provenance 族字段断言、历史轨迹行语义），ps1 与 _mac.sh 同步
  2. **needle 扩容（双端）**：observe 回复确认请求 needle 从 {执行, 继续} 扩为语义组 {执行, 继续, 确认}（命中"确认"即视为确认请求措辞——覆盖"先等你确认/请确认/待确认"形态）；涉及 `run_mix_single_tick_e2e` 与 `run_vit_product_path_smoke` 的 ps1+_mac.sh 四处 needle 定义；判据保持"needle 命中或 L3-incomplete 只读分支"结构
  3. **settle 窗口升级**：PC ps1 `Invoke-AgentChat` 的 settle 默认 300s → **720s**（SETTLE-1 实现处）；mac `_mac.sh` `--chat-settle-seconds` 默认同步 720——理由入注释（推理型模型重回合偶超 300s，实测 720 稳）
  4. **PC 跑绿**：⑤⑥④ 三件 ps1 各权威 run exit 0（吸收 SETTLE-2 的 F⑤ 目标：vocal focus 轮在扩容 needle+720s 窗下到达 admissible 终态）——flash 引擎口径（不换模型）
- §8 纪律：每件 ≤3 轮、同断点两败止损取证上交
- 文件域：`scripts/`（⑤⑥④ 的 ps1 与 _mac.sh 各处 needle/断言/默认值 + SMOKE_TESTS.md 相应描述）；`agent/`、`VitApp/`、前端仓零触碰
- 验收标准：①⑤ 断言块重设计 diff（含静态审读笔记：新断言↔实际 schema 字段对照表）；②needle 扩容 diff（双端四文件）；③settle 默认值变更 diff；④PC ⑤⑥④ 三件 exit 0 工件（run ID/日志）；⑤SMOKE_TESTS.md 同步
- 停止条件：⑥ vocal focus 在新 needle+窗口下仍两败同断点 → 取证上交（届时红因排除脚本面后指向 agent 续跑语义，另开 agent 侧卡）；⑤ 新断言与 schema 仍有未预见层 → 逐层记录按轮推进；需改 agent/内核 → 域外上报
- 领取：2026-09-20 18:15 / PC 执行侧（GLM-5.3） / origin/main=4826248379367db4a9bb99b11178e2740a1fd27d / 分支 port/ps1-sync-2 / 领取时工作树：M VitApp/Workspace/default_project.xml（烟测已知保留项）、D coord/cards/doing/2026-09-19-PORT-PS1-SETTLE-1-chat-settle-driver.md（决策侧遗留，不代提交）、?? coord/runs/ + extension 构建产物 + godot-cpp/（均域外不动）
- 回执：2026-09-20 19:55 / 分支 port/ps1-sync-2（实现 1f424b5c + 85e2936f，已推）；coord 领取 585cc3f4 已推 main。结果：⑥ exit 0 ✓；④⑤ 止损取证上交（红因=agent 改善性提案/free-state 语义分支，脚本面修复已穷尽）。卡留 doing/ 待决策侧裁定（未全绿不自动 done）。
  - 验收①（⑤ 断言族重设计）：ps1 `Assert-KernelPreparedTrackWaveformRows` 重写为 per-track ready（与 _mac.sh 修复 1:1），`Assert-AcousticReadinessReasons` 传入 ExpectedTrackIDs（调用点 :1263）。schema 对照（PC 实测工件 20260919_211727）：envelope rows=每 request 历史轨迹行（PC 该轮 4 行全 ready；mac 工件曾现 [partial 1/20, ready 20/20, missing]）→ 新断言=每 expected track ≥1 ready 行 + 该行 track_id/clip_id/request_id/source_revision 非空，无 ready 行时输出全部行 shape（track/status/tiles）；latest_request kernel_prepared 判定实测命中（kernel_prepared_spectral_field_1026/status=materialized）；bridge 行断言（request_id 一致性+非 ready 需 reason+spectrogram 源族）不变且与 _mac.sh 逐字同构。_mac.sh 侧零改动（已是目标语义）。
  - 验收②（needle 扩容）：mix_single_tick ps1:571 区 + _mac.sh:95/:717 区扩为 {执行,继续,确认}（确认=0x786E,0x8BA4 码点写法，判据结构不变）。**前提修正**：卡面"四处 needle 定义"失准——product path 双端（ps1/_mac.sh）无 {执行,继续} needle 对（证据：静态 grep 无命中 + `git log -S "executeNeedle"` 两文件零历史；其 observe 判据=结构性只读断言 stop_reason=done+零 mix_tick.pending+needs_confirmation=false，确认类措辞命中在该件反而是 FAIL 语义）。故扩容实落 2 文件，product path 不适用。
  - 验收③（settle 720）：product path ps1:55（300→720 含理由注释）+ _mac.sh:78（含 help 文本）同步。追加（run1 红证驱动）：④ mix_single_tick ps1 原无 settle 锚——observe 原始 limit_reached 直接断言红 → 移植 SETTLE-1 锚（-ChatSettleSeconds 默认 720，触发=goal_status=waiting_continue 或 stop_reason=limit_reached）；双端 settle 合成补全 waiting_confirmation 面（needs_confirmation=true + 从 runtime status continuations[].pending_interaction 重建 PendingCandidate typed event；mac product-path 先行设计移植 PC 双 ps1 + mix_single_tick_mac 补齐；PC 侧修两个 PS5.1 StrictMode 陷阱：合成行 pscustomobject、基列表 null 过滤；④ ps1 加 UTF-8 BOM 使含 CJK 代码确定性读取）。
  - 验收④（PC 跑绿）：⑥ **exit 0**（run `live_material_observation_20260920_182336`，observation obs_20260920T102411_51f19def3348，4/4 轨 ready；kernel-prepared 分支本轮真实执行——修复了"PC 绿轮走 else 分支"的历史缺口）。④ 3 轮红止损；⑤ 4 轮红止损。flash 口径全程未换模型（deepseek-v4-flash，~/.vit/config.json 留存）。
  - ④ 止损链：run1=缺 settle 锚（脚本面，已修，后续轮不再现）；run2/run3=**两败同断点**——flash 将 observe 消息路由进自由态实验轨迹（trajectory 实验回合/假设/轮）→ `improvement_proposal_confirmation_required` 停车，"[mix.tick.pending] stored" 全程 0 条（agent_last.log 证实；run2 复用栈、run3 全新栈同形）。手动探针补证（conv mix_single_tick_e2e_20260920_182906）：确认①→ `improvement_proposal_native_tool_confirmation_required`（workflow=mix_tick，"Track 1010 电平 -1dB 待确认"）；确认②→ **已应用并回读验证**（-1dB 回读 0→-1）+ 终态 `d1_post_action_evaluation_required`（workflow=free_state_d1_s1）。即双跳确认+d1 终态 ≠ 经典单跳 mix_tick_applied_reobserved。
  - ⑤ 止损链：run1=settle 合成缺 needs_confirmation（脚本面，修）；run2/run3=本人合成代码两确定性陷阱（OrderedDictionary 点访问 StrictMode 崩、@($null) 单元素数组——已修，非语义红）；run4=语义终点红：vocal focus 轮 settle 至 waiting_confirmation（needs_confirmation 合成 ✓ + PendingCandidate 重建 ✓ 两断言过），终断言"pending route 含 mix.derive"红——route 仅 ccb.observation_catalog；agent 日志证该轮终态 stop=improvement_proposal_confirmation_required（18:38:21）。④ 探针旁证 derive/apply 发生在确认后（提案停车时不执行）。
  - 裁定建议：卡面停止条件"红因排除脚本面后指向 agent 续跑语义"成立 → 建议另开 agent 侧卡，对接点三选一（决策侧定）：a) 改善性提案工作流补 mix.tick.pending 事件/单跳确认/提案时 derive 投影；b) 烟测断言族按新工作流重设计（改 ④⑤ 语义，超出本卡授权）；c) 确认这是自由态演进的预期行为，④⑤ 烟测转 agent 侧改造配套卡。
  - 工件与 §9：⑥ …/smoke/live_material_observation_20260920_182327↛**182336**/；⑤ …/smoke/product_path_20260920_{183703,184528,184741,184954}/（含各轮 chat_vocal_focus.json settled 响应链）；全轮 console 日志 coord/runs/PORT-PS1-SYNC-2/（7 份，未提交）。内核=VitApp/build/VitApp_artefacts/Release/VitApp.exe（2026-09-12 18:48，与 09-19 参照 run 同款；本卡 agent/内核零改动）；测试 HEAD=585cc3f4+当轮 scripts 工作树。
  - 验收⑤（SMOKE_TESTS.md）：已同步八处（PC④ needle 组+settle 锚、PC⑤ settle 720 两处、PC⑥ per-track ready、mac④ needle 组+mac⑥ per-track ready、mac⑤ 720）。
  - 纪律记录：域外零触碰（agent/、VitApp/、前端仓只读取证）；default_project.xml 烟测改写未提交未还原（已知保留项）；远端 main 并行推进（b15b35b2 mac 领卡），本地 coord 提交经软重置重做避让、未动他人改动；SETTLE-1 doing 卡删除（决策侧遗留）未代提交；探针回合属烟测栈内对话（conv mix_single_tick_e2e_*），无域外副作用。
- 验收：
