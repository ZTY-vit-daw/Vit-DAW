# REFSCHEMA-L0-1：统一 Ref Schema L0 落地——文法+前缀注册表+解析器（agent 侧纯新增）

- 优先级 / 预估 / 依赖：P0 / 0.5 天 / **G1 裁决定版 `coord/decisions/2026-09-27-g1-ref-schema-ruling.md` 为唯一规格**（L0 文法要点节）；L1-1-RECON 报告为注册表初值来源
- 模型分级：L2 / GLM 亲自或强督导（全库引用格式的地基件）
- **执行侧（mac 会话，夜池）**
- 目标：
  1. **红先行**：文法/注册表/解析器的表驱动测试（非法 ref 拒绝/转义往返/三态解析）。
  2. **实现**（agent 侧纯新增包或 agentprotocol 内文件域，零既有行为改动）：①L0 文法（ruling 的 BNF 基线：`vit://kind/scope[/window]@snapshot[#hash]`，段内百分号转义，`t=all`/`t=s..e`，`#-`=显式未 CAS 化）②前缀注册表（agentprotocol 常量表；初值=L1-1 报告 §2 A 类四变体 kind+G 类前缀族，legacy 格式字面量→类属→目标格式翻译条目）③解析器三态 `parsed/legacy/opaque`（opaque=未收录 WARN 一次+透传——WARN 用 logx，注意tim.AssertWarnLogger 同款注入模式参考）④序列化函数（结构化→L0 字符串）。
  3. **消费面零改动证明**：`EvidenceRefs []string` 全部消费点不动——本卡只落"生产工具"，不接线任何生成点（接线=后续各组件卡顺手迁移，D/G 类排程见 ruling）。
  4. **门**：agent 全量 84 包 0 FAIL+新测试全绿+vet/gofmt。
  5. 回执：红绿+diff stat+注册表初值清单（收录条目数）+HEAD。
- 约束：零既有生成点改动（纯新增工具层）；注册表初值必须逐条对照 L1-1 报告锚点（不凭记忆收录）。
- 验收：红绿+全量 0 FAIL+注册表初值可核对+零消费面触碰
- 停止条件：实现中发现 ruling 文法有内在矛盾（如转义规则不闭合）→ 停止上交（裁决修订而非执行侧改规格）
- 领取：2026-09-26 15:57 UTC / f307f2becbd6d3b053ad64a1155c1e19e693dc80（origin/main）/ port/refschema-l0-1
- 回执：（待回填）
