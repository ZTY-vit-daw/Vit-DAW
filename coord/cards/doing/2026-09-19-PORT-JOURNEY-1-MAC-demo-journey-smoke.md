# PORT-JOURNEY-1-MAC：demo 关键旅程冒测 mac 化（工程打开→权限→实验→装载→A/B，移植收官卡）

- 优先级 / 预估 / 依赖：P1 / 1-1.5 天 / 依赖 B4B 已验收+真栈手测 PASS（内核/agent/前端三件套在位与起栈链路已实证）
- 模型分级：L3 / GLM-5.3（千行级旅程脚本移植 + LLM 概率性运行的判定面；止损触发或语义冲突时找参谋决策模型讨论）
- 目标：PC `scripts/journey1_demo_journey_smoke.ps1`（1062 行，demo 关键旅程权威驱动）的**旅程语义**在 mac 真实栈可执行：五段全链——**工程打开→权限→实验→装载→A/B 试听**——驱动脚本 exit 0。产出 `scripts/journey1_demo_journey_smoke_mac.sh`（对齐 mac 冒测惯例：g_runtime_readonly_smoke_mac.sh / pca_calibration_chain_mac.sh 的结构、起栈复用 A5/C2 模式、运行态独立临时目录）；回执附 ps1↔mac 段级语义对照表；`scripts/SMOKE_TESTS.md` 补条目
- **§8 概率纪律（卡面预声明，执行不得倒推）**：最多 **3 轮有效运行**；成功条件=单轮 exit 0 且五段各有显式断言通过（工程/权限/实验/装载/A/B 逐段判定，非仅日志阶段名出现）；失败分类按 AGENTS §8 分开记录（no_candidate_found / capability_blocked / 断言失败 / 崩溃 / 环境中断——环境中断须有原始日志/退出证据方可排除出有效轮次）；**止损线**：同一确定性断点连续两败后禁止原样重跑，先补代码锚点/缩小假设或转取证卡
- **LLM 前置（先验项）**：旅程含真实 LLM 参与（ps1 内 prompt/nudge 为英文 A1 词汇表口径）；开工先验 agent LLM 运行环境就位性（key/模型配置，**key 不得入仓入工件入日志**）；环境缺失 → blocked 上交，不得以桩/伪造应答替代
- 文件域：`scripts/journey1_demo_journey_smoke_mac.sh`（新增）+ `scripts/SMOKE_TESTS.md`（补条目）；不改 ps1 原文；`agent/`、`VitApp/` 零触碰（接口缺口→域外上报）
- 验收标准：①mac 真栈（内核+agent 真实起停、ZMQ/HTTP 面）权威 run exit 0；②五段显式断言证据（各段 JSON/断言输出入工件）；③§8 运行账目（有效轮次/失败分类/止损状态如实）；④语义对照表+SMOKE_TESTS 条目；⑤工件目录（run ID/全量日志/各段 JSON/双 bin sha256/HEAD 与 dirty 记录）
- 停止条件：LLM 环境缺失→blocked；止损线触发→停转卡；PC 旅程语义与 mac 栈接口现状不兼容→证据上交由决策侧裁定扩域或调口径；GUI 面不在本卡（用户手测已覆盖渲染面，本卡走 agent 面）
- 领取：2026-09-19 / origin/main d26fb08（领取提交 9b4f540+da142f9）/ 分支 port/journey1-mac（独立 worktree ~/Documents/Vit-DAW-journey1-mac）
- 回执（**中期——[等待用户: ~/.vit/config.json 就位后跑权威 run]**，完成面见下）：
  - **LLM 前置先验（卡面先验项）**：mac 侧环境缺失——`~/.vit/config.json` 不存在（agent 唯一文件配置途径，`agent/internal/config/config.go:154` Load→`~/.vit/config.json`→env 回退 `VIT_AGENT_LLM_*`/`OPENAI_*`）；四个 env 均未设、shell profile 无导出、transfer/本机无可复用配置。2026-09-19 用户在场裁定「现在配置」（AskUserQuestion 应答），截至 02:11 配置仍未落盘。**未以桩/伪造应答替代**：脚本内置 LLM preflight 守卫，配置不完整即 exit 2 拒跑（守卫自测通过：exit 2 + report written + prereq 落盘 + 无栈启动无 LLM 调用，不入 §8 有效轮次）。
  - **已交付（不依赖 LLM 的全部文件域工作）**：`scripts/journey1_demo_journey_smoke_mac.sh`（分支 port/journey1-mac，已提交；五段显式断言 S1 工程打开/S2 权限/S3 实验/S4 装载/S5 A-B 试听 + ps1 A1–A4 全语义锚定；A5/C2 起栈模式：假内核根+端口归属断言+TERM→KILL 停栈；隔离契约同 ps1：工程副本/VIT_PROJECT_XML/VIT_HISTORY_DRAFT_ROOT/VIT_ORCHESTRATION_STORE_PATH；早退路径修复=trap 前移，任意 fatal 均完整落 teardown+指纹+report）；`scripts/SMOKE_TESTS.md` 补「Journey 1 Demo Journey Mac (PORT-JOURNEY-1-MAC)」条目。ps1 原文零改动；`agent/`、`VitApp/` 零触碰。
  - **ps1↔mac 段级语义对照表**：①工程副本与源：`D:\Godot\...912.vit`→`~/Documents/vit-daw-frontend/912.vit`（mac 在位同名文件），cp 入 run dir 只操作副本；用户工程与 `.vit_history` 前后 sha256/树指纹断言只读同款。②隔离三 env 同名同语义（VitPaths.h:183 / history.go:2261 / artifacts+mixboard）。③起栈：Start-Process Hidden→`(cd 假根; exec env …) &`+pid/端口归属（A5 增强）；停栈 TERM→KILL+wait+端口释放等待（A1 发现纪律）。④S1 open_project：body 形态 `tool+command+source+confirmed` 与 requires_confirmation 二次提交同款。⑤S2 authority：POST `{authority_mode}` 回显断言同款。⑥S3 实验段：slice 循环（prompt/nudge、waiting_continue 轮询 runtime/status、waiting_clarification/confirmation→nudge 递进）同构；prompt「帮低音轨做个均衡实验，然后让我试听」/nudge「可以执行」/占位文案/A1 词汇表（哪个/方向/选择/请问/你希望/倾向/还是/？）逐字同源——ps1 的 base64/码点构造是 PS5.1 ANSI 码页规避，mac 直接 UTF-8 字面量。⑦A1 判定：选项标记正则 `(?:^|[\s;:.,()\[\]])([A-D])[.、．)）]` + 问号 + 词汇命中同款；delivered text 双面（chat reply + event title/body/summary）同款。⑧S4/A2 确定性探针：rack_add_node 命令形态/zone_id Z3/gate denial delta/plugin_id+plugin_instance_ready/UI 投影（ui/context 选 bass→ui/state）三判定面同构；**唯一机器态替换**：PC promoted 静态 EQ `VST3-bx_hybrid V2-…`（Plugin Alliance，mac 未装）→ mac promoted `VST3-C1 comp Mono-10456661-65e94c5e`（U2 裁定 mac=Waves-only、24 promoted 主体无 EQ 族；探针断言语义="promoted 处理器可装载落地"不变，`--probe-plugin-identifier` 可换）；journey-turn 半边（bass plugin_count 增长）照 ps1 单独记录不进判定。⑨S5/A3：audition.*/mix_tick 事件型 + audition_session_id 原文扫描同款。⑩save point 三步（version_project_save_prepare→save_project→version_project_saved）工具名与参数形态同款。⑪Phase B reopen+A4：stop→restart→重开→dwell 20s→`/agent/state?detail=full` live 会话（graph nodes/messages/commit_count/history_dir）+ stall WARN 计数 + session 目录 rewritten 文件枚举同构（时间基准 epoch vs ps1 ISO parse，语义同）。⑫exit 0/1/2 与 JOURNEY1_MAC_VERDICT 映射同 ps1。卡面增量：five_segment 显式断言块（验收②）、双 bin sha256（验收⑤）、LLM preflight 守卫、orchestration store 隔离与 `-vsp-hub-url ""`（A5/C2 旅程中性增强）。
  - **§8 运行账目（中期）**：有效轮次 **0**（无任何 run 抵达实验段）；守卫自测 3 次均为 LLM 前置拒绝（各 workdir 留 exit 2 + report 证据，测试目录已清理）；无止损触发。
  - **权威 run 预案（配置就位后）**：复验存在性（不打印 key）→ `./scripts/journey1_demo_journey_smoke_mac.sh --kernel-bin ~/Documents/Vit-DAW/VitApp/build/VitApp_artefacts/Debug/VitApp`（内核=9-18 23:47 构建，晚于最后一次 VitApp 源码变更 9-17 0ea259a，sha256 fd0d3d84… 入工件；agent 每次 go build 新建）→ ≤3 轮有效、两败止损。
- 验收：
