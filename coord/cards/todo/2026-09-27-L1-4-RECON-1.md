# L1-4-RECON-1：上下文装配现状盘点（四层前缀与退场机制的前置勘察）

- 优先级 / 预估 / 依赖：P2 / 0.4 天 / 路线图 D6+L1-4 段（AGENTIC-OBSERVATION 蓝图）；夜间托管卡池成员
- 模型分级：L1 / flash 可接（纯只读勘察+报告，零代码改动）
- **执行侧（mac 会话，夜间托管）**
- 目标：
  1. **现行上下文装配链**：每轮 LLM 调用的上下文由什么构成、按什么顺序装配——promptruntime/contextruntime/ccb_model_prompt（含 `evidence_refs` 占位符处）的装配代码结构；哪些块每轮固定（=事实上的稳定前缀）、哪些动态注入；锚点文件:行。
  2. **LLMContext 结构盘点**：各投影的 LLMContext 字段形态（compact facts 与 raw package 排除位），≥6 个投影逐一列出结构+`do_not_include_raw_package` 标志现状。
  3. **相位轮次的装配角色**：自由态相位（FS0-FS9）/轮边界 AdvancePhase/phase 上下文键在上下文中的表达——路线图裁定"相位轮次降级为冷启动第一轮"，现状它是怎么推送的（锚点）。
  4. **退场机制现状**：轮次间什么保留什么丢弃——execution_memory 的记忆面、conversation 图的消息裁剪、历史压缩；evidence refs 在历史消息中的存续形态（旧轮的 ref 还在吗、能重拉吗——与 L1-1-RECON-1 报告互证处注明）。
  5. **D6 四层映射表**：agent 规则/用户偏好/环境实例/工程账本四层各自的现存载体最接近物（如 agent 提示词常量/设置文件/环境指纹/决策记录——一一对照，缺的明示"无载体"）。
  6. 报告落 `coord/runs/L1-4-RECON-1/CONTEXT_ASSEMBLY_INVENTORY.md`。
- 约束：零代码改动；锚点带文件:行；token 体积若无法精确测，给出估算方法与量级判断即可，不编数。
- 验收：①装配链完整（从触发到 prompt 成形）②LLMContext ≥6 投影 ③相位表达锚点 ④退场现状+refs 存续结论 ⑤四层映射表 ⑥报告入库
- 停止条件：装配链多层入口盘不完 → 成稿+剩余列明上交
