# L2-2-SEG-1：段落发现层 v1 实现——SegmentationPrimitives 特征产出（onset 密度+能量 novelty，确定性断言）

- 优先级 / 预估 / 依赖：P1 / 0.6 天 / L2-2-SEG-RECON-1 已验收（`coord/runs/L2-2-SEG-RECON-1/SEGMENT_DSP_INVENTORY.md` 为落点权威输入：K1 帧序列内存现算/K4 包络瓦片/K7 双渲染确定性范式/K8 SegmentationPrimitives 槽位）；VitApp 域（内核 C++）
- 模型分级：L2 / GLM 亲自或强督导（内核扩展，ctest 双配置门）
- **执行侧（PC 会话）**
- 目标（v1 范围钉死——只做 primitives 产出，不做边界切分/段落命名/marker 写入）：
  1. **红先行**：SegmentationPrimitives 特征产出的 ctest 红测试（修前该特征类型无产出=红）：①同一段真实音频分析两次，primitives 逐字节一致（确定性重放——K7 范式）②primitives 内容含 onset 事件序列与能量 novelty 曲线且结构非空 ③无音频/空轨 → 特征状态 missing/not_evaluable 语义（不静默空）。
  2. **实现**：`AudioFeatureType::SegmentationPrimitives`（槽已注册 AudioFeatureTypes.h:18）挂入 L3 分析管线：onset 密度事件（复用 K2 瞬态/频段事件存量）+ 能量域 novelty（K4 波形包络短窗能量差分）——帧粒度与既有特征对齐；序列级发布走既有 feature snapshot 通道（K1 所述"序列级发布"缺口即本卡补的点）；参数限幅（K7 先例）。
  3. **SSM 不做**（勘察结论：零存量需补算，v2）；边界切分/阈值算法不做（发现层 primitives 就位即可，切分=下一卡）。
  4. **门**：ctest Debug+Release 双配置全绿（含既有 6 项零回退）+ 新增测试全绿；`go test ./...` agent 侧零回退（若 agent 消费面未动则只需确认编译面无涉）。
  5. 回执：红绿退出码+diff stat+HEAD+新特征的一帧样例（JSON 摘录）；端侧烟测由决策侧验收后另开腿卡（真栈 feature snapshot 落盘断言）。
- 约束：零既有特征行为改动（DOM/MOM/COM 等消费面不碰）；确定性是硬验收（重放不一致=红）；文件域 VitApp/Source+VitApp/Tests。
- 验收：红绿 T1-T3+双配置 ctest 绿+既有零回退
- 停止条件：实现中发现 K1/K4 锚点与现实不符（帧序列实际不可得/包络瓦片粒度不符）→ 停止，冲突实证上交（勘察修订而非硬凑）
