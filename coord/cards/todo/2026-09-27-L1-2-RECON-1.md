# L1-2-RECON-1：投影计算触发链与依赖图盘点（物化+增量失效的前置勘察，G2 输入）

- 优先级 / 预估 / 依赖：P1 / 0.5 天 / 路线图 D4+L1-2 段（AGENTIC-OBSERVATION 蓝图）；与 L1-1-RECON-1 同族（那张盘"引用格式"，这张盘"计算与依赖"）
- 模型分级：L1 / flash 可接（纯只读勘察+报告，零代码改动，适合夜间长跑）
- **执行侧（mac 会话，夜间窗口）**
- 背景：路线图 D4 要把投影从"按请求现算"改为"事件驱动物化+依赖图增量失效"，G2 质量门=失效正确性专项（漏标脏传播=静默错误数据）。本卡把现状的触发链、依赖边、变更事件源盘成事实清单。
- 目标：
  1. **计算触发链矩阵**：九投影（dom/mom/tim/tom/fxm/com/epm/rlm/acousticpackage）逐个回答——谁触发计算（请求驱动/事件驱动/轮询）、计算入口函数（文件:行）、计算时机（装载/变更/render/会话轮）、结果去向（内存/落盘/进 CCB）。附 DAD 证据层（audioclosure/frequencycleanup）同款。
  2. **投影间依赖边清单**：静态读码梳理"X 投影消费 Y 投影/数据源的什么"（如 MOM 消费 acousticpackage 的哪些字段、COM 依赖 render 结果的什么、FXM 的 A/B 对如何锚定）——每条边带两端锚点；标注哪些边是显式代码依赖、哪些是时序隐式依赖（上游没算完下游拿到旧数据）。
  3. **变更事件目录**：内核→agent 的事件流里与投影输入相关的事件全集（事件名/粒度/携带信息/当前哪些投影响应它）——这是未来"变更事件沿依赖图脏传播"的事件源候选目录。
  4. **现存失效/缓存机制盘点**：已有的缓存、快照、freshness 标注（BELL/F5 链的产物）逐一列出语义——哪些可作物化层的种子。
  5. 报告落 `coord/runs/L1-2-RECON-1/PROJECTION_PIPELINE_INVENTORY.md`。
- 约束：零代码改动；锚点带文件:行；隐式依赖必须给时序证据（如两个字段的写入顺序）不得凭感觉标注。
- 验收：①触发链矩阵覆盖九投影+DAD ②依赖边 ≥10 条两端锚点 ③事件目录 ≥15 个事件 ④现存机制 ≥3 个语义说明 ⑤报告入库
- 停止条件：同 L1-1-RECON-1——盘不完的包成稿+剩余列明上交拆卡
