# L2-2-SEG-2：段落边界切分 v1——primitives→确定性边界→marker 写入（发现层闭环）

- 优先级 / 预估 / 依赖：P1 / 0.6 天 / SEG-1 primitives 已合入（内核特征产出在位）+SEG-RECON 勘察（marker 承载=project.markers.apply_section_markers 现成，section_id/source/confidence 字段语义）；设计内嵌卡（勘察已给落点，范围钉死线性算法）
- 模型分级：L2 / GLM 亲自或强督导
- **执行侧（mac 会话，夜池）**
- 目标（v1 钉死：确定性线性阈值切分，不做自适应/学习式）：
  1. **红先行**：边界切分纯函数表驱动测试（合成 primitives 输入→预期边界位置）+marker 写入断言。
  2. **实现**（agent 侧消费内核 primitives）：①切分算法纯函数：onset_density+energy_novelty 两信号（SEG-1 载荷）线性组合的阈值边界检测——参数全部显式常量+透出（确定性范式）；边界最短段长约束（防碎片段）；degenerate 输入（NE primitives）→不产生边界+显式 NE 结论。②中性 ID S1…Sn 分配+marker 写入：`project.markers.apply_section_markers`（section_id=S 前缀中性 ID，source="segmentation_primitives_v1"，confidence=确定性算法显式置信规则——如边界两侧信号差强度分档，非编造概率）。③A/B 同素材两轮切分一致性断言（确定性重放）。
  3. **门**：agent 全量 84 包 0 FAIL+红绿；marker 写入走内核命令（真栈面留烟测腿，本卡单测/组件级+命令构造单测）。
  4. 回执：红绿+diff stat+切分样例（合成输入→边界表摘录）。
- 约束：命名层零实现（"像 hook"假设标签=后续卡）；阈值参数常量化透出（不发明自适应）；文件域 agent/internal 新包或 acousticpackage 相邻域+不碰内核。
- 验收：红绿+全量 0 FAIL+确定性断言在案
- 停止条件：primitives 载荷在 agent 侧实际消费面缺失（特征快照→agent 通道不通）→ 取证上交（通道=内核腿，域外）
