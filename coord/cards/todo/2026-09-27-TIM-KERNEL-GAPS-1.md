# TIM-KERNEL-GAPS-1：TIM 断言器内核腿缺口八项细化（设计 §5 展开，供后续内核卡排程）

- 优先级 / 预估 / 依赖：P2 / 0.3 天 / L2-1-TIM-DESIGN-1 已验收（设计文档 `docs/TIM_ASSERTER_V1_DESIGN.md` §5 缺口清单为输入）；夜间托管卡池成员
- 模型分级：L1 / flash 可接（纯只读勘察+报告，零代码改动，VitApp 域）
- **执行侧（mac 会话，夜间托管）**
- 目标：把设计文档 §5 的内核腿缺口清单（8 项，如 true peak 4x 过采样、块长一致性、per-instance 装载态出 get_project_state、轨间 send/aux 拓扑等——以设计文档实际清单为准）逐项展开：
  1. **每项四要素**：实现锚点（内核侧现状最接近的文件:行——现有分析设施/数据结构离目标的距离）+ 工作量评估（小时级粗估+依据）+ 依赖（内核设施是否已存在/需先建什么）+ 排卡建议（独立卡/与哪项合并/优先级理由）。
  2. **共性基础设施识别**：多项共享的前置（如过采样工具/分析器挂载点/get_project_state 扩展模式）——共享前置单列，避免后续排卡重复建设。
  3. **风险标注**：每项的 Technical 风险（如 true peak 的 BS.1770 合规性、过采样对内核实时性的影响面）。
  4. 报告落 `coord/runs/TIM-KERNEL-GAPS-1/KERNEL_GAP_EXPANSION.md`。
- 约束：零代码改动；锚点带文件:行；工作量评估必须给依据（代码量/复杂度参照），不拍脑袋数字。
- 验收：①设计 §5 清单逐项覆盖（8 项±以文档为准）②每项四要素齐 ③共性前置识别 ≥1 组 ④报告入库
- 停止条件：设计 §5 与文档实际清单不符（分支未合入等）→ 停止上交，不按错误清单展开
