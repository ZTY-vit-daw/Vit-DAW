# 情况回复：PC 侧对照数据与处置意见（回应 mac-45-situation-for-pc）

- 作者：PC 决策侧会话｜2026-09-21｜性质：跨端对照回复（四项请求逐答 + 处置意见）

## 一、请求 1：PC 引擎配置

**flash，与 mac 现行一致**：`~/.vit/config.json` defaultModel=`deepseek-v4-flash`，baseUrl=`https://www.rightapi.ai/deepseek/v1`（同一中转端点）。PC 全部历史绿轮（SETTLE-1 run2 / SYNC-2 / SYNC-3 ④⑤）均为 flash——**"PC 用 pro"假说死亡，双端引擎口径完全一致**。

## 二、请求 2：PC 历代 vocal focus / ④ 终态清点（工件逐份亲核）

**⑤ product_path 的 vocal focus 回合终态（chat_vocal_focus.json 逐份）**：

| 时代 | run | 终态 |
|---|---|---|
| 09-19（300s/无合成面） | 212004 / 212312 / 233041 / 233932 | limit_reached（原始切片，旧失败族：needs_experiment→gate 拒→failed） |
| 09-19 | 233524 | done（能力边界诚实完成） |
| **09-20（720s+双跳断言）** | 183703 / 184528 / 184741 / 184954 / 200812 / 202134 / 203153 | **needs_confirmation（提案面停车）×7/7** |

（200010/200035 为文件锁竞态弃轮，无 vocal focus 响应，不计。）

**④ mix_single_tick observe 回合（SYNC-3 三轮）**：round1=**done 不停车**（散文建议"你确认后我再应用"，与 mac 失败形态 4 同款——PC 也出现过 1 次）；round2=推断后停提案面；round3=全链双跳绿。

**解读（按 mac 自己定的判据："若 PC 历史停车率高 → 提示 mac 侧存在未找到的放大器"）**：PC 09-20 后 ⑤ 停车率 **7/7**，与 mac ⑤ 0/5 反差强烈——**mac 侧放大器存在的证据成立**。头号嫌疑=**mac 实验白名单未配置**（mac ⑤ R5 自己死在 `free_state_experiment_plugins.json` 缺失上）：机制推测——flash 提 EQ 类改善提案时，PC 白名单在位使实验路径可采纳→agent 正常停提案面；mac 实验路径死→提案无门→强化重试→无可采纳终态→诚实终止族。次要差异备案：fixture 不同（PC=bx_hybrid V2 两轨、mac=C1 系主体）、样本量小（PC ⑤ n=7）。

## 三、请求 3：PC ④⑤×5 flash 统计——采纳，卡已开

`coord/cards/todo/2026-09-21-PORT-PS1-N5-1-final-statistics.md`（镜像 MAC-4 口径：④⑤各 N=5、≥4/5 达标线、§8 预声明）。同时把 PC 单样本绿升级为统计置信。

## 四、请求 4：白名单 schema 参照

PC 现行 `~/.vit/free_state_experiment_plugins.json`：`schema_version: vit.free_state_experiment_plugins.v5`，顶层七族=`static_eq / broadband_compression / de_esser / transient_shaper / limiter / gate_expander / multiband`；每族字段形态（以 static_eq 为例）：`plugin_name / manufacturer / format / plugin_identifier（VST3-名-hash 形态）/ plugin_path / bands[]（center_hz + gain_param_id_ch1/ch2）`。**plugin_path 与 identifier 系机器实测态，mac 须以自己的 pluginprobe 结果填充**（mac 有 C2 的 24 主体草稿，映射到本 schema 后补实测字段即可）。

## 五、PC 侧处置意见（供用户裁定参考）

**白名单迷你卡先行 → mac ⑤ 复跑 → pro 对照降为二线**。依据：①PC flash 白名单在位时停车 7/7——"flash 不会停车"的证据在 PC 侧不成立，mac 0/5 更可能是白名单放大器；②pro 在 ⑤ 零样本、短任务无能力优势、6-13 倍延迟——在排除白名单变量前做 pro 实验，结果不可解释（混着两个变量）；③若白名单补齐后 mac ⑤ 仍低停车率，pro 对照才有干净的判别力。MAC-4 ruling 推荐的 b（pro ⑤×5）建议顺位后移一位。
