# 双端任务同步快照（2026-09-24 晚，PC 决策会话组装）

> 两端 origin/main 零差量。本文件为双端共同视图；后续各端领取/完成以 coord 卡片状态机为准。

## 一、今日已闭环（"全量可选"弧线，双端 8 卡）

| 卡 | 端 | 结果 |
|---|---|---|
| FIX-PLUGIN-SELECT-1 + MAC-1 | PC→mac | LLM 插件自选机制复活+mac 实证（v6 schema+membership+有界披露） |
| FIX-D1-PLUGIDENT-1 | mac→双端 | WaveShell 成员 identifier 直达（七族 resolver） |
| FIX-BROADBAND-SHARED-1 | PC | broadband 共享阈值形态解锁 |
| FIX-PCA-AUTOSWEEP-1 | PC | 全库批量认证扫描工具入仓（7→53 条目） |
| FIX-PCA-EQCHANNEL-1 | PC | EQ 认证通道接入（EQ 1→6；PC 58 条目七族全自选） |
| PORT-PCA-FULL-CANDIDATES-1 / PC-1 | mac/PC | 构建器入仓+七族派生（mac 23 条目底座；PC 盘点校准） |
| PORT-PCA-AUTOSWEEP-MAC-1 | mac | mac 全库 sweep（23→47 条目；跨平台确定性 0/705 分歧实证） |
| FORENSIC-MAC-CLARIFY-CHAIN-1 + FIX-REPAIR-CLARIFY-DEATH-1 | mac | clarify 链失败定责（双方差，非 F2 回归）+修复验收 |

**双端终态**：PC 58 / mac 47 白名单条目，共享 Waves 集逐主体一致，七族全部多候选自选；演示风险族（clarify 判死）已修复闭环。

## 二、当前队列（todo/ 四张，doing/ 空）

| 卡 | 端 | 优先级 | 前置 | 备注 |
|---|---|---|---|---|
| FIX-PCA-CERTAUTH-TOKEN-1 | PC 开发（双端受益） | P2 | 无 | 解锁 104 例（PC 57+mac 47）static_eq 认证——EQ 候选增长唯一闸门；~40-60 行 agent 改动须分支+决策侧合入 |
| FIX-KERNEL-PLUGINLIST-HYGIENE-1 | kernel C++（L3） | P2 | 需 PC 决策侧共建裁定 | 994 死条目+取消/崩溃隔离；演示后 |
| FIX-GD-TELEMETRY-BELL-1 | Godot 仓 | P2 | 无 | 前端第三写者标注 |
| FIX-TEST-CTXSYMLINK-1 | mac 验证 | P3 | 无 | 半小时小修 |

## 三、无卡事项（按建议优先级）

1. **演示录屏兜底**（demo-capture SOP，尚未执行过——演示前最值的一小时）；
2. 演示剧本定稿：PC 可演"24 压缩器自选"（Pro-C 2 实证）/EQ 6 选 1（bx_hybrid 实证）；mac 可演 EQ 7 选 1（Q10 实证）——均在真栈 all_green 过的场面；
3. PC 修复后 N=5 统计（论文对照表"修复后"列；mac 已有）；
4. pro 对照（引擎差判别，可选）；
5. GUI 集成+跨机认证目录（答辩后产品化，未开卡，用户示意后记 backlog）；
6. 观察项（无卡，复发才开）：D1-S1 base revision 竞态、A4 会话恢复签名、F4 模型方差、masking 缺口。

## 四、建议下一步（PC 决策会话意见，mac 决策会话可并行补充）

- **演示前**：录屏兜底 + 剧本定稿（不开发新面）；
- **演示后第一张**：CERTAUTH-TOKEN（解锁 104 例，EQ 池双端扩容，mac 侧随即幂等吸收）；
- **其后**：K-HYGIENE（先出共建裁定）→ GD-TELEMETRY / CTXSYMLINK 收尾；
- 统计与 pro 对照按论文需要插空。
