# TASK_TEMPLATE.md — 任务开场模板

用途：sol→terra 交接物 + 所有 AI 会话的任务开场。每个新任务开始前，复制下方模板填好四项，作为会话第一条消息的一部分。填写原则：目标一句话说清；范围里"明确不做什么"与"做什么"同等重要；验收命令必须是你自己会运行且看得懂输出的命令；参考只指向现行文档（以 CURRENT-STATE.md 索引为准）。

```text
目标：<一句话>
涉及文件/范围：<路径列表；明确不做什么>
验收命令：<一条你自己会运行且看得懂输出的命令>
参考：<相关现行文档章节>
```

示例（真实任务应替换为实际内容）：

```text
目标：修复 chat 包测试把运行时状态写入源码树的泄漏
涉及文件/范围：agent/internal/chat/*_test.go；不改非测试代码
验收命令：cd agent && go test ./internal/chat -count=1
参考：docs/G0_C2_BASELINE_2026-08-17.md 健康检查章节
```
